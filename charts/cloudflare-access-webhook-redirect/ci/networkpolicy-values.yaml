application:
  handler:
    targetBase: "https://test.google.com"
    paths:
      api/webhook:
        - ALL
  cloudflareAccess:
    clientId: "testId"
    clientSecret: "testSecret"

networkPolicy:
  enabled: true
  egress:
    enabled: true
    https:
      enabled: true
