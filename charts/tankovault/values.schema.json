{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
    "allowUnsafeExposure": {
      "default": false,
      "description": "Permit `control-plane`, `sync`, `render` and `challenge-solver` to use a Service type\nother than `ClusterIP`. Off by default and validated, because those four expose privileged\ncontracts: upstream publishes none of them even on a single host, and reaching them directly\nbypasses both the frontend proxy and the rate limiter.",
      "required": [],
      "title": "allowUnsafeExposure",
      "type": "boolean"
    },
    "anilist": {
      "additionalProperties": true,
      "properties": {
        "clientId": {
          "default": "",
          "description": "AniList OAuth application client ID. Register at \u003chttps://anilist.co/settings/developer\u003e.",
          "required": [],
          "title": "clientId",
          "type": "string"
        },
        "clientSecret": {
          "default": "",
          "description": "AniList OAuth application client secret.",
          "required": [],
          "title": "clientSecret",
          "type": "string"
        },
        "redirectUri": {
          "default": "",
          "description": "OAuth redirect URI. Left empty it is derived from the ingress as\n`\u003cexternal URL\u003e/account/anilist-callback`. It must point at the **frontend**, not the API:\nthe API callback would need the SPA's in-memory bearer token, which a browser redirect\ncannot carry.",
          "required": [],
          "title": "redirectUri",
          "type": "string"
        },
        "tokenEncryptionKey": {
          "default": "",
          "description": "Base64 of exactly 32 bytes, sealing every user's AniList token at rest. Left empty the\nchart generates one when `services.sync` is enabled and remembers it across upgrades, which\nis the recommended setting; set one explicitly (`openssl rand -base64 32`) only if it has to\nbe known outside the release. Losing it forces every account to re-link; leaking it exposes\nevery stored token. Rotating it does not re-seal tokens already stored.",
          "required": [],
          "title": "tokenEncryptionKey",
          "type": "string"
        }
      },
      "required": [],
      "title": "anilist"
    },
    "auth": {
      "additionalProperties": true,
      "properties": {
        "jwtSecret": {
          "default": "",
          "description": "Token signing secret (`auth.jwt_secret`). Left empty the chart generates one and remembers\nit across upgrades, which is the recommended setting; set one explicitly (minimum 32\ncharacters, e.g. `openssl rand -hex 32`) only if it has to be known outside the release. The\nknown upstream placeholder is refused at boot in every profile, and rotating the value signs\nevery user out.",
          "required": [],
          "title": "jwtSecret",
          "type": "string"
        },
        "mfaEncryptionKey": {
          "default": "",
          "description": "Base64 of exactly 32 bytes (`auth.mfa_encryption_key`), sealing every enrolled TOTP secret\nat rest. A TOTP secret is symmetric, unlike a password hash — whoever reads the column can mint\nthat account's codes — so a database dump must not be enough on its own. Left empty the chart\ngenerates one when `services.api` is enabled and remembers it across upgrades, which is the\nrecommended setting; set one explicitly (`openssl rand -base64 32`) only if it has to be known\noutside the release. **It must never change once anyone has enrolled**: rotating or losing it\nlocks every enrolled account out of its second factor, and the only repair is clearing\n`user_totp` and having them enrol again. Deliberately not derived from `jwtSecret`, which is\nroutine to rotate. Unset disables authenticator-app enrolment only — security keys and\nrecovery codes still work — but the seeded administrator holds permissions that require a\nsecond factor, so unset leaves it only a hardware key.",
          "required": [],
          "title": "mfaEncryptionKey",
          "type": "string"
        },
        "passwordPepper": {
          "default": "",
          "description": "Server-side pepper mixed into every argon2id hash, so a database leak alone cannot be\nbrute-forced offline. Left empty the chart generates one **on a first install only** and\nremembers it across upgrades; a release that already exists without a pepper keeps running\nwithout one, because every password stored unpeppered would stop verifying the moment one\nappeared. For the same reason it must never change once set: rotating or losing it\ninvalidates every stored password, so back the Secret up. The `seed-admin` step receives the\nidentical value, or the administrator it creates could never log in.",
          "required": [],
          "title": "passwordPepper",
          "type": "string"
        }
      },
      "required": [],
      "title": "auth"
    },
    "bootstrap": {
      "additionalProperties": true,
      "description": "Schema migration and first-install seeding, all from the `bootstrap` image. Nothing\npublished carries a destructive command; resetting the schema is not available in any image.",
      "properties": {
        "image": {
          "additionalProperties": true,
          "properties": {
            "repository": {
              "default": "timschoenle/tankovault-bootstrap",
              "description": "Image repository.",
              "required": [],
              "title": "repository",
              "type": "string"
            },
            "tag": {
              "default": "v7.0.0@sha256:2432d757e08fc3f72ee1ab32b6090fccf7f1f863db6f874ed03c360d00c44ed1",
              "description": "Image tag, pinned by digest.",
              "required": [],
              "title": "tag",
              "type": "string"
            }
          },
          "required": [],
          "title": "image"
        },
        "migrate": {
          "additionalProperties": true,
          "properties": {
            "argoSyncWaveBase": {
              "default": 0,
              "description": "Sync wave the migration Job takes under `ordering: argoSyncWave`; the workloads take\nthis plus one, which is what reproduces the `pre-upgrade` guarantee — Argo CD holds a wave\nuntil the previous one is healthy, and a Job is healthy only once it is Complete. Anything\nthe migration itself depends on — the ExternalSecret carrying `database__url`, a database\nsome operator provisions — must be given a wave strictly below this one.",
              "required": [],
              "title": "argoSyncWaveBase",
              "type": "integer"
            },
            "backoffLimit": {
              "default": 3,
              "description": "Retries before the migration Job is considered failed.",
              "minimum": 0,
              "required": [],
              "title": "backoffLimit",
              "type": "integer"
            },
            "mode": {
              "default": "auto",
              "description": "How `bootstrap migrate` runs. `job` is a `pre-install,pre-upgrade` Helm hook, correct\nwhen the database already exists. `initContainer` runs it in every service pod, which is\nwhat the bundled PostgreSQL needs — a pre-install hook would run before the StatefulSet\nexists and could only ever fail. Concurrent runs are safe: sqlx takes a Postgres advisory\nlock. `auto` picks `initContainer` when `postgresql.enabled`, otherwise `job`.",
              "enum": [
                "auto",
                "job",
                "initContainer",
                "disabled"
              ],
              "required": [],
              "title": "mode"
            },
            "ordering": {
              "default": "helmHook",
              "description": "How the migration Job is ordered against the workloads. Ignored unless `mode` resolves\nto `job`, and irrelevant under plain `helm install`, which has no phases to order across.\n`helmHook` is the `pre-install,pre-upgrade` hook, which Argo CD compiles to the PreSync\nphase — and PreSync runs before **every** Sync-phase resource, with sync waves ordering\nonly within a phase. So when `database__url` arrives from an ExternalSecret, a first\ninstall deadlocks and never recovers: the Job's projected secrets volume names a Secret\nthat does not exist yet, the pod sits in ContainerCreating on FailedMount, the Job never\ncompletes, PreSync never finishes, Sync never starts, and the ExternalSecret that would\nhave created that Secret is therefore never applied. Nothing inside PreSync can break the\ncycle. `argoSyncWave` drops the hook annotations entirely and orders the Job by sync wave\ninside the Sync phase instead, where the ExternalSecret reconciles alongside it.",
              "enum": [
                "helmHook",
                "argoSyncWave"
              ],
              "required": [],
              "title": "ordering"
            }
          },
          "required": [],
          "title": "migrate"
        },
        "resourcesPreset": {
          "default": "small",
          "description": "Resource t-shirt size for the bootstrap containers.",
          "enum": [
            "",
            "nano",
            "micro",
            "small",
            "medium",
            "large"
          ],
          "required": [],
          "title": "resourcesPreset"
        },
        "seedAdmin": {
          "additionalProperties": true,
          "properties": {
            "email": {
              "default": "",
              "description": "Administrator email address.",
              "required": [],
              "title": "email",
              "type": "string"
            },
            "enabled": {
              "default": false,
              "description": "Create the first administrator on install. Create-only: re-running changes nothing.\nThis is the only account privilege is ever minted for, since registration confers none.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "password": {
              "default": "",
              "description": "Initial administrator password. Left empty the chart generates one and remembers it\nacross upgrades; NOTES.txt prints the command that reads it back out of the Secret. It is\ncreated with the same `auth.passwordPepper` the API runs with, because a mismatch produces\nan account that can never log in.",
              "required": [],
              "title": "password",
              "type": "string"
            },
            "username": {
              "default": "admin",
              "description": "Administrator username.",
              "required": [],
              "title": "username",
              "type": "string"
            }
          },
          "required": [],
          "title": "seedAdmin"
        },
        "seedProviders": {
          "additionalProperties": true,
          "properties": {
            "enabled": {
              "default": false,
              "description": "Install the built-in provider presets. Each can be disabled or retargeted from the\nadmin console afterwards.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            }
          },
          "required": [],
          "title": "seedProviders"
        }
      },
      "required": [],
      "title": "bootstrap"
    },
    "branding": {
      "additionalProperties": true,
      "description": "What this deployment calls itself. The name, the wordmark, the tagline, the copyright line,\nthe licence label and the two project links all come from here rather than from a literal in the\nbinary, so rebranding a fork is a values change instead of a rebuild of the WASM bundle and the\ndesktop installers. Three services read it: `api` publishes the reader-facing subset\nunauthenticated at `/v1/branding` — the SPA's rail, footer and document title are built from it,\nand it stays open on a deployment with the account gate on, because the sign-in screen draws the\nwordmark — and stamps the name into transactional email, the WebAuthn prompt and the TOTP issuer;\n`frontend` writes it into the served app shell so a tab and a link unfurl are named before the\nWASM bundle boots; `worker` reads `botUserAgent` alone.\n\nEvery key is optional, and every empty one is left out of the rendered configuration, so an\nuntouched block reproduces the shipped identity exactly. This is for a deployment that is not\nTankoVault; a normal install fills in none of it.",
      "properties": {
        "botUserAgent": {
          "default": "",
          "description": "The identifiable crawler user-agent the `worker` sends, applied to any provider still\ncarrying the built-in default; a provider whose politeness settings name their own keep it.\nNever published to clients. Set it on a fork so the sites it crawls can attribute the traffic\nto you and reach you about it — `MyDeployment/1.0 (+https://example.org/bot)` is the shape\noperators expect.",
          "required": [],
          "title": "botUserAgent",
          "type": "string"
        },
        "copyright": {
          "additionalProperties": true,
          "description": "The footer's copyright line.",
          "properties": {
            "holder": {
              "default": "",
              "description": "Who holds it. Empty keeps `Tim Schönle`, which is upstream's — set it on any deployment\nthat publishes its own footer.",
              "required": [],
              "title": "holder",
              "type": "string"
            },
            "notice": {
              "default": "",
              "description": "The whole notice, verbatim, for when `© {year} {holder}` does not fit. Wins over `holder`\nand `year` **and** over the catalogue's translation of the line, so those two become dead\nconfiguration next to it and the chart refuses the pair.",
              "required": [],
              "title": "notice",
              "type": "string"
            },
            "year": {
              "default": "",
              "description": "A year or a range (`2024–2026`). Empty resolves per response to the current year, so a\ndeployment nobody has touched since December is not still claiming last year. Quote it:\na bare `2026` is an integer to YAML and a string to the service.",
              "required": [],
              "title": "year",
              "type": "string"
            }
          },
          "required": [],
          "title": "copyright"
        },
        "licence": {
          "additionalProperties": true,
          "description": "How the deployment's own code is licensed, as printed in the footer. This is a label the\nfooter prints, not a licence grant: it says nothing about what you may do with the upstream\ncode, which is governed by the licence the source ships under.",
          "properties": {
            "name": {
              "default": "",
              "description": "What the footer prints. Empty keeps `PolyForm Noncommercial 1.0.0`.",
              "required": [],
              "title": "name",
              "type": "string"
            },
            "url": {
              "default": "",
              "description": "Absolute `http(s)` URL the label links to. Empty renders it as plain text, which is what\na self-hosted deployment with nowhere to point should show rather than a dead link.",
              "required": [],
              "title": "url",
              "type": "string"
            }
          },
          "required": [],
          "title": "licence"
        },
        "name": {
          "default": "",
          "description": "The product name, in prose. Substituted for `{brand}` in every translated message that names\nit, and used as the WebAuthn relying-party name and the TOTP issuer unless\n`config.auth.webauthn_rp_name` / `config.auth.totp_issuer` name one. Empty keeps `TankoVault`.\n\nRenaming this does **not** re-key existing passkeys or authenticators: the relying party is\nidentified by `webauthn_rp_id`, and a TOTP secret already provisioned keeps the issuer it was\nenrolled with. Only newly enrolled credentials carry the new name.",
          "required": [],
          "title": "name",
          "type": "string"
        },
        "projectUrl": {
          "default": "",
          "description": "Absolute `http(s)` URL for the footer's source link and the desktop client's About tab.\nEmpty keeps this project's repository.",
          "required": [],
          "title": "projectUrl",
          "type": "string"
        },
        "releasesUrl": {
          "default": "",
          "description": "Absolute `http(s)` URL where a reader downloads the native client. Advertised by the web\nbuild only. Empty keeps this project's latest release.",
          "required": [],
          "title": "releasesUrl",
          "type": "string"
        },
        "tagline": {
          "default": "",
          "description": "One line under the wordmark. Empty keeps the shipped tagline, which is **translated**;\nsetting it replaces that with this one string in every language. That is the right trade only\nfor a deployment the shipped sentence does not describe.",
          "required": [],
          "title": "tagline",
          "type": "string"
        },
        "wordmark": {
          "additionalProperties": true,
          "description": "The two-tone lockup drawn in the SPA's rail and footer.",
          "properties": {
            "accent": {
              "default": "",
              "description": "The half drawn in the accent colour. Empty draws `lead` alone. Meaningless without\n`lead`, and the chart refuses that combination rather than rendering a lockup nothing reads.",
              "required": [],
              "title": "accent",
              "type": "string"
            },
            "lead": {
              "default": "",
              "description": "The half drawn in the body colour. Setting this is what opts into a two-tone lockup:\nleft empty, a deployment that set `name` draws that name as one word, because the shipped\n`Tankō`/`Vault` split is a property of the shipped name and not a template to pour another\none into.",
              "required": [],
              "title": "lead",
              "type": "string"
            }
          },
          "required": [],
          "title": "wordmark"
        }
      },
      "required": [],
      "title": "branding"
    },
    "channels": {
      "additionalProperties": true,
      "description": "Notification fan-out targets read by the `notifier` service.",
      "properties": {
        "discordWebhookUrl": {
          "default": "",
          "description": "Discord webhook URL. Empty disables the channel.",
          "required": [],
          "title": "discordWebhookUrl",
          "type": "string"
        },
        "emailTo": {
          "description": "Static recipient addresses for new-chapter notifications. Empty disables the channel.",
          "items": {
            "required": [],
            "type": "string"
          },
          "required": [],
          "title": "emailTo",
          "type": "array"
        },
        "webhookUrl": {
          "default": "",
          "description": "Generic outbound webhook URL. Empty disables the channel.",
          "required": [],
          "title": "webhookUrl",
          "type": "string"
        }
      },
      "required": [],
      "title": "channels"
    },
    "cloudflare": {
      "additionalProperties": true,
      "description": "What the frontend's Content-Security-Policy concedes to Cloudflare, one flag per product.\nBoth default off and both belong off on a deployment that is not behind Cloudflare: each admits\nsomething the policy otherwise refuses, and an unused concession is only a weakness. Nothing\nelse Cloudflare's bot products need is opened here — the `/cdn-cgi/challenge-platform/` scripts\nand their beacons are same-origin, so `script-src 'self'` and `connect-src 'self'` already\ncover them. These are the only two things `'self'` can never reach.",
      "properties": {
        "scriptNonce": {
          "default": false,
          "description": "Send a freshly minted `'nonce-…'` in `script-src` on every response. Set this when the zone\nruns Bot Fight Mode, JavaScript Detections or the challenge platform: each injects an inline\n`\u003cscript\u003e` at the edge, *after* the frontend has hashed the shell, so `script-src` refuses it\nand the detection silently never runs. Cloudflare reads the nonce out of the response header\nand copies it onto what it injects, so nothing is stamped into the shell and the shell's own\ninline scripts keep being admitted by hash. Decide this rather than setting it by default: an\ninjected script that can already run could read the header back off a same-origin fetch and\nadmit further inline script. It cannot forge a nonce ahead of time (128 CSPRNG bits, minted\nper response) and it still reaches neither `'unsafe-eval'` nor an off-origin host — but that\nargument rests on the shell being served `Cache-Control: no-cache`, so **do not put an edge\ncache in front of the shell**. A cached shell pins one nonce across every reader for the\nlifetime of the entry, which is `'unsafe-inline'` with extra steps.",
          "required": [],
          "title": "scriptNonce",
          "type": "boolean"
        },
        "turnstile": {
          "default": false,
          "description": "Admit `https://challenges.cloudflare.com` in `script-src` and `frame-src`, for a Turnstile\nwidget embedded **in** a page the frontend serves. One flag reaches both directives because\nTurnstile loads `api.js` and then frames the widget from that same host; admitting the script\nwithout the frame renders an empty box. A managed-challenge interstitial needs nothing here —\nit is a Cloudflare-served document carrying its own policy. `frame-ancestors 'none'` is\nuntouched: this app framing the widget is the opposite direction from this app being framed.",
          "required": [],
          "title": "turnstile",
          "type": "boolean"
        }
      },
      "required": [],
      "title": "cloudflare"
    },
    "common": {
      "description": "Shared template partials for the TimSchoenle Helm charts",
      "properties": {
        "affinity": {
          "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.Affinity",
          "required": []
        },
        "automountServiceAccountToken": {
          "default": false,
          "description": "Mount the ServiceAccount API token into the pod.\nSet on the pod itself, which is what actually keeps the token out of the container: the\nServiceAccount-level setting is ignored as soon as a pod names a different account.",
          "required": [],
          "title": "automountServiceAccountToken",
          "type": "boolean"
        },
        "commonAnnotations": {
          "additionalProperties": true,
          "description": "Annotations added to every object the chart creates. Values may contain Go templates.",
          "required": [],
          "title": "commonAnnotations",
          "type": "object"
        },
        "commonLabels": {
          "additionalProperties": true,
          "description": "Labels added to every object the chart creates. Values may contain Go templates.",
          "required": [],
          "title": "commonLabels",
          "type": "object"
        },
        "component": {
          "default": "",
          "description": "Value for the `app.kubernetes.io/component` label.",
          "required": [],
          "title": "component",
          "type": "string"
        },
        "config": {
          "additionalProperties": true,
          "description": "Application configuration, expressed as the TOML tree the service documents. Rendered by\n`common.toml` into the ConfigMap the pod mounts, never passed as environment variables: the\nloader every application shares refuses a key supplied by both the environment and a file,\nand a value that lives in a file is one the kubelet can rotate under a running process.",
          "required": [],
          "title": "config",
          "type": "object"
        },
        "configExtraToml": {
          "default": "",
          "description": "Verbatim TOML appended after the rendered `config` tree. The escape hatch for anything\n`common.toml` cannot express, notably arrays of tables.",
          "required": [],
          "title": "configExtraToml",
          "type": "string"
        },
        "configMount": {
          "additionalProperties": true,
          "description": "Where the rendered configuration and the credential files land in the container. Consumed\nby `common.fileConfig.*`, which also passes both directories to the application as\n`\u003cPREFIX\u003e_CONFIG` and `\u003cPREFIX\u003e_SECRETS_DIR`.",
          "properties": {
            "configDir": {
              "default": "",
              "description": "Directory the rendered `config.toml` is mounted at.",
              "required": [],
              "title": "configDir",
              "type": "string"
            },
            "secretsDir": {
              "default": "",
              "description": "Directory the credential files are mounted at, one file per configuration key.",
              "required": [],
              "title": "secretsDir",
              "type": "string"
            }
          },
          "required": [],
          "title": "configMount"
        },
        "dnsConfig": {
          "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.PodDNSConfig",
          "required": []
        },
        "dnsPolicy": {
          "default": "",
          "description": "Pod DNS policy.",
          "required": [],
          "title": "dnsPolicy",
          "type": "string"
        },
        "existingConfigMap": {
          "default": "",
          "description": "Name of an existing ConfigMap holding `config.toml`, replacing the one this chart renders.",
          "required": [],
          "title": "existingConfigMap",
          "type": "string"
        },
        "existingSecret": {
          "default": "",
          "description": "Name of an existing Secret holding the credential files, replacing the one this chart\nrenders. Its keys must be the configuration paths the service reads, with `__` for nesting\nand no dots — that is the file name the loader parses.",
          "required": [],
          "title": "existingSecret",
          "type": "string"
        },
        "extraEnv": {
          "description": "Additional environment variables appended to the application container.",
          "items": {
            "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.EnvVar",
            "required": []
          },
          "required": [],
          "title": "extraEnv",
          "type": "array"
        },
        "extraVolumeMounts": {
          "description": "Additional volume mounts added to the application container.",
          "items": {
            "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.VolumeMount",
            "required": []
          },
          "required": [],
          "title": "extraVolumeMounts",
          "type": "array"
        },
        "extraVolumes": {
          "description": "Additional volumes added to the pod.",
          "items": {
            "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.Volume",
            "required": []
          },
          "required": [],
          "title": "extraVolumes",
          "type": "array"
        },
        "fullnameOverride": {
          "default": "",
          "description": "Override the full generated resource name.",
          "required": [],
          "title": "fullnameOverride",
          "type": "string"
        },
        "gateway": {
          "additionalProperties": true,
          "description": "Gateway API configuration, consumed by `common.gateway.*`. The successor to `ingress`, and\nan independent switch from it: a cluster migrating between an Ingress controller and a Gateway\nimplementation runs both for a while.\n\nThe division of labour is the API's, not this chart's. A `Gateway` — the listeners, the\naddress, the certificates — belongs to the cluster operator; an application owns only the\n`HTTPRoute` that attaches to it. So the default here is route-only, and `create` is for\ninstalls that have no cluster-wide Gateway to attach to.",
          "properties": {
            "addresses": {
              "description": "Addresses requested for the created Gateway, e.g. a fixed `IPAddress`.",
              "items": {
                "required": []
              },
              "required": [],
              "title": "addresses",
              "type": "array"
            },
            "allowedRoutes": {
              "additionalProperties": true,
              "description": "Which routes may attach to the created Gateway's listeners. Defaults to `Same`: a Gateway\nthis chart owns should not be attachable from another namespace unless that is asked for.",
              "required": [],
              "title": "allowedRoutes",
              "type": "object"
            },
            "annotations": {
              "additionalProperties": true,
              "description": "Annotations for the HTTPRoute and the created Gateway. Values may contain Go templates.",
              "required": [],
              "title": "annotations",
              "type": "object"
            },
            "backendRefs": {
              "description": "Backends for rules that name none. Defaults to this chart's own Service. Weights are\nhonoured, so a traffic split needs no custom rule.",
              "items": {
                "required": []
              },
              "required": [],
              "title": "backendRefs",
              "type": "array"
            },
            "create": {
              "default": false,
              "description": "Also create the Gateway itself. Leave off when the cluster already runs one — that is the\nnormal case, and one Gateway per application usually means one load balancer per\napplication. When on, a route that names no parent attaches to it automatically.",
              "required": [],
              "title": "create",
              "type": "boolean"
            },
            "enabled": {
              "default": false,
              "description": "Create the HTTPRoute. Requires the `gateway.networking.k8s.io` CRDs;\n`common.gateway.validate` fails the render loudly rather than silently dropping the route\nwhen they are absent.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "filters": {
              "description": "Filters applied to the default rule: `RequestHeaderModifier`, `ResponseHeaderModifier`,\n`RequestRedirect`, `URLRewrite`, `RequestMirror`, `ExtensionRef`. This is where an\nIngress controller's annotations end up, as typed fields. Ignored when `rules` is set.",
              "items": {
                "required": []
              },
              "required": [],
              "title": "filters",
              "type": "array"
            },
            "gatewayClassName": {
              "default": "",
              "description": "GatewayClass that programs the created Gateway, e.g. `cilium`, `istio`, `envoy-gateway`,\n`nginx`. Required by `create`; a Gateway without one is never reconciled. Ignored otherwise.",
              "required": [],
              "title": "gatewayClassName",
              "type": "string"
            },
            "hostnames": {
              "description": "Hostnames the route serves. Values may contain Go templates.\n\nRequired unless `create` is set. A route with no hostnames matches every name its listener\naccepts: on a Gateway this chart owns that is harmless, and sometimes the point — an install\nreached by address has no DNS name to state. On a shared Gateway it means taking over traffic\nmeant for other applications, so the render refuses it.",
              "items": {
                "required": []
              },
              "required": [],
              "title": "hostnames",
              "type": "array"
            },
            "httpPort": {
              "default": 80,
              "description": "Port for the derived HTTP listener.",
              "required": [],
              "title": "httpPort",
              "type": "integer"
            },
            "httpsPort": {
              "default": 443,
              "description": "Port for the derived HTTPS listener.",
              "required": [],
              "title": "httpsPort",
              "type": "integer"
            },
            "httpsRedirect": {
              "additionalProperties": true,
              "description": "A second route that redirects plaintext traffic to HTTPS. Under Ingress this was a\ncontroller-specific annotation; Gateway API expresses it as a typed `RequestRedirect`\nfilter, which means it has to be a real object.",
              "properties": {
                "enabled": {
                  "default": false,
                  "description": "Create the redirect route.",
                  "required": [],
                  "title": "enabled",
                  "type": "boolean"
                },
                "port": {
                  "default": "null",
                  "description": "Port to redirect to. Left unset, the scheme implies it.",
                  "required": [],
                  "title": "port",
                  "type": [
                    "integer",
                    "null"
                  ]
                },
                "sectionName": {
                  "default": "",
                  "description": "Listener to bind the redirect to. Must be the plaintext one: attached to every listener,\nthe redirect would also apply to the HTTPS listener and loop forever. Defaults to `http`,\nthe name of the listener `create` renders.",
                  "required": [],
                  "title": "sectionName",
                  "type": "string"
                },
                "statusCode": {
                  "default": 301,
                  "description": "Redirect status code. `301` or `302`.",
                  "required": [],
                  "title": "statusCode",
                  "type": "integer"
                }
              },
              "required": [],
              "title": "httpsRedirect"
            },
            "infrastructure": {
              "additionalProperties": true,
              "description": "`infrastructure.labels` / `infrastructure.annotations` for the created Gateway, passed\nthrough to the load balancer the implementation provisions. Where Cilium's LB-IPAM\nannotations go.",
              "required": [],
              "title": "infrastructure",
              "type": "object"
            },
            "listeners": {
              "description": "Listeners for the created Gateway, replacing the derived ones entirely. Reach for this\nwhen a listener needs its own hostname or certificate.",
              "items": {
                "required": []
              },
              "required": [],
              "title": "listeners",
              "type": "array"
            },
            "parentRefs": {
              "description": "Gateways the route attaches to. Each entry takes `name` and optionally `namespace`,\n`sectionName` (a single listener), `port`, `group` and `kind`; the API's defaults are filled\nin. Values may contain Go templates.\n\nA route that names no parent is accepted by the API server and then does nothing — no\nlistener ever programs it — so this is required unless `create` is set.\nExample:\n```yaml\nparentRefs:\n  - name: shared-gateway\n    namespace: gateway-system\n    sectionName: https\n```",
              "items": {
                "required": []
              },
              "required": [],
              "title": "parentRefs",
              "type": "array"
            },
            "path": {
              "default": "/",
              "description": "Path prefix for the default rule. Ignored when `rules` is set.",
              "required": [],
              "title": "path",
              "type": "string"
            },
            "rules": {
              "description": "Routing rules, in full `HTTPRouteRule` form (`matches`, `filters`, `backendRefs`,\n`timeouts`). An entry that omits `backendRefs` inherits this chart's Service, so a rule that\nonly narrows the path does not have to restate where the traffic goes.\n\nLeft empty, the route gets one rule matching `path` as a prefix — the Gateway API equivalent\nof a single-path Ingress, and a complete configuration together with `hostnames`.",
              "items": {
                "required": []
              },
              "required": [],
              "title": "rules",
              "type": "array"
            },
            "timeouts": {
              "additionalProperties": true,
              "description": "Timeouts for the default rule: `request` and `backendRequest`, as Go durations. Ignored\nwhen `rules` is set.",
              "required": [],
              "title": "timeouts",
              "type": "object"
            },
            "tls": {
              "additionalProperties": true,
              "description": "TLS for the created Gateway's HTTPS listener. Ignored without `create`, and irrelevant\nwhen attaching to somebody else's Gateway — the certificate is theirs.",
              "properties": {
                "certificateRefs": {
                  "description": "Secrets holding the certificate. Required by `Terminate`: unlike an Ingress there is no\nconvention by which one is looked up from the hostname. A ref naming another namespace\nadditionally needs a `ReferenceGrant` there, which this chart deliberately does not create\n— a grant is the target namespace owner's to give.",
                  "items": {
                    "required": []
                  },
                  "required": [],
                  "title": "certificateRefs",
                  "type": "array"
                },
                "enabled": {
                  "default": false,
                  "description": "Add an HTTPS listener.",
                  "required": [],
                  "title": "enabled",
                  "type": "boolean"
                },
                "mode": {
                  "default": "Terminate",
                  "description": "TLS mode.",
                  "enum": [
                    "Terminate",
                    "Passthrough"
                  ],
                  "required": [],
                  "title": "mode"
                },
                "options": {
                  "additionalProperties": true,
                  "description": "Implementation-specific TLS options.",
                  "required": [],
                  "title": "options",
                  "type": "object"
                }
              },
              "required": [],
              "title": "tls"
            }
          },
          "required": [],
          "title": "gateway"
        },
        "global": {
          "description": "Global values are values that can be accessed from any chart or subchart by exactly the same name.",
          "required": [],
          "title": "global",
          "type": "object"
        },
        "grafanaDashboard": {
          "additionalProperties": true,
          "description": "Grafana dashboard delivery, consumed by `common.grafana.dashboard.*`. A consuming chart\nexposes this shape wherever it likes — `metrics.dashboard` is the convention — and passes it in\nas the `values` argument. Grafana has no Kubernetes-native dashboard type, so the partials\nrender both mechanisms: a labelled ConfigMap for the sidecar, and, optionally, one\n`GrafanaDashboard` per file for grafana-operator v5.",
          "properties": {
            "enabled": {
              "default": false,
              "description": "Create the ConfigMap holding the dashboard JSON. Required by the operator path too, which\nreferences it rather than duplicating the JSON into the custom resources.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "grafanaOperator": {
              "additionalProperties": true,
              "properties": {
                "allowCrossNamespaceImport": {
                  "default": true,
                  "description": "Let the resources bind to Grafana instances outside the release namespace. With `false`\nthe operator only considers Grafana custom resources in the same namespace.",
                  "required": [],
                  "title": "allowCrossNamespaceImport",
                  "type": "boolean"
                },
                "enabled": {
                  "default": false,
                  "description": "Also create one `GrafanaDashboard` per dashboard file. This is the only delivery path a\nchart can make cross-namespace on its own terms. Requires the `grafana.integreatly.org/v1beta1`\nCRDs; `common.grafana.dashboard.errors` fails the render loudly rather than silently\ndropping the objects when they are absent.",
                  "required": [],
                  "title": "enabled",
                  "type": "boolean"
                },
                "folder": {
                  "default": "",
                  "description": "Folder to file the dashboards under. Empty leaves them at the Grafana root.",
                  "required": [],
                  "title": "folder",
                  "type": "string"
                },
                "instanceSelector": {
                  "additionalProperties": true,
                  "description": "Label selector for the Grafana instances to import into. Must select something: unlike a\nKubernetes label selector, an empty `instanceSelector` matches no instance at all.",
                  "properties": {
                    "matchLabels": {
                      "additionalProperties": false,
                      "properties": {
                        "dashboards": {
                          "default": "grafana",
                          "required": [],
                          "title": "dashboards",
                          "type": "string"
                        }
                      },
                      "required": [],
                      "title": "matchLabels",
                      "type": "object"
                    }
                  },
                  "required": [],
                  "title": "instanceSelector",
                  "type": "object"
                },
                "resyncPeriod": {
                  "default": "5m",
                  "description": "How often the operator re-reconciles the dashboard, undoing edits made in the Grafana UI.\nA Go duration.",
                  "required": [],
                  "title": "resyncPeriod",
                  "type": "string"
                }
              },
              "required": [],
              "title": "grafanaOperator"
            },
            "label": {
              "default": "grafana_dashboard",
              "description": "Label a Grafana sidecar watches for dashboard ConfigMaps. Discovery is a property of the\n*Grafana* release: the sidecar only sees this ConfigMap if `sidecar.dashboards.searchNamespace`\ncovers the namespace it lands in, which a chart cannot influence from its own side.",
              "required": [],
              "title": "label",
              "type": "string"
            },
            "labelValue": {
              "default": "1",
              "description": "Value for that label.",
              "required": [],
              "title": "labelValue",
              "type": "string"
            }
          },
          "required": [],
          "title": "grafanaDashboard"
        },
        "hostAliases": {
          "description": "Host aliases injected into the pod's /etc/hosts.",
          "items": {
            "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.HostAlias",
            "required": []
          },
          "required": [],
          "title": "hostAliases",
          "type": "array"
        },
        "image": {
          "additionalProperties": true,
          "properties": {
            "pullPolicy": {
              "default": "",
              "description": "Image pull policy. Left empty, resolves to `Always` for the `latest` tag and\n`IfNotPresent` for anything pinned.",
              "enum": [
                "",
                "Always",
                "IfNotPresent",
                "Never"
              ],
              "required": [],
              "title": "pullPolicy"
            },
            "registry": {
              "default": "",
              "description": "Registry host. Left empty, the repository is used as-is (Docker Hub).",
              "required": [],
              "title": "registry",
              "type": "string"
            },
            "repository": {
              "default": "",
              "description": "Image repository. Required.",
              "required": [],
              "title": "repository",
              "type": "string"
            },
            "tag": {
              "default": "",
              "description": "Image tag. Defaults to the chart's `appVersion`. May pin a digest inline\n(`v1.2.3\nversion marker.",
              "required": [],
              "title": "tag",
              "type": "string"
            }
          },
          "required": [],
          "title": "image"
        },
        "imagePullSecrets": {
          "description": "Pull secrets for private registries. Accepts `- name: regcred` or the shorthand `- regcred`.",
          "items": {
            "required": []
          },
          "required": [],
          "title": "imagePullSecrets",
          "type": "array"
        },
        "kubeVersionOverride": {
          "default": "",
          "description": "Kubernetes version to target when branching on API availability.\nLets `helm template` render for a specific cluster version without a live connection.\nLeave empty to detect from the cluster.",
          "required": [],
          "title": "kubeVersionOverride",
          "type": "string"
        },
        "livenessProbe": {
          "additionalProperties": true,
          "description": "Liveness probe. See `startupProbe` for the accepted shape.",
          "properties": {
            "enabled": {
              "default": false,
              "description": "Enable the liveness probe.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            }
          },
          "required": [],
          "title": "livenessProbe"
        },
        "nameOverride": {
          "default": "",
          "description": "Override the chart name used in resource names and labels.",
          "required": [],
          "title": "nameOverride",
          "type": "string"
        },
        "namespaceOverride": {
          "default": "",
          "description": "Deploy into a namespace other than the release namespace.",
          "required": [],
          "title": "namespaceOverride",
          "type": "string"
        },
        "networkPolicy": {
          "additionalProperties": true,
          "description": "Network policy configuration.\n\nEvery generated egress rule is scoped by a `to:` selector. An egress rule that lists only\nports is not a restriction — the NetworkPolicy API reads a missing `to` as \"any\ndestination\", so such a policy permits traffic to every in-cluster service and to the\ncloud metadata endpoint.",
          "properties": {
            "cilium": {
              "additionalProperties": true,
              "description": "Cilium-only additions, used when `engine` is `cilium` or `both`. Everything above is\ntranslated into the CiliumNetworkPolicy automatically; these are the rules the portable API\nhas no way to express.\n\nNote that `extraIngress`, `extraEgress` and the per-section `customRules` above are *not*\ncarried over: those are verbatim `networking.k8s.io/v1` rule objects and are not valid CNP.\nThe fields below are their counterparts.",
              "properties": {
                "description": {
                  "default": "",
                  "description": "`spec.description`, which Cilium surfaces in `cilium policy get` and in Hubble flow\nverdicts. The one place to record *why* a rule exists where an operator debugging a drop\nwill actually see it.",
                  "required": [],
                  "title": "description",
                  "type": "string"
                },
                "egress": {
                  "additionalProperties": true,
                  "description": "Cilium-only egress rules.",
                  "properties": {
                    "customRules": {
                      "description": "Additional egress rules in CiliumNetworkPolicy form, appended verbatim.",
                      "items": {
                        "required": []
                      },
                      "required": [],
                      "title": "customRules",
                      "type": "array"
                    },
                    "dnsMatchPatterns": {
                      "description": "What the DNS proxy may resolve, e.g. `- matchPattern: \"*.example.com\"`. Defaults to\neverything, which only permits the lookup — an answer is still only reachable if some\nrule allows the address.",
                      "items": {
                        "required": []
                      },
                      "required": [],
                      "title": "dnsMatchPatterns",
                      "type": "array"
                    },
                    "entityPorts": {
                      "description": "Restrict the `toEntities` rule to specific ports. Empty means all ports.",
                      "items": {
                        "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.networking.v1.NetworkPolicyPort",
                        "required": []
                      },
                      "required": [],
                      "title": "entityPorts",
                      "type": "array"
                    },
                    "fqdnPorts": {
                      "description": "Ports the `toFQDNs` rule allows. Defaults to TCP/443.",
                      "items": {
                        "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.networking.v1.NetworkPolicyPort",
                        "required": []
                      },
                      "required": [],
                      "title": "fqdnPorts",
                      "type": "array"
                    },
                    "httpRules": {
                      "description": "L7 HTTP rules layered onto the `toFQDNs` rule, e.g.\n`- method: GET` / `path: \"/v1/.*\"`. Turns \"may reach this host\" into \"may make these\nrequests to this host\". Costs a proxy hop per connection.",
                      "items": {
                        "required": []
                      },
                      "required": [],
                      "title": "httpRules",
                      "type": "array"
                    },
                    "toEntities": {
                      "description": "Named destination sets, e.g. `world` for everything outside the cluster, or\n`kube-apiserver`. Not a synonym for the `egress.cidr`/`except` translation: `world` does\nnot carve out the cloud metadata endpoint the way those defaults do.",
                      "items": {
                        "required": [],
                        "type": "string"
                      },
                      "required": [],
                      "title": "toEntities",
                      "type": "array"
                    },
                    "toFQDNs": {
                      "description": "Destinations by name rather than by address, e.g. `- matchName: api.example.com` or\n`- matchPattern: \"*.example.com\"`. This is the rule the CIDR-based `egress.https` was\nalways a poor approximation of: \"may reach the internet on 443\" permits every public host\nthat exists, where this permits the ones the application actually talks to.\n\nEnforced against the addresses Cilium's DNS proxy saw returned for the name, so\n`egress.dns.enabled` must stay on — the render fails if it is not.",
                      "items": {
                        "required": []
                      },
                      "required": [],
                      "title": "toFQDNs",
                      "type": "array"
                    }
                  },
                  "required": [],
                  "title": "egress"
                },
                "enableDefaultDeny": {
                  "default": true,
                  "description": "State default-deny explicitly rather than relying on it being implied by the presence of\nrules. This is what makes the intentional default-deny case — a policy with an empty rule\nlist — actually deny, instead of being treated as no policy at all. Requires Cilium 1.16+.",
                  "required": [],
                  "title": "enableDefaultDeny",
                  "type": "boolean"
                },
                "extraEgress": {
                  "description": "Extra egress rules in CiliumNetworkPolicy form, appended regardless of `egress.enabled`.",
                  "items": {
                    "required": []
                  },
                  "required": [],
                  "title": "extraEgress",
                  "type": "array"
                },
                "extraIngress": {
                  "description": "Extra ingress rules in CiliumNetworkPolicy form, appended regardless of\n`ingress.enabled`.",
                  "items": {
                    "required": []
                  },
                  "required": [],
                  "title": "extraIngress",
                  "type": "array"
                },
                "ingress": {
                  "additionalProperties": true,
                  "description": "Cilium-only ingress rules.",
                  "properties": {
                    "customRules": {
                      "description": "Additional ingress rules in CiliumNetworkPolicy form, appended verbatim.",
                      "items": {
                        "required": []
                      },
                      "required": [],
                      "title": "customRules",
                      "type": "array"
                    },
                    "fromEntities": {
                      "description": "Named source sets, e.g. `cluster`, `host`, `remote-node`, `world`, `kube-apiserver`.\nA named entity stays correct when the cluster is renumbered; a CIDR list does not.",
                      "items": {
                        "required": [],
                        "type": "string"
                      },
                      "required": [],
                      "title": "fromEntities",
                      "type": "array"
                    }
                  },
                  "required": [],
                  "title": "ingress"
                }
              },
              "required": [],
              "title": "cilium"
            },
            "egress": {
              "additionalProperties": true,
              "description": "Egress rules.",
              "properties": {
                "cidr": {
                  "default": "0.0.0.0/0",
                  "description": "Destination CIDR for the HTTP/HTTPS rules.",
                  "required": [],
                  "title": "cidr",
                  "type": "string"
                },
                "customRules": {
                  "description": "Additional egress rules, appended verbatim. Each must carry its own `to:` selector.",
                  "items": {
                    "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.networking.v1.NetworkPolicyEgressRule",
                    "required": []
                  },
                  "required": [],
                  "title": "customRules",
                  "type": "array"
                },
                "dns": {
                  "additionalProperties": true,
                  "description": "Allow DNS resolution.",
                  "properties": {
                    "enabled": {
                      "default": true,
                      "description": "Allow egress to the cluster DNS service.",
                      "required": [],
                      "title": "enabled",
                      "type": "boolean"
                    },
                    "namespaceSelector": {
                      "additionalProperties": true,
                      "description": "Namespace selector for the DNS service.",
                      "properties": {
                        "kubernetes.io/metadata.name": {
                          "default": "kube-system",
                          "required": [],
                          "title": "kubernetes.io/metadata.name",
                          "type": "string"
                        }
                      },
                      "required": [],
                      "title": "namespaceSelector",
                      "type": "object"
                    },
                    "podSelector": {
                      "additionalProperties": true,
                      "description": "Pod selector for the DNS service.",
                      "properties": {
                        "k8s-app": {
                          "default": "kube-dns",
                          "required": [],
                          "title": "k8s-app",
                          "type": "string"
                        }
                      },
                      "required": [],
                      "title": "podSelector",
                      "type": "object"
                    }
                  },
                  "required": [],
                  "title": "dns"
                },
                "enabled": {
                  "default": true,
                  "description": "Add egress rules. Disabled means default-deny for outbound traffic.",
                  "required": [],
                  "title": "enabled",
                  "type": "boolean"
                },
                "except": {
                  "description": "CIDRs carved out of `cidr`. Defaults exclude RFC1918 private space and link-local\n169.254.0.0/16, which covers the cloud instance metadata endpoint.",
                  "items": {
                    "anyOf": [
                      {
                        "required": [],
                        "type": "string"
                      },
                      {
                        "required": [],
                        "type": "string"
                      },
                      {
                        "required": [],
                        "type": "string"
                      },
                      {
                        "required": [],
                        "type": "string"
                      }
                    ],
                    "required": []
                  },
                  "required": [],
                  "title": "except",
                  "type": "array"
                },
                "http": {
                  "additionalProperties": true,
                  "description": "Allow outbound HTTP.",
                  "properties": {
                    "enabled": {
                      "default": false,
                      "description": "Allow egress to TCP/80 on the destinations described by `cidr`/`except`.",
                      "required": [],
                      "title": "enabled",
                      "type": "boolean"
                    }
                  },
                  "required": [],
                  "title": "http"
                },
                "https": {
                  "additionalProperties": true,
                  "description": "Allow outbound HTTPS.",
                  "properties": {
                    "enabled": {
                      "default": true,
                      "description": "Allow egress to TCP/443 on the destinations described by `cidr`/`except`.",
                      "required": [],
                      "title": "enabled",
                      "type": "boolean"
                    }
                  },
                  "required": [],
                  "title": "https"
                }
              },
              "required": [],
              "title": "egress"
            },
            "enabled": {
              "default": false,
              "description": "Create the NetworkPolicies. When enabled with no rules configured, the result is a\ndefault-deny policy, which is intentional.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "engine": {
              "default": "kubernetes",
              "description": "Which policy dialect to render. `kubernetes` emits the portable\n`networking.k8s.io/v1` pair; `cilium` emits `CiliumNetworkPolicy`, which can express FQDN\ndestinations, named entities and L7 rules that the portable API cannot; `both` emits both,\nfor the window in which a cluster is migrating between CNIs.\n\nThe engine picks the dialect, not the rules — every value below is translated either way, so\nswitching is a one-line change. `both` is additive rather than stricter: policies selecting\none pod union their allowances, so it is a migration setting and not a hardening one.",
              "enum": [
                "kubernetes",
                "cilium",
                "both"
              ],
              "required": [],
              "title": "engine"
            },
            "extraEgress": {
              "description": "Extra egress rules appended regardless of `egress.enabled`.",
              "items": {
                "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.networking.v1.NetworkPolicyEgressRule",
                "required": []
              },
              "required": [],
              "title": "extraEgress",
              "type": "array"
            },
            "extraIngress": {
              "description": "Extra ingress rules appended regardless of `ingress.enabled`.",
              "items": {
                "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.networking.v1.NetworkPolicyIngressRule",
                "required": []
              },
              "required": [],
              "title": "extraIngress",
              "type": "array"
            },
            "ingress": {
              "additionalProperties": true,
              "description": "Ingress rules.",
              "properties": {
                "controller": {
                  "additionalProperties": true,
                  "description": "Allow traffic from the ingress controller.",
                  "properties": {
                    "enabled": {
                      "default": true,
                      "description": "Allow ingress from the ingress controller.",
                      "required": [],
                      "title": "enabled",
                      "type": "boolean"
                    },
                    "namespace": {
                      "default": "traefik",
                      "description": "Namespace the ingress controller runs in.",
                      "required": [],
                      "title": "namespace",
                      "type": "string"
                    },
                    "ports": {
                      "description": "Restrict the rule to specific ports. Empty means all ports.",
                      "items": {
                        "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.networking.v1.NetworkPolicyPort",
                        "required": []
                      },
                      "required": [],
                      "title": "ports",
                      "type": "array"
                    },
                    "selector": {
                      "additionalProperties": true,
                      "description": "Pod selector matching the ingress controller.",
                      "properties": {
                        "app.kubernetes.io/name": {
                          "default": "traefik",
                          "required": [],
                          "title": "app.kubernetes.io/name",
                          "type": "string"
                        }
                      },
                      "required": [],
                      "title": "selector",
                      "type": "object"
                    }
                  },
                  "required": [],
                  "title": "controller"
                },
                "customRules": {
                  "description": "Additional ingress rules, appended verbatim.",
                  "items": {
                    "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.networking.v1.NetworkPolicyIngressRule",
                    "required": []
                  },
                  "required": [],
                  "title": "customRules",
                  "type": "array"
                },
                "enabled": {
                  "default": true,
                  "description": "Add ingress rules. Disabled means default-deny for inbound traffic.",
                  "required": [],
                  "title": "enabled",
                  "type": "boolean"
                },
                "gateway": {
                  "additionalProperties": true,
                  "description": "Allow traffic from the Gateway API data plane. Only rendered when `gateway.enabled` is\nalso set, so it costs nothing on a chart exposed through an Ingress.\n\nNeeds no configuration in the common case: the Gateway that must be admitted is by\ndefinition the one `gateway.parentRefs` names, so both fields below are derived from it.\nRestating the Gateway's identity here would be a second place to edit on a rename, and a\npolicy that names the wrong Gateway looks correct and blocks all inbound traffic.",
                  "properties": {
                    "enabled": {
                      "default": true,
                      "description": "Allow ingress from the Gateway's data plane.",
                      "required": [],
                      "title": "enabled",
                      "type": "boolean"
                    },
                    "namespace": {
                      "default": "",
                      "description": "Namespace the data plane runs in. Empty derives it from `gateway.parentRefs`.",
                      "required": [],
                      "title": "namespace",
                      "type": "string"
                    },
                    "ports": {
                      "description": "Restrict the rule to specific ports. Empty means all ports.",
                      "items": {
                        "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.networking.v1.NetworkPolicyPort",
                        "required": []
                      },
                      "required": [],
                      "title": "ports",
                      "type": "array"
                    },
                    "selector": {
                      "additionalProperties": true,
                      "description": "Pod selector matching the data plane. Empty derives\n`gateway.networking.k8s.io/gateway-name: \u003cparentRef\u003e`, the label Cilium, Envoy Gateway,\nIstio and NGINX Gateway Fabric all put on the pods they provision for a Gateway.",
                      "required": [],
                      "title": "selector",
                      "type": "object"
                    }
                  },
                  "required": [],
                  "title": "gateway"
                },
                "monitoring": {
                  "additionalProperties": true,
                  "description": "Allow scraping from a monitoring namespace.",
                  "properties": {
                    "enabled": {
                      "default": true,
                      "description": "Allow ingress from the monitoring namespace.",
                      "required": [],
                      "title": "enabled",
                      "type": "boolean"
                    },
                    "namespace": {
                      "default": "monitoring",
                      "description": "Monitoring namespace, matched on `kubernetes.io/metadata.name`.",
                      "required": [],
                      "title": "namespace",
                      "type": "string"
                    },
                    "namespaceSelector": {
                      "additionalProperties": true,
                      "description": "Override the namespace selector entirely.",
                      "required": [],
                      "title": "namespaceSelector",
                      "type": "object"
                    },
                    "ports": {
                      "description": "Restrict the rule to specific ports. Empty means all ports.",
                      "items": {
                        "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.networking.v1.NetworkPolicyPort",
                        "required": []
                      },
                      "required": [],
                      "title": "ports",
                      "type": "array"
                    }
                  },
                  "required": [],
                  "title": "monitoring"
                }
              },
              "required": [],
              "title": "ingress"
            }
          },
          "required": [],
          "title": "networkPolicy"
        },
        "nodeSelector": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Node selector for pod assignment.",
          "required": [],
          "title": "nodeSelector",
          "type": "object"
        },
        "partOf": {
          "default": "",
          "description": "Value for the `app.kubernetes.io/part-of` label.",
          "required": [],
          "title": "partOf",
          "type": "string"
        },
        "podAnnotations": {
          "additionalProperties": true,
          "description": "Annotations added to the pod template.",
          "required": [],
          "title": "podAnnotations",
          "type": "object"
        },
        "podAntiAffinity": {
          "default": "",
          "description": "Shorthand for spreading replicas across nodes. `soft` prefers, `hard` requires.",
          "enum": [
            "",
            "soft",
            "hard"
          ],
          "required": [],
          "title": "podAntiAffinity"
        },
        "podLabels": {
          "additionalProperties": true,
          "description": "Labels added to the pod template.",
          "required": [],
          "title": "podLabels",
          "type": "object"
        },
        "podSecurityContext": {
          "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.PodSecurityContext",
          "required": []
        },
        "podSecurityContextPreset": {
          "default": "restricted",
          "description": "Baseline for the pod security context. `restricted` applies the Pod Security Standards\nrestricted profile (`runAsNonRoot`, `seccompProfile: RuntimeDefault`,\n`fsGroupChangePolicy: OnRootMismatch`); `none` applies nothing. Identity fields\n(`runAsUser`, `runAsGroup`, `fsGroup`) are always left to the chart, since they must match\nthe UID baked into the image.",
          "enum": [
            "restricted",
            "none"
          ],
          "required": [],
          "title": "podSecurityContextPreset"
        },
        "priorityClassName": {
          "default": "",
          "description": "PriorityClass for the pod.",
          "required": [],
          "title": "priorityClassName",
          "type": "string"
        },
        "prometheusRule": {
          "additionalProperties": true,
          "description": "Prometheus recording and alerting rules, consumed by `common.prometheus.rules.*`. Unlike the\ndashboards there is only one carrier — `PrometheusRule` is already the operator's own CRD — and\nno per-object cross-namespace grant: which namespaces a Prometheus loads rules from is decided\nby `ruleNamespaceSelector` and `ruleSelector` on the Prometheus custom resource.",
          "properties": {
            "enabled": {
              "default": false,
              "description": "Create a PrometheusRule from the chart's rule files. Requires the `monitoring.coreos.com/v1`\nCRDs; `common.prometheus.rules.errors` fails the render loudly rather than silently dropping\nthe object when they are absent.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "labels": {
              "additionalProperties": true,
              "description": "Extra labels, templated. This is the only half of rule discovery a chart controls: a\ncluster whose Prometheus selects `release: kube-prometheus-stack` needs that label here, or\nthe rules are created and never loaded.",
              "required": [],
              "title": "labels",
              "type": "object"
            },
            "scope": {
              "additionalProperties": true,
              "description": "Confine the rules to a subset of the cluster's series. A `PrometheusRule` is not scoped to\nthe namespace it lives in, so `up{job=\"api\"} == 0` matches somebody else's `api` job in\nanother namespace and two installs of one chart alert on each other. Rather than parse PromQL\nin a Go template, the rule files mark every selector with an always-true placeholder matcher\nand the library swaps it for a real one — see the note in `_prometheus.tpl`. Consuming charts\nnormally derive `matcher` rather than exposing it raw.",
              "properties": {
                "matcher": {
                  "default": "",
                  "description": "What to swap it for, e.g. `namespace=\"prod\"`. Empty disables the substitution; the\nplaceholder is left in place, which is correct, because it is already a no-op matcher.",
                  "required": [],
                  "title": "matcher",
                  "type": "string"
                },
                "placeholder": {
                  "default": "",
                  "description": "The literal placeholder written into the rule files, e.g. `myapp_scope=~\".*\"`. Empty\ndisables the substitution and the files install exactly as vendored.",
                  "required": [],
                  "title": "placeholder",
                  "type": "string"
                }
              },
              "required": [],
              "title": "scope"
            }
          },
          "required": [],
          "title": "prometheusRule"
        },
        "readinessProbe": {
          "additionalProperties": true,
          "description": "Readiness probe. See `startupProbe` for the accepted shape.",
          "properties": {
            "enabled": {
              "default": false,
              "description": "Enable the readiness probe.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            }
          },
          "required": [],
          "title": "readinessProbe"
        },
        "resources": {
          "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.ResourceRequirements",
          "required": []
        },
        "resourcesPreset": {
          "default": "",
          "description": "Named resource sizing. Ignored when `resources` is set.\nPresets set a memory limit and CPU/memory requests but no CPU limit: a CPU limit does not\nprotect the node the way a memory limit does, it only throttles this workload.",
          "enum": [
            "",
            "nano",
            "micro",
            "small",
            "medium",
            "large"
          ],
          "required": [],
          "title": "resourcesPreset"
        },
        "revisionHistoryLimit": {
          "default": 3,
          "description": "Number of old ReplicaSets to retain for rollback.",
          "required": [],
          "title": "revisionHistoryLimit",
          "type": "integer"
        },
        "securityContext": {
          "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.SecurityContext",
          "required": []
        },
        "securityContextPreset": {
          "default": "restricted",
          "description": "Baseline for the container security context. `restricted` drops all capabilities and\nforbids privilege escalation, a writable root filesystem and running as root.",
          "enum": [
            "restricted",
            "none"
          ],
          "required": [],
          "title": "securityContextPreset"
        },
        "serviceAccount": {
          "additionalProperties": true,
          "properties": {
            "annotations": {
              "additionalProperties": true,
              "description": "Annotations for the ServiceAccount.",
              "required": [],
              "title": "annotations",
              "type": "object"
            },
            "automountToken": {
              "default": false,
              "description": "Mount the API token into pods that default to this ServiceAccount.",
              "required": [],
              "title": "automountToken",
              "type": "boolean"
            },
            "create": {
              "default": true,
              "description": "Create a dedicated ServiceAccount.",
              "required": [],
              "title": "create",
              "type": "boolean"
            },
            "name": {
              "default": "",
              "description": "Name of the ServiceAccount. Generated when empty.",
              "required": [],
              "title": "name",
              "type": "string"
            }
          },
          "required": [],
          "title": "serviceAccount"
        },
        "startupProbe": {
          "additionalProperties": true,
          "description": "Startup probe. Set `enabled` and exactly one handler (`httpGet`, `tcpSocket`, `exec`\nor `grpc`); unset timing fields fall back to the Kubernetes defaults.",
          "properties": {
            "enabled": {
              "default": false,
              "description": "Enable the startup probe.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            }
          },
          "required": [],
          "title": "startupProbe"
        },
        "strategy": {
          "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.apps.v1.DeploymentStrategy",
          "required": []
        },
        "terminationGracePeriodSeconds": {
          "default": 30,
          "description": "Grace period for pod shutdown.",
          "required": [],
          "title": "terminationGracePeriodSeconds",
          "type": "integer"
        },
        "tolerations": {
          "description": "Tolerations for pod assignment.",
          "items": {
            "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.Toleration",
            "required": []
          },
          "required": [],
          "title": "tolerations",
          "type": "array"
        },
        "topologySpreadConstraints": {
          "description": "Topology spread constraints.",
          "items": {
            "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.TopologySpreadConstraint",
            "required": []
          },
          "required": [],
          "title": "topologySpreadConstraints",
          "type": "array"
        }
      },
      "required": [],
      "title": "common",
      "type": "object"
    },
    "commonAnnotations": {
      "additionalProperties": true,
      "description": "Annotations added to every object this chart creates.",
      "required": [],
      "title": "commonAnnotations",
      "type": "object"
    },
    "commonLabels": {
      "additionalProperties": true,
      "description": "Labels added to every object this chart creates.",
      "required": [],
      "title": "commonLabels",
      "type": "object"
    },
    "config": {
      "additionalProperties": true,
      "description": "Global TankoVault configuration, expressed exactly as the TOML tree documented in\n`docs/CONFIGURATION.md` (`database.max_connections`, `security.cors.allowed_origins`, ...).\nIt is rendered to a ConfigMap and mounted as a file, never passed as environment variables,\nso **changing a value here reloads the running services in place instead of restarting\nthem**. Two carve-outs from upstream: `telemetry.*` and `metrics.*` are installed\nprocess-globally and still need a restart to take effect.",
      "required": [],
      "title": "config",
      "type": "object"
    },
    "configExtraToml": {
      "default": "",
      "description": "Verbatim TOML appended after the rendered `config` tree. The escape hatch for anything\nthis chart's TOML renderer cannot express, notably arrays of tables.",
      "required": [],
      "title": "configExtraToml",
      "type": "string"
    },
    "configReload": {
      "additionalProperties": true,
      "properties": {
        "configDir": {
          "default": "/etc/tankovault/config",
          "description": "Directory the configuration fragments are mounted at, passed as `TANKOVAULT_CONFIG`.",
          "required": [],
          "title": "configDir",
          "type": "string"
        },
        "rolloutOnChange": {
          "default": false,
          "description": "Add `checksum/config` pod annotations so a configuration change rolls the Deployments.\nOff by default, and deliberately so: every TankoVault service watches the directories its\nconfiguration came from and rebuilds its runtime when the kubelet updates the mounted\nConfigMap or Secret, which is strictly better than a rollout. Turn this on only if you\nwant config changes to behave like an ordinary image bump.",
          "required": [],
          "title": "rolloutOnChange",
          "type": "boolean"
        },
        "secretsDir": {
          "default": "/etc/tankovault/secrets",
          "description": "Directory the credential files are mounted at, passed as `TANKOVAULT_SECRETS_DIR`.",
          "required": [],
          "title": "secretsDir",
          "type": "string"
        }
      },
      "required": [],
      "title": "configReload"
    },
    "defaults": {
      "additionalProperties": true,
      "description": "Settings shared by every TankoVault workload. Anything here can be overridden per service\nunder `services.\u003cname\u003e`, which is merged over this block.",
      "properties": {
        "affinity": {
          "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.Affinity",
          "required": []
        },
        "automountServiceAccountToken": {
          "default": false,
          "description": "Whether to automount the service account token. Nothing in TankoVault talks to the\nKubernetes API.",
          "required": [],
          "title": "automountServiceAccountToken",
          "type": "boolean"
        },
        "extraEnv": {
          "description": "Extra environment variables. Use sparingly: this chart delivers configuration as files\nprecisely so it can be reloaded, and a `TANKOVAULT_*` key supplied both here and as a\nmounted secret file **fails the boot** rather than resolving by precedence.",
          "items": {
            "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.EnvVar",
            "required": []
          },
          "required": [],
          "title": "extraEnv",
          "type": "array"
        },
        "extraVolumeMounts": {
          "description": "Extra container volume mounts.",
          "items": {
            "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.VolumeMount",
            "required": []
          },
          "required": [],
          "title": "extraVolumeMounts",
          "type": "array"
        },
        "extraVolumes": {
          "description": "Extra pod volumes.",
          "items": {
            "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.Volume",
            "required": []
          },
          "required": [],
          "title": "extraVolumes",
          "type": "array"
        },
        "livenessProbe": {
          "additionalProperties": true,
          "description": "Liveness probe. Uses `/health`, never `/ready`: `/ready` reports on Postgres and NATS,\nso wiring liveness to it would turn a database blip into a fleet-wide restart storm.",
          "properties": {
            "enabled": {
              "default": true,
              "description": "Enable the liveness probe.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "failureThreshold": {
              "default": 5,
              "description": "Consecutive failures before the container is restarted.",
              "required": [],
              "title": "failureThreshold",
              "type": "integer"
            },
            "httpGet": {
              "additionalProperties": true,
              "description": "HTTP handler for the probe.",
              "properties": {
                "path": {
                  "default": "/health",
                  "description": "Liveness path.",
                  "required": [],
                  "title": "path",
                  "type": "string"
                },
                "port": {
                  "default": "http",
                  "description": "Named container port to probe.",
                  "required": [],
                  "title": "port",
                  "type": "string"
                }
              },
              "required": [],
              "title": "httpGet"
            },
            "periodSeconds": {
              "default": 30,
              "description": "Probe interval.",
              "required": [],
              "title": "periodSeconds",
              "type": "integer"
            },
            "timeoutSeconds": {
              "default": 5,
              "description": "Probe timeout.",
              "required": [],
              "title": "timeoutSeconds",
              "type": "integer"
            }
          },
          "required": [],
          "title": "livenessProbe"
        },
        "nodeSelector": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Node selector for pod assignment.",
          "required": [],
          "title": "nodeSelector",
          "type": "object"
        },
        "podAnnotations": {
          "additionalProperties": true,
          "description": "Extra pod annotations.",
          "required": [],
          "title": "podAnnotations",
          "type": "object"
        },
        "podAntiAffinity": {
          "default": "soft",
          "description": "Pod anti-affinity across nodes. `soft` prefers spreading replicas, `hard` requires it.",
          "enum": [
            "",
            "soft",
            "hard"
          ],
          "required": [],
          "title": "podAntiAffinity"
        },
        "podLabels": {
          "additionalProperties": true,
          "description": "Extra pod labels.",
          "required": [],
          "title": "podLabels",
          "type": "object"
        },
        "podSecurityContext": {
          "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.PodSecurityContext",
          "required": []
        },
        "podSecurityContextPreset": {
          "default": "restricted",
          "description": "Pod security context baseline.",
          "enum": [
            "restricted",
            "none"
          ],
          "required": [],
          "title": "podSecurityContextPreset"
        },
        "priorityClassName": {
          "default": "",
          "description": "Priority class for the pods.",
          "required": [],
          "title": "priorityClassName",
          "type": "string"
        },
        "readinessProbe": {
          "additionalProperties": true,
          "description": "Readiness probe. Uses `/ready`, which probes every registered dependency concurrently\nand returns per-dependency detail.",
          "properties": {
            "enabled": {
              "default": true,
              "description": "Enable the readiness probe.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "failureThreshold": {
              "default": 3,
              "description": "Consecutive failures before the pod leaves the Service.",
              "required": [],
              "title": "failureThreshold",
              "type": "integer"
            },
            "httpGet": {
              "additionalProperties": true,
              "description": "HTTP handler for the probe.",
              "properties": {
                "path": {
                  "default": "/ready",
                  "description": "Readiness path.",
                  "required": [],
                  "title": "path",
                  "type": "string"
                },
                "port": {
                  "default": "http",
                  "description": "Named container port to probe.",
                  "required": [],
                  "title": "port",
                  "type": "string"
                }
              },
              "required": [],
              "title": "httpGet"
            },
            "periodSeconds": {
              "default": 10,
              "description": "Probe interval.",
              "required": [],
              "title": "periodSeconds",
              "type": "integer"
            },
            "timeoutSeconds": {
              "default": 3,
              "description": "Probe timeout. Upstream bounds each dependency check at 2s, so 3s leaves headroom\nwithout letting a wedged probe hang.",
              "required": [],
              "title": "timeoutSeconds",
              "type": "integer"
            }
          },
          "required": [],
          "title": "readinessProbe"
        },
        "resources": {
          "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.ResourceRequirements",
          "required": []
        },
        "resourcesPreset": {
          "default": "medium",
          "description": "Default resource t-shirt size, overridden per service below.",
          "enum": [
            "",
            "nano",
            "micro",
            "small",
            "medium",
            "large"
          ],
          "required": [],
          "title": "resourcesPreset"
        },
        "revisionHistoryLimit": {
          "default": 3,
          "description": "Number of old ReplicaSets to retain.",
          "required": [],
          "title": "revisionHistoryLimit",
          "type": "integer"
        },
        "securityContext": {
          "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.SecurityContext",
          "required": []
        },
        "securityContextPreset": {
          "default": "restricted",
          "description": "Container security context baseline. Safe for every service: eight of the nine images\nare bare `scratch` layers holding only the binary, the musl loader and a CA bundle, so\nthere is no writable path to depend on.",
          "enum": [
            "restricted",
            "none"
          ],
          "required": [],
          "title": "securityContextPreset"
        },
        "startupProbe": {
          "additionalProperties": true,
          "description": "Startup probe. Hits `/health`, which deliberately checks nothing external.",
          "properties": {
            "enabled": {
              "default": true,
              "description": "Enable the startup probe.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "failureThreshold": {
              "default": 30,
              "description": "Consecutive failures before the container is considered failed.",
              "required": [],
              "title": "failureThreshold",
              "type": "integer"
            },
            "httpGet": {
              "additionalProperties": true,
              "description": "HTTP handler for the probe.",
              "properties": {
                "path": {
                  "default": "/health",
                  "description": "Liveness path.",
                  "required": [],
                  "title": "path",
                  "type": "string"
                },
                "port": {
                  "default": "http",
                  "description": "Named container port to probe.",
                  "required": [],
                  "title": "port",
                  "type": "string"
                }
              },
              "required": [],
              "title": "httpGet"
            },
            "periodSeconds": {
              "default": 5,
              "description": "Probe interval.",
              "required": [],
              "title": "periodSeconds",
              "type": "integer"
            },
            "timeoutSeconds": {
              "default": 3,
              "description": "Probe timeout.",
              "required": [],
              "title": "timeoutSeconds",
              "type": "integer"
            }
          },
          "required": [],
          "title": "startupProbe"
        },
        "strategy": {
          "additionalProperties": true,
          "description": "Deployment update strategy.",
          "required": [],
          "title": "strategy"
        },
        "terminationGracePeriodSeconds": {
          "default": 30,
          "description": "Grace period for shutdown. Every service cancels its work loop on SIGTERM and stops\nbetween units of work.",
          "required": [],
          "title": "terminationGracePeriodSeconds",
          "type": "integer"
        },
        "tolerations": {
          "description": "Tolerations for pod assignment.",
          "items": {
            "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.Toleration",
            "required": []
          },
          "required": [],
          "title": "tolerations",
          "type": "array"
        },
        "topologySpreadConstraints": {
          "description": "Topology spread constraints.",
          "items": {
            "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.TopologySpreadConstraint",
            "required": []
          },
          "required": [],
          "title": "topologySpreadConstraints",
          "type": "array"
        }
      },
      "required": [],
      "title": "defaults"
    },
    "email": {
      "additionalProperties": true,
      "description": "Transactional email relay, shared by `api` (password reset, verification) and `notifier`.\nThe channel is enabled only when a relay and `from` are both present; a partial\nconfiguration falls back to a no-op mailer that logs and drops.",
      "properties": {
        "baseUrl": {
          "default": "",
          "description": "Public base URL used to build links in outgoing mail. Left empty it is derived from the\ningress. Leaving it pointed at localhost produces unusable links in real emails.",
          "required": [],
          "title": "baseUrl",
          "type": "string"
        },
        "from": {
          "default": "",
          "description": "`From` header, e.g. `TankoVault \u003cno-reply",
          "required": [],
          "title": "from",
          "type": "string"
        },
        "host": {
          "default": "",
          "description": "SMTP host. Empty disables outbound email entirely.",
          "required": [],
          "title": "host",
          "type": "string"
        },
        "password": {
          "default": "",
          "description": "SMTP password.",
          "required": [],
          "title": "password",
          "type": "string"
        },
        "port": {
          "default": 587,
          "description": "SMTP port.",
          "maximum": 65535,
          "minimum": 1,
          "required": [],
          "title": "port",
          "type": "integer"
        },
        "security": {
          "default": "starttls",
          "description": "Transport security for the relay connection.",
          "enum": [
            "starttls",
            "tls",
            "none"
          ],
          "required": [],
          "title": "security"
        },
        "username": {
          "default": "",
          "description": "SMTP username.",
          "required": [],
          "title": "username",
          "type": "string"
        }
      },
      "required": [],
      "title": "email"
    },
    "existingSecret": {
      "default": "",
      "description": "Name of an existing Secret holding the credentials, instead of having the chart render one\nfrom the values above. When set it takes precedence and no credential value is written into\nthe Helm release. Its keys are the configuration paths with `__` for nesting and no dots —\n`auth__jwt_secret`, `auth__password_pepper`, `auth__mfa_encryption_key`,\n`anilist__client_id`, `anilist__client_secret`, `anilist__token_encryption_key`,\n`email__username`, `email__password`, `channels__discord_webhook_url`,\n`channels__webhook_url`, `database__url`, `redis__url`. Only the keys a given service reads\nare projected into it. Under `internal.identity=token` add `internal__tokens__api` and\n`internal__tokens__worker`: those two are keyed by caller rather than by configuration path,\nbecause one token is read as `internal.caller.token` by the caller and as\n`internal.peers.\u003ccaller\u003e.token` by each of its callees, and the projection maps the key onto\nwhichever path the reading pod needs. Under `mtls` no internal credential is needed at all.",
      "required": [],
      "title": "existingSecret",
      "type": "string"
    },
    "externalDatabase": {
      "additionalProperties": true,
      "description": "Point TankoVault at a PostgreSQL you already run. Used whenever `postgresql.enabled` is\nfalse, and the only supported production topology.",
      "properties": {
        "existingSecret": {
          "default": "",
          "description": "Name of an existing Secret holding the connection URL.",
          "required": [],
          "title": "existingSecret",
          "type": "string"
        },
        "url": {
          "default": "",
          "description": "Connection URL, e.g. `postgres://user:password\nchart's Secret; prefer `existingSecret` so it never enters the Helm release.",
          "required": [],
          "title": "url",
          "type": "string"
        },
        "urlKey": {
          "default": "database__url",
          "description": "Key within that Secret.",
          "required": [],
          "title": "urlKey",
          "type": "string"
        }
      },
      "required": [],
      "title": "externalDatabase"
    },
    "externalNats": {
      "additionalProperties": true,
      "description": "Point TankoVault at a NATS you already run. Used whenever `nats.enabled` is false.",
      "properties": {
        "perServiceSecret": {
          "default": "",
          "description": "Name of an existing Secret holding one NATS URL per service, keyed by service slug (`api`,\n`control-plane`, `worker`, `notifier`). Each pod projects its own key as `nats__url`, which\nreplaces the derived URL entirely. This is how per-service NATS **accounts** are delivered —\nthe thing that stops `notifier` publishing scan tasks — since the credential belongs in the\nURL and so cannot live in the ConfigMap. Note that the accounts themselves are NATS server\nconfiguration: the services present what you give them here, and cannot assert what a broker\ndoes not enforce.",
          "required": [],
          "title": "perServiceSecret",
          "type": "string"
        },
        "url": {
          "default": "",
          "description": "Connection URL, e.g. `nats://host:4222`. Required by control-plane, worker and notifier;\noptional on the API, where its absence only degrades the live notification stream. Under\n`internal.identity=mtls` the services present their client certificate to the broker and\nrequire TLS on that connection, so it must be a TLS-enabled NATS.",
          "required": [],
          "title": "url",
          "type": "string"
        }
      },
      "required": [],
      "title": "externalNats"
    },
    "externalRedis": {
      "additionalProperties": true,
      "description": "Point TankoVault at a Redis-compatible server you already run. Used whenever\n`valkey.enabled` is false.",
      "properties": {
        "existingSecret": {
          "default": "",
          "description": "Name of an existing Secret holding the connection URL.",
          "required": [],
          "title": "existingSecret",
          "type": "string"
        },
        "url": {
          "default": "",
          "description": "Connection URL, e.g. `redis://host:6379`. Empty leaves Redis unconfigured, which is\nsupported: the rate limiter and scheduler both degrade rather than fail.",
          "required": [],
          "title": "url",
          "type": "string"
        },
        "urlKey": {
          "default": "redis__url",
          "description": "Key within that Secret.",
          "required": [],
          "title": "urlKey",
          "type": "string"
        }
      },
      "required": [],
      "title": "externalRedis"
    },
    "externalTrawl": {
      "additionalProperties": true,
      "description": "Point the challenge solver at a TRAWL you already run.",
      "properties": {
        "url": {
          "default": "",
          "description": "Endpoint URL, e.g. `http://trawl:8191`.",
          "required": [],
          "title": "url",
          "type": "string"
        }
      },
      "required": [],
      "title": "externalTrawl"
    },
    "fullnameOverride": {
      "default": "",
      "description": "Override the fully qualified release name.",
      "required": [],
      "title": "fullnameOverride",
      "type": "string"
    },
    "gateway": {
      "additionalProperties": true,
      "description": "Gateway API exposure, the successor to `ingress` and an independent switch from it: a\ncluster migrating from an Ingress controller to a Gateway implementation runs both for a while.\n\nWhat it publishes is the same thing `ingress` does and for the same reason — the frontend\nserves the SPA and reverse-proxies `/v1/*` to the API, so one origin covers the whole\napplication. Only the mechanism differs: the `Gateway` (its listeners, its address, its\ncertificates) belongs to the cluster operator, and this chart owns the `HTTPRoute` that\nattaches to it.",
      "properties": {
        "addresses": {
          "description": "Addresses requested for the created Gateway, e.g. a fixed `IPAddress`.",
          "items": {
            "required": []
          },
          "required": [],
          "title": "addresses",
          "type": "array"
        },
        "allowedRoutes": {
          "additionalProperties": true,
          "description": "Which routes may attach to the created Gateway's listeners. Defaults to `Same`.",
          "required": [],
          "title": "allowedRoutes",
          "type": "object"
        },
        "annotations": {
          "additionalProperties": true,
          "description": "Annotations for the HTTPRoute.",
          "required": [],
          "title": "annotations",
          "type": "object"
        },
        "api": {
          "additionalProperties": true,
          "description": "A second route publishing the API on its own hostname, for split-origin clients.\nEnabling it means the browser origin no longer matches the API origin, so CORS,\n`auth.webauthn_origin` and cookie `SameSite` all have to be set consistently or logins\nbreak in ways that only appear at runtime.",
          "properties": {
            "annotations": {
              "additionalProperties": true,
              "description": "Annotations for the API HTTPRoute.",
              "required": [],
              "title": "annotations",
              "type": "object"
            },
            "enabled": {
              "default": false,
              "description": "Create the API HTTPRoute.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "filters": {
              "description": "Filters applied to the API route.",
              "items": {
                "required": []
              },
              "required": [],
              "title": "filters",
              "type": "array"
            },
            "host": {
              "default": "",
              "description": "Hostname the API is served on.",
              "required": [],
              "title": "host",
              "type": "string"
            },
            "timeouts": {
              "additionalProperties": true,
              "description": "Timeouts for the API route.",
              "required": [],
              "title": "timeouts",
              "type": "object"
            }
          },
          "required": [],
          "title": "api"
        },
        "create": {
          "default": false,
          "description": "Also create the Gateway itself, for an install with no cluster-wide one to attach to.\nA route that names no parent attaches to it automatically.",
          "required": [],
          "title": "create",
          "type": "boolean"
        },
        "enabled": {
          "default": false,
          "description": "Create an HTTPRoute for the frontend. Requires the `gateway.networking.k8s.io` CRDs; the\nrender fails loudly rather than silently dropping the route when they are absent.",
          "required": [],
          "title": "enabled",
          "type": "boolean"
        },
        "filters": {
          "description": "Filters applied to the frontend route: `RequestHeaderModifier`,\n`ResponseHeaderModifier`, `RequestRedirect`, `URLRewrite`, `RequestMirror`, `ExtensionRef`.\nThis is where an Ingress controller's annotations end up, as typed fields.",
          "items": {
            "required": []
          },
          "required": [],
          "title": "filters",
          "type": "array"
        },
        "gatewayClassName": {
          "default": "",
          "description": "GatewayClass that programs the created Gateway, e.g. `cilium`, `istio`, `envoy-gateway`.\nRequired by `create`; a Gateway without one is never reconciled.",
          "required": [],
          "title": "gatewayClassName",
          "type": "string"
        },
        "host": {
          "default": "",
          "description": "Hostname the application is served on.",
          "required": [],
          "title": "host",
          "type": "string"
        },
        "httpPort": {
          "default": 80,
          "description": "Port for the derived HTTP listener.",
          "required": [],
          "title": "httpPort",
          "type": "integer"
        },
        "httpsPort": {
          "default": 443,
          "description": "Port for the derived HTTPS listener.",
          "required": [],
          "title": "httpsPort",
          "type": "integer"
        },
        "httpsRedirect": {
          "additionalProperties": true,
          "description": "A second route that redirects plaintext traffic to HTTPS. Under Ingress this was a\ncontroller-specific annotation; Gateway API expresses it as a typed `RequestRedirect` filter,\nwhich means it has to be a real object.",
          "properties": {
            "enabled": {
              "default": false,
              "description": "Create the redirect route.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "sectionName": {
              "default": "",
              "description": "Listener to bind the redirect to. Must be the plaintext one: attached to every listener\nthe redirect would also apply to the HTTPS listener and loop forever. Defaults to `http`,\nthe name of the listener `create` renders.",
              "required": [],
              "title": "sectionName",
              "type": "string"
            },
            "statusCode": {
              "default": 301,
              "description": "Redirect status code. `301` or `302`.",
              "required": [],
              "title": "statusCode",
              "type": "integer"
            }
          },
          "required": [],
          "title": "httpsRedirect"
        },
        "infrastructure": {
          "additionalProperties": true,
          "description": "`infrastructure.labels` / `infrastructure.annotations` for the created Gateway, passed\nthrough to the load balancer the implementation provisions. Where Cilium's LB-IPAM\nannotations go.",
          "required": [],
          "title": "infrastructure",
          "type": "object"
        },
        "listeners": {
          "description": "Listeners for the created Gateway, replacing the derived `http`/`https` pair entirely.",
          "items": {
            "required": []
          },
          "required": [],
          "title": "listeners",
          "type": "array"
        },
        "parentRefs": {
          "description": "Gateways the routes attach to. Each entry takes `name` and optionally `namespace`,\n`sectionName`, `port`, `group` and `kind`. Required unless `create` is set: a route with no\nparent is accepted by the API server and then never programmed by any listener.",
          "items": {
            "required": []
          },
          "required": [],
          "title": "parentRefs",
          "type": "array"
        },
        "path": {
          "default": "/",
          "description": "Path prefix the route matches.",
          "required": [],
          "title": "path",
          "type": "string"
        },
        "timeouts": {
          "additionalProperties": true,
          "description": "Timeouts for the frontend route: `request` and `backendRequest`, as Go durations.\n\nLeave `request` unset or generous if the SPA holds the `/v1/events` SSE stream open through\nthis route — a request timeout cuts a long-lived stream at the Gateway, and the symptom is a\nUI that quietly stops updating rather than an error anybody sees.",
          "required": [],
          "title": "timeouts",
          "type": "object"
        },
        "tls": {
          "additionalProperties": true,
          "description": "TLS for the route's hostname.",
          "properties": {
            "certificateRefs": {
              "description": "Secrets holding the certificate for the created Gateway's listener. Required by\n`Terminate`: unlike an Ingress there is no convention by which one is looked up from the\nhostname. Ignored without `create` — otherwise the certificate is the Gateway owner's.",
              "items": {
                "required": []
              },
              "required": [],
              "title": "certificateRefs",
              "type": "array"
            },
            "enabled": {
              "default": false,
              "description": "The hostname is served over HTTPS. Two effects: it adds the HTTPS listener when\n`create` is set, and it is what the derived external URL takes its scheme from — so set it\neven when the Gateway is somebody else's and it is their listener terminating TLS.\nNote `auth.cookie_secure` defaults to true, so sessions are lost over plain HTTP on any\nhost other than `localhost`.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "mode": {
              "default": "Terminate",
              "description": "TLS mode for the created Gateway's listener.",
              "enum": [
                "Terminate",
                "Passthrough"
              ],
              "required": [],
              "title": "mode"
            },
            "options": {
              "additionalProperties": true,
              "description": "Implementation-specific TLS options for the created Gateway's listener.",
              "required": [],
              "title": "options",
              "type": "object"
            }
          },
          "required": [],
          "title": "tls"
        },
        "url": {
          "default": "",
          "description": "Override the derived external URL used for `anilist.redirect_uri`, `email.base_url` and\n`auth.webauthn_origin`. Takes precedence over `ingress.url`. Set this when TLS terminates on\na proxy in front of the Gateway.",
          "required": [],
          "title": "url",
          "type": "string"
        }
      },
      "required": [],
      "title": "gateway"
    },
    "global": {
      "description": "Global values are values that can be accessed from any chart or subchart by exactly the same name.",
      "required": [],
      "title": "global",
      "type": "object"
    },
    "image": {
      "additionalProperties": true,
      "properties": {
        "pullPolicy": {
          "default": "",
          "description": "Image pull policy. Empty derives it from the tag: pinned digests and versions get\n`IfNotPresent`, `latest` gets `Always`.",
          "enum": [
            "",
            "Always",
            "IfNotPresent",
            "Never"
          ],
          "required": [],
          "title": "pullPolicy"
        },
        "registry": {
          "default": "",
          "description": "Registry host shared by every TankoVault image. Empty means Docker Hub.",
          "required": [],
          "title": "registry",
          "type": "string"
        }
      },
      "required": [],
      "title": "image"
    },
    "imagePullSecrets": {
      "description": "Image pull secrets for private registries.",
      "items": {
        "required": [],
        "type": "string"
      },
      "required": [],
      "title": "imagePullSecrets",
      "type": "array"
    },
    "ingress": {
      "additionalProperties": true,
      "description": "Ingress. Only the frontend is exposed by default; it serves the SPA and proxies `/v1/*`\nto the API, so one origin covers the whole application and same-origin cookies, SSE and\npasskey origins all line up without extra configuration.",
      "properties": {
        "annotations": {
          "additionalProperties": true,
          "description": "Extra Ingress annotations.",
          "required": [],
          "title": "annotations",
          "type": "object"
        },
        "api": {
          "additionalProperties": true,
          "description": "A second Ingress publishing the API on its own hostname, for split-origin clients.\nEnabling it means the browser origin no longer matches the API origin, so CORS,\n`auth.webauthn_origin` and cookie `SameSite` all have to be set consistently or logins\nbreak in ways that only appear at runtime.",
          "properties": {
            "annotations": {
              "additionalProperties": true,
              "description": "Extra Ingress annotations.",
              "required": [],
              "title": "annotations",
              "type": "object"
            },
            "enabled": {
              "default": false,
              "description": "Create the API Ingress.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "host": {
              "default": "",
              "description": "Hostname the API is served on.",
              "required": [],
              "title": "host",
              "type": "string"
            }
          },
          "required": [],
          "title": "api"
        },
        "className": {
          "default": "",
          "description": "IngressClass name.",
          "required": [],
          "title": "className",
          "type": "string"
        },
        "enabled": {
          "default": false,
          "description": "Create an Ingress for the frontend.",
          "required": [],
          "title": "enabled",
          "type": "boolean"
        },
        "host": {
          "default": "",
          "description": "Hostname the application is served on.",
          "required": [],
          "title": "host",
          "type": "string"
        },
        "path": {
          "default": "/",
          "description": "Path prefix.",
          "required": [],
          "title": "path",
          "type": "string"
        },
        "pathType": {
          "default": "Prefix",
          "description": "Path type.",
          "enum": [
            "Prefix",
            "Exact",
            "ImplementationSpecific"
          ],
          "required": [],
          "title": "pathType"
        },
        "tls": {
          "additionalProperties": true,
          "properties": {
            "enabled": {
              "default": false,
              "description": "Terminate TLS. Note `auth.cookie_secure` defaults to true, so sessions are lost over\nplain HTTP on any host other than `localhost`.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "secretName": {
              "default": "",
              "description": "Name of the TLS Secret. Defaults to `\u003cfullname\u003e-tls` when empty.",
              "required": [],
              "title": "secretName",
              "type": "string"
            }
          },
          "required": [],
          "title": "tls"
        },
        "url": {
          "default": "",
          "description": "Override the derived external URL used for `anilist.redirect_uri`, `email.base_url` and\n`auth.webauthn_origin`. Set this when TLS terminates on a proxy in front of the ingress.",
          "required": [],
          "title": "url",
          "type": "string"
        }
      },
      "required": [],
      "title": "ingress"
    },
    "internal": {
      "additionalProperties": true,
      "description": "How the services identify each other on their privileged internal routes. Authorisation is\nthe same either way — every callee carries a compiled-in table of which callers may reach which\nroutes — so this chooses only how a caller proves who it is, and the two modes are otherwise\nindistinguishable.",
      "properties": {
        "identity": {
          "default": "mtls",
          "description": "Identification mode. `mtls` identifies a caller by the DNS SAN on its verified client\ncertificate; it is the default, it needs no credential in the release at all, and the\ncertificates can come either from cert-manager or from Secrets you already have. `token`\ngives each caller its own secret, presented in `X-Internal-Token`, and is the mode for\nclusters with no PKI to hand.",
          "enum": [
            "mtls",
            "token"
          ],
          "required": [],
          "title": "identity"
        },
        "tls": {
          "additionalProperties": true,
          "description": "Certificate material for `identity: mtls`. Ignored entirely under `token`.",
          "properties": {
            "ca": {
              "additionalProperties": true,
              "description": "Where the CA every service verifies its peers against comes from.",
              "properties": {
                "bundle": {
                  "additionalProperties": true,
                  "description": "The trust-manager `Bundle` that produces the ConfigMap, rendered as part of this\nrelease. Only meaningful with `source: configMap`.",
                  "properties": {
                    "create": {
                      "default": false,
                      "description": "Render the `Bundle`. Off by default because a Bundle is cluster-scoped: two releases\ncreating the same name would be two objects overwriting each other. Leave it off and\npoint `ca.name` at the ConfigMap your existing cluster-wide Bundle already writes here.",
                      "required": [],
                      "title": "create",
                      "type": "boolean"
                    },
                    "namespaceSelector": {
                      "additionalProperties": true,
                      "description": "Label selector for the namespaces the Bundle writes into. Empty confines it to this\nrelease's namespace; a Bundle with no selector at all would write a ConfigMap into\nevery namespace in the cluster.",
                      "required": [],
                      "title": "namespaceSelector",
                      "type": "object"
                    },
                    "sources": {
                      "description": "trust-manager `spec.sources`, verbatim. Required when `create` is set, because\nnothing in an issuer reference says where the CA certificate lives — a `ClusterIssuer`\nnames a Secret in cert-manager's namespace, not in this one. For a CA issuer that is\nusually `[{secret: {name: internal-ca, key: ca.crt}}]`.",
                      "items": {
                        "additionalProperties": true,
                        "required": [],
                        "type": "object"
                      },
                      "required": [],
                      "title": "sources",
                      "type": "array"
                    }
                  },
                  "required": [],
                  "title": "bundle"
                },
                "key": {
                  "default": "ca.crt",
                  "description": "Key holding the PEM bundle, in whichever object `source` selects.",
                  "required": [],
                  "title": "key",
                  "type": "string"
                },
                "name": {
                  "default": "tankovault-ca",
                  "description": "Name of the ConfigMap or Secret holding the bundle. Required unless `source` is\n`certificateSecret`, which reads the keypair Secret the service already mounts. With\n`bundle.create` this is also the name of the trust-manager `Bundle`.",
                  "required": [],
                  "title": "name",
                  "type": "string"
                },
                "source": {
                  "default": "certificateSecret",
                  "description": "CA source. `certificateSecret` reads it from the `ca.crt` key of the same Secret that\nholds the keypair — the default, because it needs no second object, no second volume and\nno trust-manager: cert-manager writes that key for a CA issuer, and the\n`kubernetes.io/tls` + `ca.crt` layout is what most PKIs produce anyway. `configMap` reads\na ConfigMap, which is what a trust-manager `Bundle` writes and the more robust setup:\none bundle for the cluster, rotatable independently of any single certificate, and the\nonly option when your issuer does not populate `ca.crt`. `secret` reads a Secret, for a\nPKI that ships its root that way.",
                  "enum": [
                    "certificateSecret",
                    "configMap",
                    "secret"
                  ],
                  "required": [],
                  "title": "source"
                }
              },
              "required": [],
              "title": "ca"
            },
            "caDir": {
              "default": "/etc/tankovault/tls-ca",
              "description": "Directory the CA bundle is mounted at, when it comes from an object of its own. Kept\napart from `certDir` because the two are then written by different controllers — a\ncert-manager Secret and a trust-manager ConfigMap — and a volume can only carry one.",
              "required": [],
              "title": "caDir",
              "type": "string"
            },
            "certDir": {
              "default": "/etc/tankovault/tls",
              "description": "Directory the keypair is mounted at, as `tls.crt` and `tls.key`. With\n`ca.source: certificateSecret` the bundle is read from this directory too.",
              "required": [],
              "title": "certDir",
              "type": "string"
            },
            "certManager": {
              "additionalProperties": true,
              "description": "Settings for `source: certManager`. Ignored under `existingSecrets`.",
              "properties": {
                "duration": {
                  "default": "2160h",
                  "description": "Requested certificate lifetime.",
                  "required": [],
                  "title": "duration",
                  "type": "string"
                },
                "issuerRef": {
                  "additionalProperties": true,
                  "description": "The cert-manager issuer that signs each service's certificate. It has to be one whose\nCA every service can also verify against, so a public ACME issuer is the wrong shape here\n— a CA `Issuer` or `ClusterIssuer`, or a Vault/Venafi issuer, is what this expects.",
                  "properties": {
                    "group": {
                      "default": "cert-manager.io",
                      "description": "API group of the issuer resource.",
                      "required": [],
                      "title": "group",
                      "type": "string"
                    },
                    "kind": {
                      "default": "ClusterIssuer",
                      "description": "Issuer kind.",
                      "enum": [
                        "Issuer",
                        "ClusterIssuer"
                      ],
                      "required": [],
                      "title": "kind"
                    },
                    "name": {
                      "default": "",
                      "description": "Issuer name. Required under `source: certManager`; there is no default to fall back\non.",
                      "required": [],
                      "title": "name",
                      "type": "string"
                    }
                  },
                  "required": [],
                  "title": "issuerRef"
                },
                "privateKey": {
                  "additionalProperties": true,
                  "description": "Private key policy, passed to the Certificate verbatim. `rotationPolicy: Always`\nissues a fresh key on every renewal rather than re-signing the old one.",
                  "properties": {
                    "algorithm": {
                      "default": "ECDSA",
                      "description": "Key algorithm.",
                      "enum": [
                        "RSA",
                        "ECDSA",
                        "Ed25519"
                      ],
                      "required": [],
                      "title": "algorithm"
                    },
                    "rotationPolicy": {
                      "default": "Always",
                      "description": "Whether a renewal also rotates the private key.",
                      "enum": [
                        "Never",
                        "Always"
                      ],
                      "required": [],
                      "title": "rotationPolicy"
                    },
                    "size": {
                      "default": 256,
                      "description": "Key size, in the units the algorithm uses.",
                      "required": [],
                      "title": "size",
                      "type": "integer"
                    }
                  },
                  "required": [],
                  "title": "privateKey"
                },
                "renewBefore": {
                  "default": "360h",
                  "description": "How far ahead of expiry cert-manager renews. Renewal needs no rollout: the services\nre-read the files every 30 seconds and swap the credential without dropping connections,\nwhich is why no `checksum/` annotation names these Secrets.",
                  "required": [],
                  "title": "renewBefore",
                  "type": "string"
                }
              },
              "required": [],
              "title": "certManager"
            },
            "existingSecrets": {
              "additionalProperties": {
                "type": "string"
              },
              "description": "Secrets holding each service's keypair, for `source: existingSecrets`, keyed by service.\nEvery enabled service but the `frontend` needs one — including `notifier`, which presents\nits certificate to NATS — and the chart refuses to render while any is missing rather than\nproducing a pod that waits forever on a volume.\n\nEach must be a `kubernetes.io/tls`-shaped Secret (`tls.crt`, `tls.key`), and **no two\nservices may share one**: a service is identified by the SANs on the certificate it\npresents, so a shared Secret would let either of them speak as the other. Each certificate\nhas to be valid for the Service name its peers dial it at — `\u003crelease\u003e-tankovault-\u003cslug\u003e` —\nand, if it also carries the name under `sans`, for that.",
              "properties": {
                "api": {
                  "default": "",
                  "required": [],
                  "title": "api",
                  "type": "string"
                },
                "challengeSolver": {
                  "default": "",
                  "required": [],
                  "title": "challengeSolver",
                  "type": "string"
                },
                "controlPlane": {
                  "default": "",
                  "required": [],
                  "title": "controlPlane",
                  "type": "string"
                },
                "notifier": {
                  "default": "",
                  "required": [],
                  "title": "notifier",
                  "type": "string"
                },
                "render": {
                  "default": "",
                  "required": [],
                  "title": "render",
                  "type": "string"
                },
                "sync": {
                  "default": "",
                  "required": [],
                  "title": "sync",
                  "type": "string"
                },
                "worker": {
                  "default": "",
                  "required": [],
                  "title": "worker",
                  "type": "string"
                }
              },
              "required": [
                "api",
                "controlPlane",
                "worker",
                "notifier",
                "sync",
                "challengeSolver",
                "render"
              ],
              "title": "existingSecrets",
              "type": "object"
            },
            "probePort": {
              "default": 9091,
              "description": "Container port carrying `/health` and `/ready` in **plaintext**, beside the mutually\nauthenticated listener. A kubelet probe presents no client certificate, so on the mTLS port\nits plain `GET /health` is answered with a TLS alert and every replica is killed by its own\nstartup probe — the chart therefore opens this port and points the three probes at it on\nevery service that serves the mTLS listener (`control-plane`, `worker`, `sync`,\n`challenge-solver`, `render`). `api` and `notifier` hold a certificate but serve no mTLS\nlistener, and their probes are untouched.\n\nIt carries the two probes and nothing else: the scrape stays on `metrics.port`, so a\ndeployment that deliberately merged the scrape onto an authenticated port keeps it there.\nThe port is opened on the container only and not published on the Service — the kubelet\nreaches the pod directly.\n\nThere is no off switch: upstream drops the listener only for a literal `null`, which this\nchart's TOML renderer cannot express, and turning it off would break the probes the chart\nwrites. Ignored entirely under `identity: token`, where the probes answer on the request\nport like everywhere else.",
              "maximum": 65535,
              "minimum": 1,
              "required": [],
              "title": "probePort",
              "type": "integer"
            },
            "sans": {
              "additionalProperties": {
                "type": "string"
              },
              "description": "The DNS name each service is identified by, keyed by service, overriding the derived\n`\u003crelease\u003e-tankovault-\u003cslug\u003e.\u003cnamespace\u003e.svc`. This is the name written into every peer's\nconfiguration and, under `source: certManager`, requested on the certificate too — the two\nhalves always agree because one value sets both.\n\nFor an external PKI that issues names of its own (`api.tankovault.internal`), name them\nhere. The verifier checks the configured name against *every* DNS SAN on the presented\ncertificate rather than only the first, so a certificate carrying both the in-cluster name\nand yours satisfies both this and the hostname check on the dialed address.",
              "required": [],
              "title": "sans",
              "type": "object"
            },
            "source": {
              "default": "certManager",
              "description": "Where each service's keypair comes from. `certManager` has this chart issue one\n`Certificate` per service and is the recommended path by some distance: cert-manager renews\nand rotates on its own, so nothing here expires unattended, and the SANs cannot drift from\nwhat the peer configuration expects because the same template writes both.\n`existingSecrets` takes Secrets you produced some other way — an external PKI, a Vault\nAgent sidecar, SPIRE, or a certificate issued by hand — and creates no `Certificate` at all.\nIn that mode renewal is yours to arrange, and a certificate that expires stops every\nprivileged call in the release.",
              "enum": [
                "certManager",
                "existingSecrets"
              ],
              "required": [],
              "title": "source"
            }
          },
          "required": [],
          "title": "tls"
        },
        "token": {
          "default": "",
          "description": "**Removed in chart 4.0.0.** The single shared token that used to open every privileged\nroute on every service. Upstream refuses it at boot in every profile with no dual-accept\nwindow, so this chart fails the render with the migration path rather than letting a release\ncarry it into a fleet-wide crash loop. Use `internal.identity` with `internal.tls` or\n`internal.tokens`.",
          "required": [],
          "title": "token",
          "type": "string"
        },
        "tokens": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Per-caller tokens for `identity: token`, keyed by caller name. Exactly two services make\nprivileged calls — `api` and `worker` — so exactly two tokens exist, and each callee receives\nonly the tokens of the callers it accepts: holding `worker`'s opens the routes `worker` may\ncall and nothing else. Left empty the chart generates each one and remembers it across\nupgrades, which is the recommended setting; set one explicitly (minimum 32 characters, e.g.\n`openssl rand -hex 32`) only if it has to be known outside the release. Ignored under `mtls`,\nand the chart refuses to render rather than leave a value here meaning nothing.",
          "properties": {
            "api": {
              "default": "",
              "required": [],
              "title": "api",
              "type": "string"
            },
            "worker": {
              "default": "",
              "required": [],
              "title": "worker",
              "type": "string"
            }
          },
          "required": [
            "api",
            "worker"
          ],
          "title": "tokens",
          "type": "object"
        }
      },
      "required": [],
      "title": "internal"
    },
    "kubeVersionOverride": {
      "default": "",
      "description": "Override the detected Kubernetes version used for API version selection.",
      "required": [],
      "title": "kubeVersionOverride",
      "type": "string"
    },
    "legal": {
      "additionalProperties": true,
      "description": "Operator-published legal documents, served unauthenticated by the API at `/v1/legal` and\n`/v1/legal/{slug}`; the frontend's footer builds its Legal column from that index. Upstream\nships none of these on purpose — every deployment is a different operator under different law,\nand an imprint is a statutory requirement in some jurisdictions and meaningless in others. With\nno documents the index is empty and the footer publishes no Legal column at all, rather than\nlinks that 404.",
      "properties": {
        "dir": {
          "default": "/etc/tankovault/legal",
          "description": "Directory relative `sources` paths resolve against, and where the chart mounts any\ndocument supplied through `content`. An absolute `sources` path ignores it.",
          "required": [],
          "title": "dir",
          "type": "string"
        },
        "documents": {
          "additionalProperties": true,
          "description": "Published documents, keyed by the URL slug they are served under. Each document names its\nbody exactly once, in one of three ways, and the chart refuses a document that names it\ntwice or not at all:\n\n- `content`: a locale-keyed map of the text itself. The chart writes it into a ConfigMap as\n  `\u003cslug\u003e.\u003clocale\u003e.md` and mounts it into the API pod. Edits are picked up without a restart —\n  the service re-reads behind an mtime check — so this is the option to reach for by default.\n- `sources`: a locale-keyed map of file paths you have arranged to mount yourself, through\n  `extraVolumes`/`extraVolumeMounts`. Use this when the texts belong to a Secret, a PVC or\n  another chart.\n- `url`: an absolute `http(s)` link to a document hosted elsewhere. Mounts nothing.\n\n`title` is a locale-keyed display name, and `updated` a free-form date shown alongside it.\nBoth are optional and independent of how the body is supplied.\n\n\u003cdetails\u003e\u003csummary\u003eExample\u003c/summary\u003e\n\n```yaml\nlegal:\n  documents:\n    terms:\n      updated: \"2026-08-04\"\n      title:\n        en: Terms of Service\n        de: Nutzungsbedingungen\n      content:\n        en: |\n          # Terms of Service\n          ...\n        de: |\n          # Nutzungsbedingungen\n          ...\n    imprint:\n      title:\n        de: Impressum\n      url: https://example.org/impressum\n```\n\n\u003c/details\u003e",
          "required": [],
          "title": "documents",
          "type": "object"
        }
      },
      "required": [],
      "title": "legal"
    },
    "metrics": {
      "additionalProperties": true,
      "description": "Prometheus integration. Every service serves a scrape on an isolated port, outside the\nrequest-facing listener and outside its own HTTP metrics middleware.",
      "properties": {
        "dashboard": {
          "additionalProperties": true,
          "properties": {
            "enabled": {
              "default": false,
              "description": "Create a ConfigMap holding the upstream Grafana overview dashboard.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "grafanaOperator": {
              "additionalProperties": true,
              "properties": {
                "allowCrossNamespaceImport": {
                  "default": true,
                  "description": "Let the CRs bind to Grafana instances outside this namespace. This is the entire point\nof the operator path: with `false` the operator only considers Grafana CRs in this\nrelease's own namespace, and a Grafana living elsewhere never imports the dashboard.",
                  "required": [],
                  "title": "allowCrossNamespaceImport",
                  "type": "boolean"
                },
                "enabled": {
                  "default": false,
                  "description": "Also create one `GrafanaDashboard` per dashboard, for clusters running grafana-operator\nv5. Unlike the sidecar ConfigMap, whose discovery is a property of the *Grafana* release,\na `GrafanaDashboard` declares its own reach, so a Grafana in another namespace can import\nit without cluster-wide sidecar configuration. The CRs reference the ConfigMap through\n`configMapRef` rather than inlining the JSON, so `dashboard.enabled` must stay true.\nRequires the `grafana.integreatly.org/v1beta1` CRDs; rendering fails loudly without them.",
                  "required": [],
                  "title": "enabled",
                  "type": "boolean"
                },
                "folder": {
                  "default": "",
                  "description": "Folder to file the dashboards under. Empty leaves them at the Grafana root.",
                  "required": [],
                  "title": "folder",
                  "type": "string"
                },
                "instanceSelector": {
                  "additionalProperties": true,
                  "description": "Label selector for the Grafana instances to import into. Must select something: an\nempty selector matches no instance, which the chart refuses rather than rendering a CR\nthat reconciles into nothing.",
                  "properties": {
                    "matchLabels": {
                      "additionalProperties": false,
                      "properties": {
                        "dashboards": {
                          "default": "grafana",
                          "required": [],
                          "title": "dashboards",
                          "type": "string"
                        }
                      },
                      "required": [
                        "dashboards"
                      ],
                      "title": "matchLabels",
                      "type": "object"
                    }
                  },
                  "required": [
                    "matchLabels"
                  ],
                  "title": "instanceSelector",
                  "type": "object"
                },
                "resyncPeriod": {
                  "default": "5m",
                  "description": "How often the operator re-reconciles the dashboard, undoing edits made in the Grafana\nUI. A Go duration.",
                  "required": [],
                  "title": "resyncPeriod",
                  "type": "string"
                }
              },
              "required": [],
              "title": "grafanaOperator"
            },
            "label": {
              "default": "grafana_dashboard",
              "description": "Label a Grafana sidecar watches for dashboard ConfigMaps. The sidecar only picks the\nConfigMap up if Grafana is configured to look in this namespace — its own by default.\nEither set `sidecar.dashboards.searchNamespace` to `ALL` on the Grafana release, or use\n`grafanaOperator` below, which carries the cross-namespace grant on the dashboard itself.",
              "required": [],
              "title": "label",
              "type": "string"
            },
            "labelValue": {
              "default": "1",
              "description": "Value for that label.",
              "required": [],
              "title": "labelValue",
              "type": "string"
            }
          },
          "required": [],
          "title": "dashboard"
        },
        "enabled": {
          "default": true,
          "description": "Expose the metrics port on each Service.",
          "required": [],
          "title": "enabled",
          "type": "boolean"
        },
        "natsExporter": {
          "additionalProperties": true,
          "properties": {
            "enabled": {
              "default": false,
              "description": "Deploy `prometheus-nats-exporter` next to NATS. NATS speaks its own monitoring protocol\nand no Prometheus exposition, so without this the scan pipeline's queue depth — tasks\nwaiting per provider lane, tasks being redelivered, whether the notifier backlog is\ndraining — is invisible. Nothing the services emit substitutes: they can report tasks handed\nout, not tasks still sitting in the broker. The rules and dashboard panels that need it are\nsimply empty while it is off, never falsely green.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "image": {
              "additionalProperties": true,
              "properties": {
                "repository": {
                  "default": "natsio/prometheus-nats-exporter",
                  "description": "Image repository.",
                  "required": [],
                  "title": "repository",
                  "type": "string"
                },
                "tag": {
                  "default": "0.20.1@sha256:4fbf6dacb84780a45a1c3af9b1080c69451a288d20902deae671b80717bb8f61",
                  "description": "Image tag, pinned by digest.",
                  "required": [],
                  "title": "tag",
                  "type": "string"
                }
              },
              "required": [],
              "title": "image"
            },
            "resourcesPreset": {
              "default": "nano",
              "description": "Resource t-shirt size. The exporter polls two JSON endpoints and re-serves them; it does\nno work proportional to traffic.",
              "enum": [
                "",
                "nano",
                "micro",
                "small",
                "medium",
                "large"
              ],
              "required": [],
              "title": "resourcesPreset"
            },
            "url": {
              "default": "",
              "description": "NATS monitoring endpoint to scrape. Defaults to the bundled NATS' `:8222`. This is not\nthe client URL: `externalNats.url` points at 4222 and speaks the NATS protocol, while the\nexporter reads HTTP on the monitoring listener, so an external NATS needs this set\nexplicitly — e.g. `http://nats.example.com:8222`.",
              "required": [],
              "title": "url",
              "type": "string"
            }
          },
          "required": [],
          "title": "natsExporter"
        },
        "port": {
          "default": 9090,
          "description": "Port the Prometheus exposition is served on.",
          "maximum": 65535,
          "minimum": 1,
          "required": [],
          "title": "port",
          "type": "integer"
        },
        "prometheusRule": {
          "additionalProperties": true,
          "properties": {
            "enabled": {
              "default": false,
              "description": "Create a PrometheusRule from the recording and alerting rules under `rules/`.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "labels": {
              "additionalProperties": true,
              "description": "Extra labels for the PrometheusRule.",
              "required": [],
              "title": "labels",
              "type": "object"
            },
            "scope": {
              "default": "namespace",
              "description": "Confine the rules to this release's namespace. A `PrometheusRule` is not scoped to the\nnamespace it lives in, so an unscoped `up{job=\"api\"} == 0` matches an `api` job anywhere\nPrometheus can see — a second TankoVault release, or anyone else's service that happens to\nproduce the same `job` label, and the two then alert on each other. `namespace` rewrites\nevery rule expression to match only series from this namespace; `none` installs them as\nwritten, which is correct when a Prometheus already sets `enforcedNamespaceLabel` and would\ndo the same rewrite itself, and wrong otherwise. Two releases of this chart in *one*\nnamespace cannot be told apart by either mechanism.",
              "enum": [
                "namespace",
                "none"
              ],
              "required": [],
              "title": "scope"
            }
          },
          "required": [],
          "title": "prometheusRule"
        },
        "serviceMonitor": {
          "additionalProperties": true,
          "properties": {
            "enabled": {
              "default": false,
              "description": "Create one ServiceMonitor per service. One per service, not one for the release:\nthe `job` label is the sole identifier of which service emitted a metric, and collapsing\nthem would silently break every per-service rule.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "interval": {
              "default": "15s",
              "description": "Scrape interval.",
              "required": [],
              "title": "interval",
              "type": "string"
            },
            "labels": {
              "additionalProperties": true,
              "description": "Extra labels, e.g. the `release` label a Prometheus Operator selector requires.",
              "required": [],
              "title": "labels",
              "type": "object"
            },
            "scrapeTimeout": {
              "default": "10s",
              "description": "Scrape timeout.",
              "required": [],
              "title": "scrapeTimeout",
              "type": "string"
            }
          },
          "required": [],
          "title": "serviceMonitor"
        }
      },
      "required": [],
      "title": "metrics"
    },
    "nameOverride": {
      "default": "",
      "description": "Override the chart name used in resource names.",
      "required": [],
      "title": "nameOverride",
      "type": "string"
    },
    "namespaceOverride": {
      "default": "",
      "description": "Override the namespace objects are created in.",
      "required": [],
      "title": "namespaceOverride",
      "type": "string"
    },
    "nats": {
      "additionalProperties": true,
      "description": "Bundled NATS JetStream, the task queue and event bus the control plane, worker and\nnotifier are built on: a single instance with a file store on a PVC. Embedded rather than\ntaken from the NATS authors' chart because that chart ships no `values.schema.json`, so\n`helm schema` derives a closed one from its own values.yaml and makes the `container.merge`\nand `podTemplate.merge` patches unsettable — and those patches are the only way to give its\npods the security context and resource requests this repository's policy scan requires.\nEvaluation-tier like the other bundled datastores: no clustering, so no replication and no\nfailover. Use `externalNats` for anything real.",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Deploy the bundled NATS.",
          "required": [],
          "title": "enabled",
          "type": "boolean"
        },
        "image": {
          "additionalProperties": true,
          "properties": {
            "repository": {
              "default": "nats",
              "description": "Image repository.",
              "required": [],
              "title": "repository",
              "type": "string"
            },
            "tag": {
              "default": "2-alpine@sha256:f2123f533c2b0cada0a5c5ec434fb2b8cfe1cf220215ef9d7517e1372917ad66",
              "description": "Image tag, pinned by digest.",
              "required": [],
              "title": "tag",
              "type": "string"
            }
          },
          "required": [],
          "title": "image"
        },
        "persistence": {
          "additionalProperties": true,
          "properties": {
            "enabled": {
              "default": true,
              "description": "Persist the JetStream file store. Without it every queued scan task and every\nundelivered event is lost when the pod is rescheduled.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "existingClaim": {
              "default": "",
              "description": "Use an existing claim instead of creating one.",
              "required": [],
              "title": "existingClaim",
              "type": "string"
            },
            "size": {
              "default": "10Gi",
              "description": "Requested volume size.",
              "required": [],
              "title": "size",
              "type": "string"
            },
            "storageClassName": {
              "default": "",
              "description": "Storage class. Empty uses the cluster default.",
              "required": [],
              "title": "storageClassName",
              "type": "string"
            }
          },
          "required": [],
          "title": "persistence"
        },
        "resourcesPreset": {
          "default": "medium",
          "description": "Resource t-shirt size.",
          "enum": [
            "",
            "nano",
            "micro",
            "small",
            "medium",
            "large"
          ],
          "required": [],
          "title": "resourcesPreset"
        }
      },
      "required": [],
      "title": "nats"
    },
    "networkPolicy": {
      "additionalProperties": true,
      "description": "NetworkPolicies. Default-deny per service, then exactly the peers each one needs. Written\nper service rather than through the `common` builder, which cannot express pod-to-pod rules\nbetween nine workloads.",
      "properties": {
        "cilium": {
          "additionalProperties": true,
          "description": "Cilium-only additions, used when `engine` is `cilium` or `both`. The whole topology above\nis rendered into the CiliumNetworkPolicies automatically; these are the rules the portable API\nhas no way to express.\n\nNote that `extraIngress` and `extraEgress` above are *not* carried over: those are verbatim\n`networking.k8s.io/v1` rule objects and are not valid CNP. The fields below are their\ncounterparts.",
          "properties": {
            "description": {
              "default": "",
              "description": "`spec.description`, which Cilium surfaces in `cilium policy get` and in Hubble flow\nverdicts. The one place to record why a rule exists where an operator debugging a drop\nwill actually see it.",
              "required": [],
              "title": "description",
              "type": "string"
            },
            "egress": {
              "additionalProperties": true,
              "description": "Cilium-only egress rules.",
              "properties": {
                "dnsMatchPatterns": {
                  "description": "What the DNS proxy may resolve, e.g. `- matchPattern: \"*.example.com\"`. Defaults to\neverything, which only permits the lookup — an answer is still only reachable if some\nrule allows the address.",
                  "items": {
                    "required": []
                  },
                  "required": [],
                  "title": "dnsMatchPatterns",
                  "type": "array"
                },
                "entityPorts": {
                  "description": "Restrict the `toEntities` rule to specific ports. Empty means all ports.",
                  "items": {
                    "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.networking.v1.NetworkPolicyPort",
                    "required": []
                  },
                  "required": [],
                  "title": "entityPorts",
                  "type": "array"
                },
                "fqdnPorts": {
                  "description": "Ports the `toFQDNs` rule allows. Defaults to TCP/443. SMTP submission needs 587 here\nif `notifier` sends mail directly.",
                  "items": {
                    "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.networking.v1.NetworkPolicyPort",
                    "required": []
                  },
                  "required": [],
                  "title": "fqdnPorts",
                  "type": "array"
                },
                "httpRules": {
                  "description": "L7 HTTP rules layered onto the `toFQDNs` rule, e.g. `- method: GET` / `path: \"/v1/.*\"`.\nCosts a proxy hop per connection.",
                  "items": {
                    "required": []
                  },
                  "required": [],
                  "title": "httpRules",
                  "type": "array"
                },
                "toEntities": {
                  "description": "Named destination sets added to every policy, e.g. `kube-apiserver`.",
                  "items": {
                    "required": [],
                    "type": "string"
                  },
                  "required": [],
                  "title": "toEntities",
                  "type": "array"
                },
                "toFQDNs": {
                  "description": "The hosts the internet-facing tiers may reach, by name: `- matchName: api.anilist.co`\nor `- matchPattern: \"*.example.com\"`. Set, this *replaces* the `internetCidrs` rule for\nevery service that carries it — `worker`, `sync`, `notifier`, `render` and the bundled\nTRAWL. That is the point: `0.0.0.0/0` on 443 permits every public host that exists, and\na scraping tier is precisely the one you do not want holding that.\n\nEmitting both would leave the broad rule in place and make this one decorative, so it is\na substitution rather than an addition.",
                  "items": {
                    "required": []
                  },
                  "required": [],
                  "title": "toFQDNs",
                  "type": "array"
                }
              },
              "required": [],
              "title": "egress"
            },
            "extraEgress": {
              "description": "Extra egress rules in CiliumNetworkPolicy form, appended to every service's policy.",
              "items": {
                "required": []
              },
              "required": [],
              "title": "extraEgress",
              "type": "array"
            },
            "extraIngress": {
              "description": "Extra ingress rules in CiliumNetworkPolicy form, appended to every service's policy.",
              "items": {
                "required": []
              },
              "required": [],
              "title": "extraIngress",
              "type": "array"
            },
            "ingress": {
              "additionalProperties": true,
              "description": "Cilium-only ingress rules, added to every service's policy.",
              "properties": {
                "fromEntities": {
                  "description": "Named source sets, e.g. `cluster`, `host`, `remote-node`, `kube-apiserver`.",
                  "items": {
                    "required": [],
                    "type": "string"
                  },
                  "required": [],
                  "title": "fromEntities",
                  "type": "array"
                }
              },
              "required": [],
              "title": "ingress"
            }
          },
          "required": [],
          "title": "cilium"
        },
        "enabled": {
          "default": false,
          "description": "Create NetworkPolicies.",
          "required": [],
          "title": "enabled",
          "type": "boolean"
        },
        "engine": {
          "default": "kubernetes",
          "description": "Which policy dialect to render. `kubernetes` emits the portable `networking.k8s.io/v1`\nobjects; `cilium` emits `CiliumNetworkPolicy`, which can express FQDN destinations, named\nentities and L7 rules that the portable API cannot; `both` emits both, for the window in\nwhich a cluster is migrating between CNIs.\n\nBoth dialects are rendered from the same derived topology, so they cannot describe different\ngraphs — the engine picks how the rules are written, never what they are.",
          "enum": [
            "kubernetes",
            "cilium",
            "both"
          ],
          "required": [],
          "title": "engine"
        },
        "extraEgress": {
          "description": "Extra egress rules appended to every service's policy.",
          "items": {
            "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.networking.v1.NetworkPolicyEgressRule",
            "required": []
          },
          "required": [],
          "title": "extraEgress",
          "type": "array"
        },
        "extraIngress": {
          "description": "Extra ingress rules appended to every service's policy.",
          "items": {
            "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.networking.v1.NetworkPolicyIngressRule",
            "required": []
          },
          "required": [],
          "title": "extraIngress",
          "type": "array"
        },
        "gateway": {
          "additionalProperties": true,
          "description": "Selectors matching the Gateway API data plane, allowed to reach the frontend (and the API\nwhen `gateway.api.enabled` is set). Left empty they are derived from `gateway.parentRefs`:\nthe namespace of the first parent, and `gateway.networking.k8s.io/gateway-name` — the label\nCilium, Envoy Gateway, Istio and NGINX Gateway Fabric all put on the pods they provision.\n\nRestating the Gateway's identity here would be a second place to edit on a rename, and a\npolicy naming the wrong Gateway looks correct and blocks all inbound traffic.",
          "properties": {
            "namespaceSelector": {
              "additionalProperties": {
                "type": "string"
              },
              "description": "Namespace selector matching the Gateway's data plane. Empty derives it.",
              "required": [],
              "title": "namespaceSelector",
              "type": "object"
            },
            "podSelector": {
              "additionalProperties": {
                "type": "string"
              },
              "description": "Pod selector matching the Gateway's data plane. Empty derives it.",
              "required": [],
              "title": "podSelector",
              "type": "object"
            }
          },
          "required": [],
          "title": "gateway"
        },
        "ingressController": {
          "additionalProperties": true,
          "properties": {
            "namespaceSelector": {
              "additionalProperties": {
                "type": "string"
              },
              "description": "Namespace selector matching the ingress controller, allowed to reach the frontend.",
              "required": [],
              "title": "namespaceSelector",
              "type": "object"
            },
            "podSelector": {
              "additionalProperties": {
                "type": "string"
              },
              "description": "Pod selector matching the ingress controller.",
              "required": [],
              "title": "podSelector",
              "type": "object"
            }
          },
          "required": [],
          "title": "ingressController"
        },
        "internetCidrs": {
          "description": "Egress CIDRs treated as \"the internet\". The worker scrapes provider sites, sync talks to\nAniList and the notifier reaches SMTP and webhook endpoints, so these tiers need it.\nRFC1918 ranges and the cloud metadata endpoint are excluded automatically.",
          "items": {
            "required": [],
            "type": "string"
          },
          "required": [],
          "title": "internetCidrs",
          "type": "array"
        },
        "monitoring": {
          "additionalProperties": true,
          "properties": {
            "namespaceSelector": {
              "additionalProperties": {
                "type": "string"
              },
              "description": "Namespace selector matching Prometheus, allowed to reach the metrics port.",
              "required": [],
              "title": "namespaceSelector",
              "type": "object"
            }
          },
          "required": [],
          "title": "monitoring"
        }
      },
      "required": [],
      "title": "networkPolicy"
    },
    "postgresql": {
      "additionalProperties": true,
      "description": "Bundled PostgreSQL. A single instance with a PVC, on the same image the upstream compose\nstack pins. Deliberately not an operator and not a third-party subchart, so `helm install`\nworks on a bare cluster — but equally deliberately **not a production database**: one\nreplica, no failover, no point-in-time recovery. Use `externalDatabase` for anything real —\nand give it [pgvector](https://github.com/pgvector/pgvector), which migration `0027` requires.",
      "properties": {
        "auth": {
          "additionalProperties": true,
          "properties": {
            "database": {
              "default": "tankovault",
              "description": "Database name.",
              "required": [],
              "title": "database",
              "type": "string"
            },
            "password": {
              "default": "",
              "description": "Database password. Generated and persisted across upgrades when left empty.",
              "required": [],
              "title": "password",
              "type": "string"
            },
            "username": {
              "default": "tankovault",
              "description": "Database role.",
              "required": [],
              "title": "username",
              "type": "string"
            }
          },
          "required": [],
          "title": "auth"
        },
        "enabled": {
          "default": false,
          "description": "Deploy the bundled PostgreSQL.",
          "required": [],
          "title": "enabled",
          "type": "boolean"
        },
        "image": {
          "additionalProperties": true,
          "properties": {
            "repository": {
              "default": "pgvector/pgvector",
              "description": "Image repository. `pgvector/pgvector`, not stock `postgres`: TankoVault migration\n`0027_recsys_signals` runs `CREATE EXTENSION vector` and the recommender retrieves through\nan HNSW index, so the extension is a hard dependency of the deployment rather than an\noptional extra. This is the official PostgreSQL image with pgvector preinstalled — same\nmajor, same entrypoint, same environment contract, same `postgres` uid — so nothing else\nin this block changes and an existing PVC is reused in place. It is Debian-based rather\nthan Alpine; `resourcesPreset` already accommodates that.",
              "required": [],
              "title": "repository",
              "type": "string"
            },
            "tag": {
              "default": "pg18@sha256:691673308c99d2161ba298736f3147f1f22d79de2fb7ec93ae9b4afcab870b62",
              "description": "Image tag, pinned by digest. Held to the same digest as upstream's compose stack and CI\nservices, so the database this chart runs is the one the queries were type-checked against.",
              "required": [],
              "title": "tag",
              "type": "string"
            }
          },
          "required": [],
          "title": "image"
        },
        "persistence": {
          "additionalProperties": true,
          "properties": {
            "enabled": {
              "default": true,
              "description": "Persist the data directory on a PersistentVolumeClaim. Turning this off means the\nwhole catalogue is lost when the pod is rescheduled.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "existingClaim": {
              "default": "",
              "description": "Use an existing claim instead of creating one.",
              "required": [],
              "title": "existingClaim",
              "type": "string"
            },
            "size": {
              "default": "20Gi",
              "description": "Requested volume size.",
              "required": [],
              "title": "size",
              "type": "string"
            },
            "storageClassName": {
              "default": "",
              "description": "Storage class. Empty uses the cluster default.",
              "required": [],
              "title": "storageClassName",
              "type": "string"
            }
          },
          "required": [],
          "title": "persistence"
        },
        "resourcesPreset": {
          "default": "large",
          "description": "Resource t-shirt size.",
          "enum": [
            "",
            "nano",
            "micro",
            "small",
            "medium",
            "large"
          ],
          "required": [],
          "title": "resourcesPreset"
        }
      },
      "required": [],
      "title": "postgresql"
    },
    "profile": {
      "default": "production",
      "description": "Deployment profile, passed as `TANKOVAULT_PROFILE`. This is a process-level key: it is\nread before the layered configuration exists, so it can only ever come from the environment.\n`production` is what turns on the fail-fast secret validation and turns off the API docs\nendpoint; anything other than exactly `production` leaves those safeties off.",
      "enum": [
        "production",
        "staging",
        "development"
      ],
      "required": [],
      "title": "profile"
    },
    "serviceAccount": {
      "additionalProperties": true,
      "properties": {
        "annotations": {
          "additionalProperties": true,
          "description": "Additional annotations for the service account.",
          "required": [],
          "title": "annotations",
          "type": "object"
        },
        "create": {
          "default": true,
          "description": "Whether to create a dedicated service account. One account is shared by every workload:\nnothing in TankoVault talks to the Kubernetes API, so per-service accounts would add\nobjects without reducing any privilege.",
          "required": [],
          "title": "create",
          "type": "boolean"
        },
        "name": {
          "default": "",
          "description": "Custom service account name (auto-generated if empty).",
          "required": [],
          "title": "name",
          "type": "string"
        }
      },
      "required": [],
      "title": "serviceAccount"
    },
    "services": {
      "additionalProperties": true,
      "description": "Per-service settings. Each block is merged over `defaults`, so any key from `defaults`\nmay be repeated here for one service only.",
      "properties": {
        "api": {
          "additionalProperties": true,
          "description": "The axum REST edge: authentication, read models, write endpoints, administration and\nthe server-sent scan feed.",
          "properties": {
            "autoscaling": {
              "additionalProperties": true,
              "properties": {
                "enabled": {
                  "default": false,
                  "description": "Enable a HorizontalPodAutoscaler.",
                  "required": [],
                  "title": "enabled",
                  "type": "boolean"
                },
                "maxReplicas": {
                  "default": 10,
                  "description": "Maximum replicas.",
                  "minimum": 1,
                  "required": [],
                  "title": "maxReplicas",
                  "type": "integer"
                },
                "minReplicas": {
                  "default": 2,
                  "description": "Minimum replicas.",
                  "minimum": 1,
                  "required": [],
                  "title": "minReplicas",
                  "type": "integer"
                },
                "targetCPUUtilizationPercentage": {
                  "default": 80,
                  "description": "Target average CPU utilisation, percent.",
                  "required": [],
                  "title": "targetCPUUtilizationPercentage",
                  "type": [
                    "integer",
                    "null"
                  ]
                },
                "targetMemoryUtilizationPercentage": {
                  "default": "null",
                  "description": "Target average memory utilisation, percent.",
                  "required": [],
                  "title": "targetMemoryUtilizationPercentage",
                  "type": [
                    "integer",
                    "null"
                  ]
                }
              },
              "required": [],
              "title": "autoscaling"
            },
            "config": {
              "additionalProperties": true,
              "description": "Service-specific configuration, merged over the global `config` tree.",
              "required": [],
              "title": "config",
              "type": "object"
            },
            "enabled": {
              "default": true,
              "description": "Deploy the API.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "image": {
              "additionalProperties": true,
              "properties": {
                "repository": {
                  "default": "timschoenle/tankovault-api",
                  "description": "Image repository.",
                  "required": [],
                  "title": "repository",
                  "type": "string"
                },
                "tag": {
                  "default": "v7.0.0@sha256:95f341761e006b1c6f50bf9e6f168cdf81522b8358a18879a67606ddb10c129f",
                  "description": "Image tag, pinned by digest.",
                  "required": [],
                  "title": "tag",
                  "type": "string"
                }
              },
              "required": [],
              "title": "image"
            },
            "podDisruptionBudget": {
              "additionalProperties": true,
              "properties": {
                "enabled": {
                  "default": false,
                  "description": "Create a PodDisruptionBudget.",
                  "required": [],
                  "title": "enabled",
                  "type": "boolean"
                },
                "maxUnavailable": {
                  "default": "null",
                  "description": "Maximum unavailable pods.",
                  "required": [],
                  "title": "maxUnavailable",
                  "type": [
                    "integer",
                    "string",
                    "null"
                  ]
                },
                "minAvailable": {
                  "default": 1,
                  "description": "Minimum available pods during voluntary disruption.",
                  "required": [],
                  "title": "minAvailable",
                  "type": [
                    "integer",
                    "string",
                    "null"
                  ]
                }
              },
              "required": [],
              "title": "podDisruptionBudget"
            },
            "replicaCount": {
              "default": 2,
              "description": "Replica count, ignored when autoscaling is enabled.",
              "minimum": 0,
              "required": [],
              "title": "replicaCount",
              "type": "integer"
            },
            "resourcesPreset": {
              "default": "large",
              "description": "Resource t-shirt size.",
              "enum": [
                "",
                "nano",
                "micro",
                "small",
                "medium",
                "large"
              ],
              "required": [],
              "title": "resourcesPreset"
            },
            "service": {
              "additionalProperties": true,
              "properties": {
                "annotations": {
                  "additionalProperties": true,
                  "description": "Extra Service annotations.",
                  "required": [],
                  "title": "annotations",
                  "type": "object"
                },
                "type": {
                  "default": "ClusterIP",
                  "description": "Service type.",
                  "enum": [
                    "ClusterIP",
                    "NodePort",
                    "LoadBalancer"
                  ],
                  "required": [],
                  "title": "type"
                }
              },
              "required": [],
              "title": "service"
            }
          },
          "required": [],
          "title": "api"
        },
        "challengeSolver": {
          "additionalProperties": true,
          "description": "Modular bot-management bypass tier. Detects Cloudflare, JavaScript and Turnstile\ninterstitials and delegates to a solver backend, TRAWL by default.",
          "properties": {
            "autoscaling": {
              "additionalProperties": true,
              "properties": {
                "enabled": {
                  "default": false,
                  "description": "Enable a HorizontalPodAutoscaler.",
                  "required": [],
                  "title": "enabled",
                  "type": "boolean"
                },
                "maxReplicas": {
                  "default": 4,
                  "description": "Maximum replicas.",
                  "minimum": 1,
                  "required": [],
                  "title": "maxReplicas",
                  "type": "integer"
                },
                "minReplicas": {
                  "default": 1,
                  "description": "Minimum replicas.",
                  "minimum": 1,
                  "required": [],
                  "title": "minReplicas",
                  "type": "integer"
                },
                "targetCPUUtilizationPercentage": {
                  "default": 80,
                  "description": "Target average CPU utilisation, percent.",
                  "required": [],
                  "title": "targetCPUUtilizationPercentage",
                  "type": [
                    "integer",
                    "null"
                  ]
                },
                "targetMemoryUtilizationPercentage": {
                  "default": "null",
                  "description": "Target average memory utilisation, percent.",
                  "required": [],
                  "title": "targetMemoryUtilizationPercentage",
                  "type": [
                    "integer",
                    "null"
                  ]
                }
              },
              "required": [],
              "title": "autoscaling"
            },
            "config": {
              "additionalProperties": true,
              "description": "Service-specific configuration, merged over the global `config` tree.",
              "required": [],
              "title": "config",
              "type": "object"
            },
            "enabled": {
              "default": true,
              "description": "Deploy the challenge solver.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "image": {
              "additionalProperties": true,
              "properties": {
                "repository": {
                  "default": "timschoenle/tankovault-challenge-solver",
                  "description": "Image repository.",
                  "required": [],
                  "title": "repository",
                  "type": "string"
                },
                "tag": {
                  "default": "v7.0.0@sha256:7b63b74767e429bb0833a685e7ab9bf7c48c0be02d71677becdb86255a1014bc",
                  "description": "Image tag, pinned by digest.",
                  "required": [],
                  "title": "tag",
                  "type": "string"
                }
              },
              "required": [],
              "title": "image"
            },
            "podDisruptionBudget": {
              "additionalProperties": true,
              "properties": {
                "enabled": {
                  "default": false,
                  "description": "Create a PodDisruptionBudget.",
                  "required": [],
                  "title": "enabled",
                  "type": "boolean"
                },
                "maxUnavailable": {
                  "default": "null",
                  "description": "Maximum unavailable pods.",
                  "required": [],
                  "title": "maxUnavailable",
                  "type": [
                    "integer",
                    "string",
                    "null"
                  ]
                },
                "minAvailable": {
                  "default": 1,
                  "description": "Minimum available pods during voluntary disruption.",
                  "required": [],
                  "title": "minAvailable",
                  "type": [
                    "integer",
                    "string",
                    "null"
                  ]
                }
              },
              "required": [],
              "title": "podDisruptionBudget"
            },
            "replicaCount": {
              "default": 1,
              "description": "Replica count, ignored when autoscaling is enabled.",
              "minimum": 0,
              "required": [],
              "title": "replicaCount",
              "type": "integer"
            },
            "resourcesPreset": {
              "default": "medium",
              "description": "Resource t-shirt size.",
              "enum": [
                "",
                "nano",
                "micro",
                "small",
                "medium",
                "large"
              ],
              "required": [],
              "title": "resourcesPreset"
            },
            "service": {
              "additionalProperties": true,
              "properties": {
                "annotations": {
                  "additionalProperties": true,
                  "description": "Extra Service annotations.",
                  "required": [],
                  "title": "annotations",
                  "type": "object"
                },
                "type": {
                  "default": "ClusterIP",
                  "description": "Service type. Publishing this service exposes a privileged contract.",
                  "enum": [
                    "ClusterIP",
                    "NodePort",
                    "LoadBalancer"
                  ],
                  "required": [],
                  "title": "type"
                }
              },
              "required": [],
              "title": "service"
            }
          },
          "required": [],
          "title": "challengeSolver"
        },
        "controlPlane": {
          "additionalProperties": true,
          "description": "The singleton scheduler: run planning, task distribution and provider health. Safe to\nrun with more than one replica — it elects a leader through Redis, and falls open to\nsole-leader when Redis is absent.",
          "properties": {
            "config": {
              "additionalProperties": true,
              "description": "Service-specific configuration, merged over the global `config` tree.",
              "required": [],
              "title": "config",
              "type": "object"
            },
            "enabled": {
              "default": true,
              "description": "Deploy the control plane.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "image": {
              "additionalProperties": true,
              "properties": {
                "repository": {
                  "default": "timschoenle/tankovault-control-plane",
                  "description": "Image repository.",
                  "required": [],
                  "title": "repository",
                  "type": "string"
                },
                "tag": {
                  "default": "v7.0.0@sha256:38e90ef539c5c55f67eab2a653612b470adef58217aa876be029bd9002c76cba",
                  "description": "Image tag, pinned by digest.",
                  "required": [],
                  "title": "tag",
                  "type": "string"
                }
              },
              "required": [],
              "title": "image"
            },
            "podDisruptionBudget": {
              "additionalProperties": true,
              "properties": {
                "enabled": {
                  "default": false,
                  "description": "Create a PodDisruptionBudget.",
                  "required": [],
                  "title": "enabled",
                  "type": "boolean"
                },
                "maxUnavailable": {
                  "default": "null",
                  "description": "Maximum unavailable pods.",
                  "required": [],
                  "title": "maxUnavailable",
                  "type": [
                    "integer",
                    "string",
                    "null"
                  ]
                },
                "minAvailable": {
                  "default": 1,
                  "description": "Minimum available pods during voluntary disruption.",
                  "required": [],
                  "title": "minAvailable",
                  "type": [
                    "integer",
                    "string",
                    "null"
                  ]
                }
              },
              "required": [],
              "title": "podDisruptionBudget"
            },
            "replicaCount": {
              "default": 1,
              "description": "Replica count.",
              "minimum": 0,
              "required": [],
              "title": "replicaCount",
              "type": "integer"
            },
            "resourcesPreset": {
              "default": "medium",
              "description": "Resource t-shirt size.",
              "enum": [
                "",
                "nano",
                "micro",
                "small",
                "medium",
                "large"
              ],
              "required": [],
              "title": "resourcesPreset"
            },
            "service": {
              "additionalProperties": true,
              "properties": {
                "annotations": {
                  "additionalProperties": true,
                  "description": "Extra Service annotations.",
                  "required": [],
                  "title": "annotations",
                  "type": "object"
                },
                "type": {
                  "default": "ClusterIP",
                  "description": "Service type. Publishing this service exposes a privileged contract; the chart\nrefuses anything but `ClusterIP` unless `allowUnsafeExposure` is set.",
                  "enum": [
                    "ClusterIP",
                    "NodePort",
                    "LoadBalancer"
                  ],
                  "required": [],
                  "title": "type"
                }
              },
              "required": [],
              "title": "service"
            }
          },
          "required": [],
          "title": "controlPlane"
        },
        "frontend": {
          "additionalProperties": true,
          "description": "The Dioxus WASM SPA and its axum server. It serves the client and reverse-proxies\n`/v1/*` to the API, so this single origin is all a browser needs — which is why it is the\nonly service the ingress exposes.",
          "properties": {
            "autoscaling": {
              "additionalProperties": true,
              "properties": {
                "enabled": {
                  "default": false,
                  "description": "Enable a HorizontalPodAutoscaler.",
                  "required": [],
                  "title": "enabled",
                  "type": "boolean"
                },
                "maxReplicas": {
                  "default": 6,
                  "description": "Maximum replicas.",
                  "minimum": 1,
                  "required": [],
                  "title": "maxReplicas",
                  "type": "integer"
                },
                "minReplicas": {
                  "default": 2,
                  "description": "Minimum replicas.",
                  "minimum": 1,
                  "required": [],
                  "title": "minReplicas",
                  "type": "integer"
                },
                "targetCPUUtilizationPercentage": {
                  "default": 80,
                  "description": "Target average CPU utilisation, percent. Null disables the CPU metric.",
                  "required": [],
                  "title": "targetCPUUtilizationPercentage",
                  "type": [
                    "integer",
                    "null"
                  ]
                },
                "targetMemoryUtilizationPercentage": {
                  "default": "null",
                  "description": "Target average memory utilisation, percent. Null disables the memory metric.",
                  "required": [],
                  "title": "targetMemoryUtilizationPercentage",
                  "type": [
                    "integer",
                    "null"
                  ]
                }
              },
              "required": [],
              "title": "autoscaling"
            },
            "config": {
              "additionalProperties": true,
              "description": "Service-specific configuration, merged over the global `config` tree for this service\nonly. Rendered into this service's own TOML fragment.",
              "required": [],
              "title": "config",
              "type": "object"
            },
            "enabled": {
              "default": true,
              "description": "Deploy the frontend.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "image": {
              "additionalProperties": true,
              "properties": {
                "repository": {
                  "default": "timschoenle/tankovault-frontend",
                  "description": "Image repository.",
                  "required": [],
                  "title": "repository",
                  "type": "string"
                },
                "tag": {
                  "default": "v7.0.0@sha256:a6bccefa37871d5c880b3ce8df9bc9e1a843ae4b6345fa06a18507f12c6344bd",
                  "description": "Image tag, pinned by digest.",
                  "required": [],
                  "title": "tag",
                  "type": "string"
                }
              },
              "required": [],
              "title": "image"
            },
            "podDisruptionBudget": {
              "additionalProperties": true,
              "properties": {
                "enabled": {
                  "default": false,
                  "description": "Create a PodDisruptionBudget.",
                  "required": [],
                  "title": "enabled",
                  "type": "boolean"
                },
                "maxUnavailable": {
                  "default": "null",
                  "description": "Maximum unavailable pods. Mutually exclusive with `minAvailable`.",
                  "required": [],
                  "title": "maxUnavailable",
                  "type": [
                    "integer",
                    "string",
                    "null"
                  ]
                },
                "minAvailable": {
                  "default": 1,
                  "description": "Minimum available pods during voluntary disruption.",
                  "required": [],
                  "title": "minAvailable",
                  "type": [
                    "integer",
                    "string",
                    "null"
                  ]
                }
              },
              "required": [],
              "title": "podDisruptionBudget"
            },
            "replicaCount": {
              "default": 2,
              "description": "Replica count, ignored when autoscaling is enabled.",
              "minimum": 0,
              "required": [],
              "title": "replicaCount",
              "type": "integer"
            },
            "resourcesPreset": {
              "default": "small",
              "description": "Resource t-shirt size.",
              "enum": [
                "",
                "nano",
                "micro",
                "small",
                "medium",
                "large"
              ],
              "required": [],
              "title": "resourcesPreset"
            },
            "service": {
              "additionalProperties": true,
              "properties": {
                "annotations": {
                  "additionalProperties": true,
                  "description": "Extra Service annotations.",
                  "required": [],
                  "title": "annotations",
                  "type": "object"
                },
                "type": {
                  "default": "ClusterIP",
                  "description": "Service type. The frontend is the one service it is safe to publish directly.",
                  "enum": [
                    "ClusterIP",
                    "NodePort",
                    "LoadBalancer"
                  ],
                  "required": [],
                  "title": "type"
                }
              },
              "required": [],
              "title": "service"
            }
          },
          "required": [],
          "title": "frontend"
        },
        "notifier": {
          "additionalProperties": true,
          "description": "Distributes new-chapter notifications to users over email, Discord and generic webhooks.",
          "properties": {
            "config": {
              "additionalProperties": true,
              "description": "Service-specific configuration, merged over the global `config` tree.",
              "required": [],
              "title": "config",
              "type": "object"
            },
            "enabled": {
              "default": true,
              "description": "Deploy the notifier.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "image": {
              "additionalProperties": true,
              "properties": {
                "repository": {
                  "default": "timschoenle/tankovault-notifier",
                  "description": "Image repository.",
                  "required": [],
                  "title": "repository",
                  "type": "string"
                },
                "tag": {
                  "default": "v7.0.0@sha256:391a28d114fada550b59335d7a949c99d041305c66a897376fcd01cb02fb3ca6",
                  "description": "Image tag, pinned by digest.",
                  "required": [],
                  "title": "tag",
                  "type": "string"
                }
              },
              "required": [],
              "title": "image"
            },
            "podDisruptionBudget": {
              "additionalProperties": true,
              "properties": {
                "enabled": {
                  "default": false,
                  "description": "Create a PodDisruptionBudget.",
                  "required": [],
                  "title": "enabled",
                  "type": "boolean"
                },
                "maxUnavailable": {
                  "default": "null",
                  "description": "Maximum unavailable pods.",
                  "required": [],
                  "title": "maxUnavailable",
                  "type": [
                    "integer",
                    "string",
                    "null"
                  ]
                },
                "minAvailable": {
                  "default": 1,
                  "description": "Minimum available pods during voluntary disruption.",
                  "required": [],
                  "title": "minAvailable",
                  "type": [
                    "integer",
                    "string",
                    "null"
                  ]
                }
              },
              "required": [],
              "title": "podDisruptionBudget"
            },
            "replicaCount": {
              "default": 1,
              "description": "Replica count. One by default; upstream runs this tier as a singleton.",
              "minimum": 0,
              "required": [],
              "title": "replicaCount",
              "type": "integer"
            },
            "resourcesPreset": {
              "default": "medium",
              "description": "Resource t-shirt size.",
              "enum": [
                "",
                "nano",
                "micro",
                "small",
                "medium",
                "large"
              ],
              "required": [],
              "title": "resourcesPreset"
            },
            "service": {
              "additionalProperties": true,
              "properties": {
                "annotations": {
                  "additionalProperties": true,
                  "description": "Extra Service annotations.",
                  "required": [],
                  "title": "annotations",
                  "type": "object"
                },
                "type": {
                  "default": "ClusterIP",
                  "description": "Service type.",
                  "enum": [
                    "ClusterIP",
                    "NodePort",
                    "LoadBalancer"
                  ],
                  "required": [],
                  "title": "type"
                }
              },
              "required": [],
              "title": "service"
            }
          },
          "required": [],
          "title": "notifier"
        },
        "render": {
          "additionalProperties": true,
          "description": "Optional headless-browser tier for JavaScript-rendered pages; doubles as a solver\nbackend. This is the one service not built on `scratch`: it is a Debian base driving a real\nChromium, so it needs writable scratch space and a shared-memory volume.",
          "properties": {
            "autoscaling": {
              "additionalProperties": true,
              "properties": {
                "enabled": {
                  "default": false,
                  "description": "Enable a HorizontalPodAutoscaler.",
                  "required": [],
                  "title": "enabled",
                  "type": "boolean"
                },
                "maxReplicas": {
                  "default": 4,
                  "description": "Maximum replicas.",
                  "minimum": 1,
                  "required": [],
                  "title": "maxReplicas",
                  "type": "integer"
                },
                "minReplicas": {
                  "default": 1,
                  "description": "Minimum replicas.",
                  "minimum": 1,
                  "required": [],
                  "title": "minReplicas",
                  "type": "integer"
                },
                "targetCPUUtilizationPercentage": {
                  "default": 80,
                  "description": "Target average CPU utilisation, percent.",
                  "required": [],
                  "title": "targetCPUUtilizationPercentage",
                  "type": [
                    "integer",
                    "null"
                  ]
                },
                "targetMemoryUtilizationPercentage": {
                  "default": "null",
                  "description": "Target average memory utilisation, percent.",
                  "required": [],
                  "title": "targetMemoryUtilizationPercentage",
                  "type": [
                    "integer",
                    "null"
                  ]
                }
              },
              "required": [],
              "title": "autoscaling"
            },
            "config": {
              "additionalProperties": true,
              "description": "Service-specific configuration, merged over the global `config` tree.",
              "required": [],
              "title": "config",
              "type": "object"
            },
            "enabled": {
              "default": false,
              "description": "Deploy the render tier.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "homeDir": {
              "default": "/home/nonroot",
              "description": "Home directory of the image's nonroot user, mounted as a writable emptyDir. Chromium\nwrites its profile and crashpad database here; when it is not writable the failure\nsurfaces as a misleading `--database is required` error.",
              "required": [],
              "title": "homeDir",
              "type": "string"
            },
            "image": {
              "additionalProperties": true,
              "properties": {
                "repository": {
                  "default": "timschoenle/tankovault-render",
                  "description": "Image repository.",
                  "required": [],
                  "title": "repository",
                  "type": "string"
                },
                "tag": {
                  "default": "v7.0.0@sha256:093e0872e2c17eb378edf186c9252295bd7c3e014bdb0561f2bab5ec9fb2e70a",
                  "description": "Image tag, pinned by digest.",
                  "required": [],
                  "title": "tag",
                  "type": "string"
                }
              },
              "required": [],
              "title": "image"
            },
            "podDisruptionBudget": {
              "additionalProperties": true,
              "properties": {
                "enabled": {
                  "default": false,
                  "description": "Create a PodDisruptionBudget.",
                  "required": [],
                  "title": "enabled",
                  "type": "boolean"
                },
                "maxUnavailable": {
                  "default": "null",
                  "description": "Maximum unavailable pods.",
                  "required": [],
                  "title": "maxUnavailable",
                  "type": [
                    "integer",
                    "string",
                    "null"
                  ]
                },
                "minAvailable": {
                  "default": 1,
                  "description": "Minimum available pods during voluntary disruption.",
                  "required": [],
                  "title": "minAvailable",
                  "type": [
                    "integer",
                    "string",
                    "null"
                  ]
                }
              },
              "required": [],
              "title": "podDisruptionBudget"
            },
            "replicaCount": {
              "default": 1,
              "description": "Replica count, ignored when autoscaling is enabled.",
              "minimum": 0,
              "required": [],
              "title": "replicaCount",
              "type": "integer"
            },
            "resources": {
              "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.ResourceRequirements",
              "required": []
            },
            "service": {
              "additionalProperties": true,
              "properties": {
                "annotations": {
                  "additionalProperties": true,
                  "description": "Extra Service annotations.",
                  "required": [],
                  "title": "annotations",
                  "type": "object"
                },
                "type": {
                  "default": "ClusterIP",
                  "description": "Service type. Publishing this service exposes a privileged contract.",
                  "enum": [
                    "ClusterIP",
                    "NodePort",
                    "LoadBalancer"
                  ],
                  "required": [],
                  "title": "type"
                }
              },
              "required": [],
              "title": "service"
            },
            "shmSize": {
              "default": "1Gi",
              "description": "Size of the `/dev/shm` in-memory volume. Chromium crashes with cryptic renderer\nfailures on the 64Mi Kubernetes default.",
              "required": [],
              "title": "shmSize",
              "type": "string"
            }
          },
          "required": [],
          "title": "render"
        },
        "sync": {
          "additionalProperties": true,
          "description": "Bidirectional AniList integration and metadata enrichment.",
          "properties": {
            "config": {
              "additionalProperties": true,
              "description": "Service-specific configuration, merged over the global `config` tree.",
              "required": [],
              "title": "config",
              "type": "object"
            },
            "enabled": {
              "default": true,
              "description": "Deploy the sync service. Requires the `anilist` credentials.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "image": {
              "additionalProperties": true,
              "properties": {
                "repository": {
                  "default": "timschoenle/tankovault-sync",
                  "description": "Image repository.",
                  "required": [],
                  "title": "repository",
                  "type": "string"
                },
                "tag": {
                  "default": "v7.0.0@sha256:dbfb5fb96e5bf2ec84f63b33b761e0ade1dec541115fc13f4fcc27926ee09e19",
                  "description": "Image tag, pinned by digest.",
                  "required": [],
                  "title": "tag",
                  "type": "string"
                }
              },
              "required": [],
              "title": "image"
            },
            "podDisruptionBudget": {
              "additionalProperties": true,
              "properties": {
                "enabled": {
                  "default": false,
                  "description": "Create a PodDisruptionBudget.",
                  "required": [],
                  "title": "enabled",
                  "type": "boolean"
                },
                "maxUnavailable": {
                  "default": "null",
                  "description": "Maximum unavailable pods.",
                  "required": [],
                  "title": "maxUnavailable",
                  "type": [
                    "integer",
                    "string",
                    "null"
                  ]
                },
                "minAvailable": {
                  "default": 1,
                  "description": "Minimum available pods during voluntary disruption.",
                  "required": [],
                  "title": "minAvailable",
                  "type": [
                    "integer",
                    "string",
                    "null"
                  ]
                }
              },
              "required": [],
              "title": "podDisruptionBudget"
            },
            "replicaCount": {
              "default": 1,
              "description": "Replica count. One by default: the reconcile loop is interval-driven and has no\nleader election, so extra replicas duplicate outbound AniList calls.",
              "minimum": 0,
              "required": [],
              "title": "replicaCount",
              "type": "integer"
            },
            "resourcesPreset": {
              "default": "medium",
              "description": "Resource t-shirt size.",
              "enum": [
                "",
                "nano",
                "micro",
                "small",
                "medium",
                "large"
              ],
              "required": [],
              "title": "resourcesPreset"
            },
            "service": {
              "additionalProperties": true,
              "properties": {
                "annotations": {
                  "additionalProperties": true,
                  "description": "Extra Service annotations.",
                  "required": [],
                  "title": "annotations",
                  "type": "object"
                },
                "type": {
                  "default": "ClusterIP",
                  "description": "Service type. Publishing this service exposes a privileged contract.",
                  "enum": [
                    "ClusterIP",
                    "NodePort",
                    "LoadBalancer"
                  ],
                  "required": [],
                  "title": "type"
                }
              },
              "required": [],
              "title": "service"
            }
          },
          "required": [],
          "title": "sync"
        },
        "worker": {
          "additionalProperties": true,
          "description": "Fetches and parses provider data through the adapters and upserts chapter and metadata\nchanges. Scales horizontally for free: replicas join one NATS JetStream consumer group.",
          "properties": {
            "autoscaling": {
              "additionalProperties": true,
              "properties": {
                "enabled": {
                  "default": false,
                  "description": "Enable a HorizontalPodAutoscaler.",
                  "required": [],
                  "title": "enabled",
                  "type": "boolean"
                },
                "maxReplicas": {
                  "default": 10,
                  "description": "Maximum replicas.",
                  "minimum": 1,
                  "required": [],
                  "title": "maxReplicas",
                  "type": "integer"
                },
                "minReplicas": {
                  "default": 2,
                  "description": "Minimum replicas.",
                  "minimum": 1,
                  "required": [],
                  "title": "minReplicas",
                  "type": "integer"
                },
                "targetCPUUtilizationPercentage": {
                  "default": 80,
                  "description": "Target average CPU utilisation, percent.",
                  "required": [],
                  "title": "targetCPUUtilizationPercentage",
                  "type": [
                    "integer",
                    "null"
                  ]
                },
                "targetMemoryUtilizationPercentage": {
                  "default": "null",
                  "description": "Target average memory utilisation, percent.",
                  "required": [],
                  "title": "targetMemoryUtilizationPercentage",
                  "type": [
                    "integer",
                    "null"
                  ]
                }
              },
              "required": [],
              "title": "autoscaling"
            },
            "config": {
              "additionalProperties": true,
              "description": "Service-specific configuration, merged over the global `config` tree.",
              "required": [],
              "title": "config",
              "type": "object"
            },
            "enabled": {
              "default": true,
              "description": "Deploy the worker.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "image": {
              "additionalProperties": true,
              "properties": {
                "repository": {
                  "default": "timschoenle/tankovault-worker",
                  "description": "Image repository.",
                  "required": [],
                  "title": "repository",
                  "type": "string"
                },
                "tag": {
                  "default": "v7.0.0@sha256:a7f0f569caa86dda74b2eab2098736822227850f3dd4c2249abaade29b91736c",
                  "description": "Image tag, pinned by digest.",
                  "required": [],
                  "title": "tag",
                  "type": "string"
                }
              },
              "required": [],
              "title": "image"
            },
            "podDisruptionBudget": {
              "additionalProperties": true,
              "properties": {
                "enabled": {
                  "default": false,
                  "description": "Create a PodDisruptionBudget.",
                  "required": [],
                  "title": "enabled",
                  "type": "boolean"
                },
                "maxUnavailable": {
                  "default": "null",
                  "description": "Maximum unavailable pods.",
                  "required": [],
                  "title": "maxUnavailable",
                  "type": [
                    "integer",
                    "string",
                    "null"
                  ]
                },
                "minAvailable": {
                  "default": 1,
                  "description": "Minimum available pods during voluntary disruption.",
                  "required": [],
                  "title": "minAvailable",
                  "type": [
                    "integer",
                    "string",
                    "null"
                  ]
                }
              },
              "required": [],
              "title": "podDisruptionBudget"
            },
            "replicaCount": {
              "default": 2,
              "description": "Replica count, ignored when autoscaling is enabled.",
              "minimum": 0,
              "required": [],
              "title": "replicaCount",
              "type": "integer"
            },
            "resourcesPreset": {
              "default": "large",
              "description": "Resource t-shirt size.",
              "enum": [
                "",
                "nano",
                "micro",
                "small",
                "medium",
                "large"
              ],
              "required": [],
              "title": "resourcesPreset"
            },
            "service": {
              "additionalProperties": true,
              "properties": {
                "annotations": {
                  "additionalProperties": true,
                  "description": "Extra Service annotations.",
                  "required": [],
                  "title": "annotations",
                  "type": "object"
                },
                "type": {
                  "default": "ClusterIP",
                  "description": "Service type.",
                  "enum": [
                    "ClusterIP",
                    "NodePort",
                    "LoadBalancer"
                  ],
                  "required": [],
                  "title": "type"
                }
              },
              "required": [],
              "title": "service"
            }
          },
          "required": [],
          "title": "worker"
        }
      },
      "required": [],
      "title": "services"
    },
    "trawl": {
      "additionalProperties": true,
      "description": "Bundled [TRAWL](https://github.com/germondai/trawl), the default backend for the challenge\nsolver.",
      "properties": {
        "browserPoolSize": {
          "default": 1,
          "description": "Number of warm browser instances to keep. Each is a full Firefox, so raising this raises\n`resources` with it. The image's own default is 3, which fits none of the sizes below.",
          "minimum": 1,
          "required": [],
          "title": "browserPoolSize",
          "type": "integer"
        },
        "enabled": {
          "default": false,
          "description": "Deploy the bundled TRAWL.",
          "required": [],
          "title": "enabled",
          "type": "boolean"
        },
        "image": {
          "additionalProperties": true,
          "properties": {
            "repository": {
              "default": "ghcr.io/germondai/trawl",
              "description": "Image repository.",
              "required": [],
              "title": "repository",
              "type": "string"
            },
            "tag": {
              "default": "1.3.1@sha256:1276e2937346190380310e15b3c4cbbf7757827c2ed3056459ad999b10cb90c9",
              "description": "Image tag, pinned by digest. Never `latest`: it silently changes the bot-management\nbehaviour the adapters are tuned against.",
              "required": [],
              "title": "tag",
              "type": "string"
            }
          },
          "required": [],
          "title": "image"
        },
        "redis": {
          "additionalProperties": true,
          "properties": {
            "enabled": {
              "default": true,
              "description": "Give TRAWL a Redis for its per-domain solved-session cache. Without one every request\nre-solves from a cold browser — slower, not broken.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "url": {
              "default": "",
              "description": "Connection URL. Defaults to the same Redis the services use (`valkey` or\n`externalRedis.url`). Sharing one instance is safe: TankoVault namespaces its keys under\n`tankovault:` and TRAWL uses `session:\u003cdomain\u003e`.",
              "required": [],
              "title": "url",
              "type": "string"
            }
          },
          "required": [],
          "title": "redis"
        },
        "resources": {
          "additionalProperties": true,
          "description": "Resource requests and limits. Set explicitly rather than by t-shirt size because the\nlargest preset caps memory at 1Gi, and a browser tier exceeds that before it has solved\nanything. Scale with `browserPoolSize`.",
          "properties": {
            "limits": {
              "additionalProperties": false,
              "properties": {
                "memory": {
                  "default": "2Gi",
                  "required": [],
                  "title": "memory",
                  "type": "string"
                }
              },
              "required": [
                "memory"
              ],
              "title": "limits",
              "type": "object"
            },
            "requests": {
              "additionalProperties": false,
              "properties": {
                "cpu": {
                  "default": "500m",
                  "required": [],
                  "title": "cpu",
                  "type": "string"
                },
                "memory": {
                  "default": "1Gi",
                  "required": [],
                  "title": "memory",
                  "type": "string"
                }
              },
              "required": [
                "cpu",
                "memory"
              ],
              "title": "requests",
              "type": "object"
            }
          },
          "required": [
            "requests",
            "limits"
          ],
          "title": "resources",
          "type": "object"
        },
        "runAsUser": {
          "default": 1000,
          "description": "Numeric UID to run as. The upstream image declares no `USER`, so it would run as root\nand the restricted Pod Security Standard would refuse the pod. Everything the image ships\nis world-readable, so any non-root UID works.",
          "required": [],
          "title": "runAsUser",
          "type": "integer"
        },
        "shmSize": {
          "default": "1Gi",
          "description": "Size of the `/dev/shm` in-memory volume. Firefox needs far more shared memory than the\n64Mi Kubernetes provides by default, and fails in confusing ways without it.",
          "required": [],
          "title": "shmSize",
          "type": "string"
        }
      },
      "required": [],
      "title": "trawl"
    },
    "valkey": {
      "additionalProperties": true,
      "description": "Bundled Valkey, backing the API rate limiter and the control-plane's leader election.\nEmbedded rather than taken as a subchart: the official Valkey chart defines its own\n`common.image`, and Helm's template namespace is global across dependencies, so it and the\n`common` library overwrite each other. TankoVault degrades gracefully without Valkey — the\nrate limiter falls back to per-replica in-memory counters and the scheduler to sole-leader.",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Deploy the bundled Valkey.",
          "required": [],
          "title": "enabled",
          "type": "boolean"
        },
        "image": {
          "additionalProperties": true,
          "properties": {
            "repository": {
              "default": "valkey/valkey",
              "description": "Image repository.",
              "required": [],
              "title": "repository",
              "type": "string"
            },
            "tag": {
              "default": "9.1.1-alpine@sha256:ee91f7a174ac4d6a6b0685b3a60e321f0a9dbbb691f9b0e285be2ba1d1be8328",
              "description": "Image tag, pinned by digest.",
              "required": [],
              "title": "tag",
              "type": "string"
            }
          },
          "required": [],
          "title": "image"
        },
        "resourcesPreset": {
          "default": "medium",
          "description": "Resource t-shirt size.",
          "enum": [
            "",
            "nano",
            "micro",
            "small",
            "medium",
            "large"
          ],
          "required": [],
          "title": "resourcesPreset"
        }
      },
      "required": [],
      "title": "valkey"
    }
  },
  "required": [],
  "type": "object"
}