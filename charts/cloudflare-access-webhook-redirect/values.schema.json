{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
    "affinity": {
      "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.Affinity",
      "required": []
    },
    "automountServiceAccountToken": {
      "default": false,
      "description": "Mount the ServiceAccount API token into the pod.\nSet on the pod itself, which is what actually keeps the token out of the container: the\nServiceAccount-level setting is ignored as soon as a pod names a different account.",
      "required": [],
      "title": "automountServiceAccountToken",
      "type": "boolean"
    },
    "autoscaling": {
      "additionalProperties": true,
      "description": "Horizontal Pod Autoscaler over the Deployment. While it is enabled the Deployment renders no\n`replicas`, so `replicaCount` is ignored and a `helm upgrade` leaves the current scale alone.",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Enable Horizontal Pod Autoscaler (HPA)",
          "required": [],
          "title": "enabled",
          "type": "boolean"
        },
        "maxReplicas": {
          "default": 5,
          "description": "Maximum replicas",
          "minimum": 1,
          "required": [],
          "title": "maxReplicas",
          "type": "integer"
        },
        "minReplicas": {
          "default": 1,
          "description": "Minimum replicas",
          "minimum": 1,
          "required": [],
          "title": "minReplicas",
          "type": "integer"
        },
        "targetCPUUtilizationPercentage": {
          "default": 80,
          "description": "Target CPU utilization (%)",
          "maximum": 100,
          "minimum": 1,
          "required": [],
          "title": "targetCPUUtilizationPercentage",
          "type": "integer"
        },
        "targetMemoryUtilizationPercentage": {
          "default": 80,
          "description": "Target memory utilization (%)",
          "maximum": 100,
          "minimum": 1,
          "required": [],
          "title": "targetMemoryUtilizationPercentage",
          "type": "integer"
        }
      },
      "required": [],
      "title": "autoscaling"
    },
    "cloudflare": {
      "additionalProperties": true,
      "description": "The Cloudflare Access service token the proxy presents to the protected origin. Both halves\nare mounted as files, so a rotation is picked up without a restart.",
      "properties": {
        "clientId": {
          "default": "",
          "description": "Cloudflare Access service token client ID (`cloudflare.client_id`). Rendered into the\nchart's Secret and mounted as a file, so a rotation is picked up without a restart.\nRequired unless `existingSecret` supplies it.",
          "required": [],
          "title": "clientId",
          "type": "string"
        },
        "clientSecret": {
          "default": "",
          "description": "Cloudflare Access service token client secret (`cloudflare.client_secret`). Required\nunless `existingSecret` supplies it.",
          "required": [],
          "title": "clientSecret",
          "type": "string"
        }
      },
      "required": [],
      "title": "cloudflare"
    },
    "common": {
      "description": "Shared template partials for the TimSchoenle Helm charts",
      "properties": {
        "affinity": {
          "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.Affinity",
          "required": []
        },
        "automountServiceAccountToken": {
          "default": false,
          "description": "Mount the ServiceAccount API token into the pod.\nSet on the pod itself, which is what actually keeps the token out of the container: the\nServiceAccount-level setting is ignored as soon as a pod names a different account.",
          "required": [],
          "title": "automountServiceAccountToken",
          "type": "boolean"
        },
        "commonAnnotations": {
          "additionalProperties": true,
          "description": "Annotations added to every object the chart creates. Values may contain Go templates.",
          "required": [],
          "title": "commonAnnotations",
          "type": "object"
        },
        "commonLabels": {
          "additionalProperties": true,
          "description": "Labels added to every object the chart creates. Values may contain Go templates.",
          "required": [],
          "title": "commonLabels",
          "type": "object"
        },
        "component": {
          "default": "",
          "description": "Value for the `app.kubernetes.io/component` label.",
          "required": [],
          "title": "component",
          "type": "string"
        },
        "config": {
          "additionalProperties": true,
          "description": "Application configuration, expressed as the TOML tree the service documents. Rendered by\n`common.toml` into the ConfigMap the pod mounts, never passed as environment variables: the\nloader every application shares refuses a key supplied by both the environment and a file,\nand a value that lives in a file is one the kubelet can rotate under a running process.",
          "required": [],
          "title": "config",
          "type": "object"
        },
        "configExtraToml": {
          "default": "",
          "description": "Verbatim TOML appended after the rendered `config` tree. The escape hatch for anything\n`common.toml` cannot express, notably arrays of tables.",
          "required": [],
          "title": "configExtraToml",
          "type": "string"
        },
        "configMount": {
          "additionalProperties": true,
          "description": "Where the rendered configuration and the credential files land in the container. Consumed\nby `common.fileConfig.*`, which also passes both directories to the application as\n`\u003cPREFIX\u003e_CONFIG` and `\u003cPREFIX\u003e_SECRETS_DIR`.",
          "properties": {
            "configDir": {
              "default": "",
              "description": "Directory the rendered `config.toml` is mounted at.",
              "required": [],
              "title": "configDir",
              "type": "string"
            },
            "secretsDir": {
              "default": "",
              "description": "Directory the credential files are mounted at, one file per configuration key.",
              "required": [],
              "title": "secretsDir",
              "type": "string"
            }
          },
          "required": [],
          "title": "configMount"
        },
        "dnsConfig": {
          "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.PodDNSConfig",
          "required": []
        },
        "dnsPolicy": {
          "default": "",
          "description": "Pod DNS policy.",
          "required": [],
          "title": "dnsPolicy",
          "type": "string"
        },
        "existingConfigMap": {
          "default": "",
          "description": "Name of an existing ConfigMap holding `config.toml`, replacing the one this chart renders.",
          "required": [],
          "title": "existingConfigMap",
          "type": "string"
        },
        "existingSecret": {
          "default": "",
          "description": "Name of an existing Secret holding the credential files, replacing the one this chart\nrenders. Its keys must be the configuration paths the service reads, with `__` for nesting\nand no dots — that is the file name the loader parses.",
          "required": [],
          "title": "existingSecret",
          "type": "string"
        },
        "extraEnv": {
          "description": "Additional environment variables appended to the application container.",
          "items": {
            "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.EnvVar",
            "required": []
          },
          "required": [],
          "title": "extraEnv",
          "type": "array"
        },
        "extraVolumeMounts": {
          "description": "Additional volume mounts added to the application container.",
          "items": {
            "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.VolumeMount",
            "required": []
          },
          "required": [],
          "title": "extraVolumeMounts",
          "type": "array"
        },
        "extraVolumes": {
          "description": "Additional volumes added to the pod.",
          "items": {
            "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.Volume",
            "required": []
          },
          "required": [],
          "title": "extraVolumes",
          "type": "array"
        },
        "fullnameOverride": {
          "default": "",
          "description": "Override the full generated resource name.",
          "required": [],
          "title": "fullnameOverride",
          "type": "string"
        },
        "gateway": {
          "additionalProperties": true,
          "description": "Gateway API configuration, consumed by `common.gateway.*`. The successor to `ingress`, and\nan independent switch from it: a cluster migrating between an Ingress controller and a Gateway\nimplementation runs both for a while.\n\nThe division of labour is the API's, not this chart's. A `Gateway` — the listeners, the\naddress, the certificates — belongs to the cluster operator; an application owns only the\n`HTTPRoute` that attaches to it. So the default here is route-only, and `create` is for\ninstalls that have no cluster-wide Gateway to attach to.",
          "properties": {
            "addresses": {
              "description": "Addresses requested for the created Gateway, e.g. a fixed `IPAddress`.",
              "items": {
                "required": []
              },
              "required": [],
              "title": "addresses",
              "type": "array"
            },
            "allowedRoutes": {
              "additionalProperties": true,
              "description": "Which routes may attach to the created Gateway's listeners. Defaults to `Same`: a Gateway\nthis chart owns should not be attachable from another namespace unless that is asked for.",
              "required": [],
              "title": "allowedRoutes",
              "type": "object"
            },
            "annotations": {
              "additionalProperties": true,
              "description": "Annotations for the HTTPRoute and the created Gateway. Values may contain Go templates.",
              "required": [],
              "title": "annotations",
              "type": "object"
            },
            "backendRefs": {
              "description": "Backends for rules that name none. Defaults to this chart's own Service. Weights are\nhonoured, so a traffic split needs no custom rule.",
              "items": {
                "required": []
              },
              "required": [],
              "title": "backendRefs",
              "type": "array"
            },
            "create": {
              "default": false,
              "description": "Also create the Gateway itself. Leave off when the cluster already runs one — that is the\nnormal case, and one Gateway per application usually means one load balancer per\napplication. When on, a route that names no parent attaches to it automatically.",
              "required": [],
              "title": "create",
              "type": "boolean"
            },
            "enabled": {
              "default": false,
              "description": "Create the HTTPRoute. Requires the `gateway.networking.k8s.io` CRDs;\n`common.gateway.validate` fails the render loudly rather than silently dropping the route\nwhen they are absent.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "filters": {
              "description": "Filters applied to the default rule: `RequestHeaderModifier`, `ResponseHeaderModifier`,\n`RequestRedirect`, `URLRewrite`, `RequestMirror`, `ExtensionRef`. This is where an\nIngress controller's annotations end up, as typed fields. Ignored when `rules` is set.",
              "items": {
                "required": []
              },
              "required": [],
              "title": "filters",
              "type": "array"
            },
            "gatewayClassName": {
              "default": "",
              "description": "GatewayClass that programs the created Gateway, e.g. `cilium`, `istio`, `envoy-gateway`,\n`nginx`. Required by `create`; a Gateway without one is never reconciled. Ignored otherwise.",
              "required": [],
              "title": "gatewayClassName",
              "type": "string"
            },
            "hostnames": {
              "description": "Hostnames the route serves. Values may contain Go templates.\n\nRequired unless `create` is set. A route with no hostnames matches every name its listener\naccepts: on a Gateway this chart owns that is harmless, and sometimes the point — an install\nreached by address has no DNS name to state. On a shared Gateway it means taking over traffic\nmeant for other applications, so the render refuses it.",
              "items": {
                "required": []
              },
              "required": [],
              "title": "hostnames",
              "type": "array"
            },
            "httpPort": {
              "default": 80,
              "description": "Port for the derived HTTP listener.",
              "required": [],
              "title": "httpPort",
              "type": "integer"
            },
            "httpsPort": {
              "default": 443,
              "description": "Port for the derived HTTPS listener.",
              "required": [],
              "title": "httpsPort",
              "type": "integer"
            },
            "httpsRedirect": {
              "additionalProperties": true,
              "description": "A second route that redirects plaintext traffic to HTTPS. Under Ingress this was a\ncontroller-specific annotation; Gateway API expresses it as a typed `RequestRedirect`\nfilter, which means it has to be a real object.",
              "properties": {
                "enabled": {
                  "default": false,
                  "description": "Create the redirect route.",
                  "required": [],
                  "title": "enabled",
                  "type": "boolean"
                },
                "port": {
                  "default": "null",
                  "description": "Port to redirect to. Left unset, the scheme implies it.",
                  "required": [],
                  "title": "port",
                  "type": [
                    "integer",
                    "null"
                  ]
                },
                "sectionName": {
                  "default": "",
                  "description": "Listener to bind the redirect to. Must be the plaintext one: attached to every listener,\nthe redirect would also apply to the HTTPS listener and loop forever. Defaults to `http`,\nthe name of the listener `create` renders.",
                  "required": [],
                  "title": "sectionName",
                  "type": "string"
                },
                "statusCode": {
                  "default": 301,
                  "description": "Redirect status code. `301` or `302`.",
                  "required": [],
                  "title": "statusCode",
                  "type": "integer"
                }
              },
              "required": [],
              "title": "httpsRedirect"
            },
            "infrastructure": {
              "additionalProperties": true,
              "description": "`infrastructure.labels` / `infrastructure.annotations` for the created Gateway, passed\nthrough to the load balancer the implementation provisions. Where Cilium's LB-IPAM\nannotations go.",
              "required": [],
              "title": "infrastructure",
              "type": "object"
            },
            "listeners": {
              "description": "Listeners for the created Gateway, replacing the derived ones entirely. Reach for this\nwhen a listener needs its own hostname or certificate.",
              "items": {
                "required": []
              },
              "required": [],
              "title": "listeners",
              "type": "array"
            },
            "parentRefs": {
              "description": "Gateways the route attaches to. Each entry takes `name` and optionally `namespace`,\n`sectionName` (a single listener), `port`, `group` and `kind`; the API's defaults are filled\nin. Values may contain Go templates.\n\nA route that names no parent is accepted by the API server and then does nothing — no\nlistener ever programs it — so this is required unless `create` is set.\nExample:\n```yaml\nparentRefs:\n  - name: shared-gateway\n    namespace: gateway-system\n    sectionName: https\n```",
              "items": {
                "required": []
              },
              "required": [],
              "title": "parentRefs",
              "type": "array"
            },
            "path": {
              "default": "/",
              "description": "Path prefix for the default rule. Ignored when `rules` is set.",
              "required": [],
              "title": "path",
              "type": "string"
            },
            "rules": {
              "description": "Routing rules, in full `HTTPRouteRule` form (`matches`, `filters`, `backendRefs`,\n`timeouts`). An entry that omits `backendRefs` inherits this chart's Service, so a rule that\nonly narrows the path does not have to restate where the traffic goes.\n\nLeft empty, the route gets one rule matching `path` as a prefix — the Gateway API equivalent\nof a single-path Ingress, and a complete configuration together with `hostnames`.",
              "items": {
                "required": []
              },
              "required": [],
              "title": "rules",
              "type": "array"
            },
            "timeouts": {
              "additionalProperties": true,
              "description": "Timeouts for the default rule: `request` and `backendRequest`, as Go durations. Ignored\nwhen `rules` is set.",
              "required": [],
              "title": "timeouts",
              "type": "object"
            },
            "tls": {
              "additionalProperties": true,
              "description": "TLS for the created Gateway's HTTPS listener. Ignored without `create`, and irrelevant\nwhen attaching to somebody else's Gateway — the certificate is theirs.",
              "properties": {
                "certificateRefs": {
                  "description": "Secrets holding the certificate. Required by `Terminate`: unlike an Ingress there is no\nconvention by which one is looked up from the hostname. A ref naming another namespace\nadditionally needs a `ReferenceGrant` there, which this chart deliberately does not create\n— a grant is the target namespace owner's to give.",
                  "items": {
                    "required": []
                  },
                  "required": [],
                  "title": "certificateRefs",
                  "type": "array"
                },
                "enabled": {
                  "default": false,
                  "description": "Add an HTTPS listener.",
                  "required": [],
                  "title": "enabled",
                  "type": "boolean"
                },
                "mode": {
                  "default": "Terminate",
                  "description": "TLS mode.",
                  "enum": [
                    "Terminate",
                    "Passthrough"
                  ],
                  "required": [],
                  "title": "mode"
                },
                "options": {
                  "additionalProperties": true,
                  "description": "Implementation-specific TLS options.",
                  "required": [],
                  "title": "options",
                  "type": "object"
                }
              },
              "required": [],
              "title": "tls"
            }
          },
          "required": [],
          "title": "gateway"
        },
        "global": {
          "description": "Global values are values that can be accessed from any chart or subchart by exactly the same name.",
          "required": [],
          "title": "global",
          "type": "object"
        },
        "grafanaDashboard": {
          "additionalProperties": true,
          "description": "Grafana dashboard delivery, consumed by `common.grafana.dashboard.*`. A consuming chart\nexposes this shape wherever it likes — `metrics.dashboard` is the convention — and passes it in\nas the `values` argument. Grafana has no Kubernetes-native dashboard type, so the partials\nrender both mechanisms: a labelled ConfigMap for the sidecar, and, optionally, one\n`GrafanaDashboard` per file for grafana-operator v5.",
          "properties": {
            "enabled": {
              "default": false,
              "description": "Create the ConfigMap holding the dashboard JSON. Required by the operator path too, which\nreferences it rather than duplicating the JSON into the custom resources.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "grafanaOperator": {
              "additionalProperties": true,
              "description": "The grafana-operator v5 path, rendered in addition to the ConfigMap above rather than\ninstead of it. Everything here is ignored while `enabled` is false.",
              "properties": {
                "allowCrossNamespaceImport": {
                  "default": true,
                  "description": "Let the resources bind to Grafana instances outside the release namespace. With `false`\nthe operator only considers Grafana custom resources in the same namespace.",
                  "required": [],
                  "title": "allowCrossNamespaceImport",
                  "type": "boolean"
                },
                "enabled": {
                  "default": false,
                  "description": "Also create one `GrafanaDashboard` per dashboard file. This is the only delivery path a\nchart can make cross-namespace on its own terms. Requires the `grafana.integreatly.org/v1beta1`\nCRDs; `common.grafana.dashboard.errors` fails the render loudly rather than silently\ndropping the objects when they are absent.",
                  "required": [],
                  "title": "enabled",
                  "type": "boolean"
                },
                "folder": {
                  "default": "",
                  "description": "Folder to file the dashboards under. Empty leaves them at the Grafana root.",
                  "required": [],
                  "title": "folder",
                  "type": "string"
                },
                "instanceSelector": {
                  "additionalProperties": true,
                  "description": "Label selector for the Grafana instances to import into. Must select something: unlike a\nKubernetes label selector, an empty `instanceSelector` matches no instance at all.",
                  "properties": {
                    "matchLabels": {
                      "additionalProperties": false,
                      "description": "Labels every Grafana custom resource to import into has to carry.",
                      "properties": {
                        "dashboards": {
                          "default": "grafana",
                          "required": [],
                          "title": "dashboards",
                          "type": "string"
                        }
                      },
                      "required": [],
                      "title": "matchLabels",
                      "type": "object"
                    }
                  },
                  "required": [],
                  "title": "instanceSelector",
                  "type": "object"
                },
                "resyncPeriod": {
                  "default": "5m",
                  "description": "How often the operator re-reconciles the dashboard, undoing edits made in the Grafana UI.\nA Go duration.",
                  "required": [],
                  "title": "resyncPeriod",
                  "type": "string"
                }
              },
              "required": [],
              "title": "grafanaOperator"
            },
            "label": {
              "default": "grafana_dashboard",
              "description": "Label a Grafana sidecar watches for dashboard ConfigMaps. Discovery is a property of the\n*Grafana* release: the sidecar only sees this ConfigMap if `sidecar.dashboards.searchNamespace`\ncovers the namespace it lands in, which a chart cannot influence from its own side.",
              "required": [],
              "title": "label",
              "type": "string"
            },
            "labelValue": {
              "default": "1",
              "description": "Value for that label.",
              "required": [],
              "title": "labelValue",
              "type": "string"
            }
          },
          "required": [],
          "title": "grafanaDashboard"
        },
        "hostAliases": {
          "description": "Host aliases injected into the pod's /etc/hosts.",
          "items": {
            "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.HostAlias",
            "required": []
          },
          "required": [],
          "title": "hostAliases",
          "type": "array"
        },
        "image": {
          "additionalProperties": true,
          "description": "Container image the workload runs, composed by `common.image` as `registry/repository:tag`.",
          "properties": {
            "pullPolicy": {
              "default": "",
              "description": "Image pull policy. Left empty, resolves to `Always` for the `latest` tag and\n`IfNotPresent` for anything pinned.",
              "enum": [
                "",
                "Always",
                "IfNotPresent",
                "Never"
              ],
              "required": [],
              "title": "pullPolicy"
            },
            "registry": {
              "default": "",
              "description": "Registry host. Left empty, the repository is used as-is (Docker Hub).",
              "required": [],
              "title": "registry",
              "type": "string"
            },
            "repository": {
              "default": "",
              "description": "Image repository. Required.",
              "required": [],
              "title": "repository",
              "type": "string"
            },
            "tag": {
              "default": "",
              "description": "Image tag. Defaults to the chart's `appVersion`. May pin a digest inline\n(`v1.2.3\nversion marker.",
              "required": [],
              "title": "tag",
              "type": "string"
            }
          },
          "required": [],
          "title": "image"
        },
        "imagePullSecrets": {
          "description": "Pull secrets for private registries. Accepts `- name: regcred` or the shorthand `- regcred`.",
          "items": {
            "required": []
          },
          "required": [],
          "title": "imagePullSecrets",
          "type": "array"
        },
        "kubeVersionOverride": {
          "default": "",
          "description": "Kubernetes version to target when branching on API availability.\nLets `helm template` render for a specific cluster version without a live connection.\nLeave empty to detect from the cluster.",
          "required": [],
          "title": "kubeVersionOverride",
          "type": "string"
        },
        "livenessProbe": {
          "additionalProperties": true,
          "description": "Liveness probe. See `startupProbe` for the accepted shape.",
          "properties": {
            "enabled": {
              "default": false,
              "description": "Enable the liveness probe.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            }
          },
          "required": [],
          "title": "livenessProbe"
        },
        "nameOverride": {
          "default": "",
          "description": "Override the chart name used in resource names and labels.",
          "required": [],
          "title": "nameOverride",
          "type": "string"
        },
        "namespaceOverride": {
          "default": "",
          "description": "Deploy into a namespace other than the release namespace.",
          "required": [],
          "title": "namespaceOverride",
          "type": "string"
        },
        "networkPolicy": {
          "additionalProperties": true,
          "description": "Network policy configuration.\n\nEvery generated egress rule is scoped by a `to:` selector. An egress rule that lists only\nports is not a restriction — the NetworkPolicy API reads a missing `to` as \"any\ndestination\", so such a policy permits traffic to every in-cluster service and to the\ncloud metadata endpoint.",
          "properties": {
            "cilium": {
              "additionalProperties": true,
              "description": "Cilium-only additions, used when `engine` is `cilium` or `both`. Everything above is\ntranslated into the CiliumNetworkPolicy automatically; these are the rules the portable API\nhas no way to express.\n\n`extraIngress`, `extraEgress` and the per-section `customRules` above are *not* carried over:\nthose are verbatim `networking.k8s.io/v1` rule objects and are not valid CNP. The fields\nbelow are their counterparts.",
              "properties": {
                "description": {
                  "default": "",
                  "description": "`spec.description`, which Cilium surfaces in `cilium policy get` and in Hubble flow\nverdicts. The one place to record *why* a rule exists where an operator debugging a drop\nwill actually see it.",
                  "required": [],
                  "title": "description",
                  "type": "string"
                },
                "egress": {
                  "additionalProperties": true,
                  "description": "Cilium-only egress rules.",
                  "properties": {
                    "customRules": {
                      "description": "Additional egress rules in CiliumNetworkPolicy form, appended verbatim.",
                      "items": {
                        "required": []
                      },
                      "required": [],
                      "title": "customRules",
                      "type": "array"
                    },
                    "dnsMatchPatterns": {
                      "description": "What the DNS proxy may resolve, e.g. `- matchPattern: \"*.example.com\"`. Defaults to\neverything, which only permits the lookup — an answer is still only reachable if some\nrule allows the address.",
                      "items": {
                        "required": []
                      },
                      "required": [],
                      "title": "dnsMatchPatterns",
                      "type": "array"
                    },
                    "entityPorts": {
                      "description": "Restrict the `toEntities` rule to specific ports. Empty means all ports.",
                      "items": {
                        "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.networking.v1.NetworkPolicyPort",
                        "required": []
                      },
                      "required": [],
                      "title": "entityPorts",
                      "type": "array"
                    },
                    "fqdnPorts": {
                      "description": "Ports the `toFQDNs` rule allows. Defaults to TCP/443.",
                      "items": {
                        "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.networking.v1.NetworkPolicyPort",
                        "required": []
                      },
                      "required": [],
                      "title": "fqdnPorts",
                      "type": "array"
                    },
                    "httpRules": {
                      "description": "L7 HTTP rules layered onto the `toFQDNs` rule, e.g.\n`- method: GET` / `path: \"/v1/.*\"`. Turns \"may reach this host\" into \"may make these\nrequests to this host\". Costs a proxy hop per connection.",
                      "items": {
                        "required": []
                      },
                      "required": [],
                      "title": "httpRules",
                      "type": "array"
                    },
                    "toEntities": {
                      "description": "Named destination sets, e.g. `world` for everything outside the cluster, or\n`kube-apiserver`. Not a synonym for the `egress.cidr`/`except` translation: `world` does\nnot carve out the cloud metadata endpoint the way those defaults do.",
                      "items": {
                        "required": [],
                        "type": "string"
                      },
                      "required": [],
                      "title": "toEntities",
                      "type": "array"
                    },
                    "toFQDNs": {
                      "description": "Destinations by name rather than by address, e.g. `- matchName: api.example.com` or\n`- matchPattern: \"*.example.com\"`. This is the rule the CIDR-based `egress.https` was\nalways a poor approximation of: \"may reach the internet on 443\" permits every public host\nthat exists, where this permits the ones the application actually talks to.\n\nEnforced against the addresses Cilium's DNS proxy saw returned for the name, so\n`egress.dns.enabled` must stay on — the render fails if it is not.",
                      "items": {
                        "required": []
                      },
                      "required": [],
                      "title": "toFQDNs",
                      "type": "array"
                    }
                  },
                  "required": [],
                  "title": "egress"
                },
                "enableDefaultDeny": {
                  "default": true,
                  "description": "State default-deny explicitly rather than relying on it being implied by the presence of\nrules. This is what makes the intentional default-deny case — a policy with an empty rule\nlist — actually deny, instead of being treated as no policy at all. Requires Cilium 1.16+.",
                  "required": [],
                  "title": "enableDefaultDeny",
                  "type": "boolean"
                },
                "extraEgress": {
                  "description": "Extra egress rules in CiliumNetworkPolicy form, appended regardless of `egress.enabled`.",
                  "items": {
                    "required": []
                  },
                  "required": [],
                  "title": "extraEgress",
                  "type": "array"
                },
                "extraIngress": {
                  "description": "Extra ingress rules in CiliumNetworkPolicy form, appended regardless of\n`ingress.enabled`.",
                  "items": {
                    "required": []
                  },
                  "required": [],
                  "title": "extraIngress",
                  "type": "array"
                },
                "ingress": {
                  "additionalProperties": true,
                  "description": "Cilium-only ingress rules.",
                  "properties": {
                    "customRules": {
                      "description": "Additional ingress rules in CiliumNetworkPolicy form, appended verbatim.",
                      "items": {
                        "required": []
                      },
                      "required": [],
                      "title": "customRules",
                      "type": "array"
                    },
                    "fromEntities": {
                      "description": "Named source sets, e.g. `cluster`, `host`, `remote-node`, `world`, `kube-apiserver`.\nA named entity stays correct when the cluster is renumbered; a CIDR list does not.",
                      "items": {
                        "required": [],
                        "type": "string"
                      },
                      "required": [],
                      "title": "fromEntities",
                      "type": "array"
                    }
                  },
                  "required": [],
                  "title": "ingress"
                }
              },
              "required": [],
              "title": "cilium"
            },
            "egress": {
              "additionalProperties": true,
              "description": "Egress rules.",
              "properties": {
                "cidr": {
                  "default": "0.0.0.0/0",
                  "description": "Destination CIDR for the HTTP/HTTPS rules.",
                  "required": [],
                  "title": "cidr",
                  "type": "string"
                },
                "customRules": {
                  "description": "Additional egress rules, appended verbatim. Each must carry its own `to:` selector.",
                  "items": {
                    "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.networking.v1.NetworkPolicyEgressRule",
                    "required": []
                  },
                  "required": [],
                  "title": "customRules",
                  "type": "array"
                },
                "dns": {
                  "additionalProperties": true,
                  "description": "Allow DNS resolution.",
                  "properties": {
                    "enabled": {
                      "default": true,
                      "description": "Allow egress to the cluster DNS service.",
                      "required": [],
                      "title": "enabled",
                      "type": "boolean"
                    },
                    "namespaceSelector": {
                      "additionalProperties": true,
                      "description": "Namespace selector for the DNS service.",
                      "properties": {
                        "kubernetes.io/metadata.name": {
                          "default": "kube-system",
                          "required": [],
                          "title": "kubernetes.io/metadata.name",
                          "type": "string"
                        }
                      },
                      "required": [],
                      "title": "namespaceSelector",
                      "type": "object"
                    },
                    "podSelector": {
                      "additionalProperties": true,
                      "description": "Pod selector for the DNS service.",
                      "properties": {
                        "k8s-app": {
                          "default": "kube-dns",
                          "required": [],
                          "title": "k8s-app",
                          "type": "string"
                        }
                      },
                      "required": [],
                      "title": "podSelector",
                      "type": "object"
                    }
                  },
                  "required": [],
                  "title": "dns"
                },
                "enabled": {
                  "default": true,
                  "description": "Add egress rules. Disabled means default-deny for outbound traffic.",
                  "required": [],
                  "title": "enabled",
                  "type": "boolean"
                },
                "except": {
                  "description": "CIDRs carved out of `cidr`. Defaults exclude RFC1918 private space and link-local\n169.254.0.0/16, which covers the cloud instance metadata endpoint.",
                  "items": {
                    "anyOf": [
                      {
                        "required": [],
                        "type": "string"
                      },
                      {
                        "required": [],
                        "type": "string"
                      },
                      {
                        "required": [],
                        "type": "string"
                      },
                      {
                        "required": [],
                        "type": "string"
                      }
                    ],
                    "required": []
                  },
                  "required": [],
                  "title": "except",
                  "type": "array"
                },
                "http": {
                  "additionalProperties": true,
                  "description": "Allow outbound HTTP.",
                  "properties": {
                    "enabled": {
                      "default": false,
                      "description": "Allow egress to TCP/80 on the destinations described by `cidr`/`except`.",
                      "required": [],
                      "title": "enabled",
                      "type": "boolean"
                    }
                  },
                  "required": [],
                  "title": "http"
                },
                "https": {
                  "additionalProperties": true,
                  "description": "Allow outbound HTTPS.",
                  "properties": {
                    "enabled": {
                      "default": true,
                      "description": "Allow egress to TCP/443 on the destinations described by `cidr`/`except`.",
                      "required": [],
                      "title": "enabled",
                      "type": "boolean"
                    }
                  },
                  "required": [],
                  "title": "https"
                }
              },
              "required": [],
              "title": "egress"
            },
            "enabled": {
              "default": false,
              "description": "Create the NetworkPolicies. When enabled with no rules configured, the result is a\ndefault-deny policy, which is intentional.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "engine": {
              "default": "kubernetes",
              "description": "Which policy dialect to render. `kubernetes` emits the portable\n`networking.k8s.io/v1` pair; `cilium` emits `CiliumNetworkPolicy`, which can express FQDN\ndestinations, named entities and L7 rules that the portable API cannot; `both` emits both,\nfor the window in which a cluster is migrating between CNIs.\n\nThe engine picks the dialect, not the rules — every value below is translated either way, so\nswitching is a one-line change. `both` is additive rather than stricter: policies selecting\none pod union their allowances, so it is a migration setting and not a hardening one.",
              "enum": [
                "kubernetes",
                "cilium",
                "both"
              ],
              "required": [],
              "title": "engine"
            },
            "extraEgress": {
              "description": "Extra egress rules appended regardless of `egress.enabled`.",
              "items": {
                "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.networking.v1.NetworkPolicyEgressRule",
                "required": []
              },
              "required": [],
              "title": "extraEgress",
              "type": "array"
            },
            "extraIngress": {
              "description": "Extra ingress rules appended regardless of `ingress.enabled`.",
              "items": {
                "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.networking.v1.NetworkPolicyIngressRule",
                "required": []
              },
              "required": [],
              "title": "extraIngress",
              "type": "array"
            },
            "ingress": {
              "additionalProperties": true,
              "description": "Ingress rules.",
              "properties": {
                "controller": {
                  "additionalProperties": true,
                  "description": "Allow traffic from the ingress controller.",
                  "properties": {
                    "enabled": {
                      "default": true,
                      "description": "Allow ingress from the ingress controller.",
                      "required": [],
                      "title": "enabled",
                      "type": "boolean"
                    },
                    "namespace": {
                      "default": "traefik",
                      "description": "Namespace the ingress controller runs in.",
                      "required": [],
                      "title": "namespace",
                      "type": "string"
                    },
                    "ports": {
                      "description": "Restrict the rule to specific ports. Empty means all ports.",
                      "items": {
                        "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.networking.v1.NetworkPolicyPort",
                        "required": []
                      },
                      "required": [],
                      "title": "ports",
                      "type": "array"
                    },
                    "selector": {
                      "additionalProperties": true,
                      "description": "Pod selector matching the ingress controller.",
                      "properties": {
                        "app.kubernetes.io/name": {
                          "default": "traefik",
                          "required": [],
                          "title": "app.kubernetes.io/name",
                          "type": "string"
                        }
                      },
                      "required": [],
                      "title": "selector",
                      "type": "object"
                    }
                  },
                  "required": [],
                  "title": "controller"
                },
                "customRules": {
                  "description": "Additional ingress rules, appended verbatim.",
                  "items": {
                    "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.networking.v1.NetworkPolicyIngressRule",
                    "required": []
                  },
                  "required": [],
                  "title": "customRules",
                  "type": "array"
                },
                "enabled": {
                  "default": true,
                  "description": "Add ingress rules. Disabled means default-deny for inbound traffic.",
                  "required": [],
                  "title": "enabled",
                  "type": "boolean"
                },
                "gateway": {
                  "additionalProperties": true,
                  "description": "Allow traffic from the Gateway API data plane. Only rendered when `gateway.enabled` is\nalso set, so it costs nothing on a chart exposed through an Ingress.\n\nNeeds no configuration in the common case: the Gateway that must be admitted is by\ndefinition the one `gateway.parentRefs` names, so both fields below are derived from it.\nRestating the Gateway's identity here would be a second place to edit on a rename, and a\npolicy that names the wrong Gateway looks correct and blocks all inbound traffic.",
                  "properties": {
                    "enabled": {
                      "default": true,
                      "description": "Allow ingress from the Gateway's data plane.",
                      "required": [],
                      "title": "enabled",
                      "type": "boolean"
                    },
                    "namespace": {
                      "default": "",
                      "description": "Namespace the data plane runs in. Empty derives it from `gateway.parentRefs`.",
                      "required": [],
                      "title": "namespace",
                      "type": "string"
                    },
                    "ports": {
                      "description": "Restrict the rule to specific ports. Empty means all ports.",
                      "items": {
                        "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.networking.v1.NetworkPolicyPort",
                        "required": []
                      },
                      "required": [],
                      "title": "ports",
                      "type": "array"
                    },
                    "selector": {
                      "additionalProperties": true,
                      "description": "Pod selector matching the data plane. Empty derives\n`gateway.networking.k8s.io/gateway-name: \u003cparentRef\u003e`, the label Cilium, Envoy Gateway,\nIstio and NGINX Gateway Fabric all put on the pods they provision for a Gateway.",
                      "required": [],
                      "title": "selector",
                      "type": "object"
                    }
                  },
                  "required": [],
                  "title": "gateway"
                },
                "monitoring": {
                  "additionalProperties": true,
                  "description": "Allow scraping from a monitoring namespace.",
                  "properties": {
                    "enabled": {
                      "default": true,
                      "description": "Allow ingress from the monitoring namespace.",
                      "required": [],
                      "title": "enabled",
                      "type": "boolean"
                    },
                    "namespace": {
                      "default": "monitoring",
                      "description": "Monitoring namespace, matched on `kubernetes.io/metadata.name`.",
                      "required": [],
                      "title": "namespace",
                      "type": "string"
                    },
                    "namespaceSelector": {
                      "additionalProperties": true,
                      "description": "Override the namespace selector entirely.",
                      "required": [],
                      "title": "namespaceSelector",
                      "type": "object"
                    },
                    "ports": {
                      "description": "Restrict the rule to specific ports. Empty means all ports.",
                      "items": {
                        "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.networking.v1.NetworkPolicyPort",
                        "required": []
                      },
                      "required": [],
                      "title": "ports",
                      "type": "array"
                    }
                  },
                  "required": [],
                  "title": "monitoring"
                }
              },
              "required": [],
              "title": "ingress"
            }
          },
          "required": [],
          "title": "networkPolicy"
        },
        "nodeSelector": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Node selector for pod assignment.",
          "required": [],
          "title": "nodeSelector",
          "type": "object"
        },
        "partOf": {
          "default": "",
          "description": "Value for the `app.kubernetes.io/part-of` label.",
          "required": [],
          "title": "partOf",
          "type": "string"
        },
        "podAnnotations": {
          "additionalProperties": true,
          "description": "Annotations added to the pod template.",
          "required": [],
          "title": "podAnnotations",
          "type": "object"
        },
        "podAntiAffinity": {
          "default": "",
          "description": "Shorthand for spreading replicas across nodes. `soft` prefers, `hard` requires.",
          "enum": [
            "",
            "soft",
            "hard"
          ],
          "required": [],
          "title": "podAntiAffinity"
        },
        "podLabels": {
          "additionalProperties": true,
          "description": "Labels added to the pod template.",
          "required": [],
          "title": "podLabels",
          "type": "object"
        },
        "podSecurityContext": {
          "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.PodSecurityContext",
          "required": []
        },
        "podSecurityContextPreset": {
          "default": "restricted",
          "description": "Baseline for the pod security context. `restricted` applies the Pod Security Standards\nrestricted profile (`runAsNonRoot`, `seccompProfile: RuntimeDefault`,\n`fsGroupChangePolicy: OnRootMismatch`); `none` applies nothing. Identity fields\n(`runAsUser`, `runAsGroup`, `fsGroup`) are always left to the chart, since they must match\nthe UID baked into the image.",
          "enum": [
            "restricted",
            "none"
          ],
          "required": [],
          "title": "podSecurityContextPreset"
        },
        "priorityClassName": {
          "default": "",
          "description": "PriorityClass for the pod.",
          "required": [],
          "title": "priorityClassName",
          "type": "string"
        },
        "prometheusRule": {
          "additionalProperties": true,
          "description": "Prometheus recording and alerting rules, consumed by `common.prometheus.rules.*`. Unlike the\ndashboards there is only one carrier — `PrometheusRule` is already the operator's own CRD — and\nno per-object cross-namespace grant: which namespaces a Prometheus loads rules from is decided\nby `ruleNamespaceSelector` and `ruleSelector` on the Prometheus custom resource.",
          "properties": {
            "enabled": {
              "default": false,
              "description": "Create a PrometheusRule from the chart's rule files. Requires the `monitoring.coreos.com/v1`\nCRDs; `common.prometheus.rules.errors` fails the render loudly rather than silently dropping\nthe object when they are absent.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "labels": {
              "additionalProperties": true,
              "description": "Extra labels, templated. This is the only half of rule discovery a chart controls: a\ncluster whose Prometheus selects `release: kube-prometheus-stack` needs that label here, or\nthe rules are created and never loaded.",
              "required": [],
              "title": "labels",
              "type": "object"
            },
            "scope": {
              "additionalProperties": true,
              "description": "Confine the rules to a subset of the cluster's series. A `PrometheusRule` is not scoped to\nthe namespace it lives in, so `up{job=\"api\"} == 0` matches somebody else's `api` job in\nanother namespace and two installs of one chart alert on each other. Rather than parse PromQL\nin a Go template, the rule files mark every selector with an always-true placeholder matcher\nand the library swaps it for a real one — see the note in `_prometheus.tpl`. Consuming charts\nnormally derive `matcher` rather than exposing it raw.",
              "properties": {
                "matcher": {
                  "default": "",
                  "description": "What to swap it for, e.g. `namespace=\"prod\"`. Empty disables the substitution; the\nplaceholder is left in place, which is correct, because it is already a no-op matcher.",
                  "required": [],
                  "title": "matcher",
                  "type": "string"
                },
                "placeholder": {
                  "default": "",
                  "description": "The literal placeholder written into the rule files, e.g. `myapp_scope=~\".*\"`. Empty\ndisables the substitution and the files install exactly as vendored.",
                  "required": [],
                  "title": "placeholder",
                  "type": "string"
                }
              },
              "required": [],
              "title": "scope"
            }
          },
          "required": [],
          "title": "prometheusRule"
        },
        "readinessProbe": {
          "additionalProperties": true,
          "description": "Readiness probe. See `startupProbe` for the accepted shape.",
          "properties": {
            "enabled": {
              "default": false,
              "description": "Enable the readiness probe.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            }
          },
          "required": [],
          "title": "readinessProbe"
        },
        "resources": {
          "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.ResourceRequirements",
          "required": []
        },
        "revisionHistoryLimit": {
          "default": 3,
          "description": "Number of old ReplicaSets to retain for rollback.",
          "required": [],
          "title": "revisionHistoryLimit",
          "type": "integer"
        },
        "securityContext": {
          "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.SecurityContext",
          "required": []
        },
        "securityContextPreset": {
          "default": "restricted",
          "description": "Baseline for the container security context. `restricted` drops all capabilities and\nforbids privilege escalation, a writable root filesystem and running as root.",
          "enum": [
            "restricted",
            "none"
          ],
          "required": [],
          "title": "securityContextPreset"
        },
        "serviceAccount": {
          "additionalProperties": true,
          "description": "ServiceAccount the pods run under. `create: false` with no `name` means the `default` one.",
          "properties": {
            "annotations": {
              "additionalProperties": true,
              "description": "Annotations for the ServiceAccount.",
              "required": [],
              "title": "annotations",
              "type": "object"
            },
            "automountToken": {
              "default": false,
              "description": "Mount the API token into pods that default to this ServiceAccount.",
              "required": [],
              "title": "automountToken",
              "type": "boolean"
            },
            "create": {
              "default": true,
              "description": "Create a dedicated ServiceAccount.",
              "required": [],
              "title": "create",
              "type": "boolean"
            },
            "name": {
              "default": "",
              "description": "Name of the ServiceAccount. Generated when empty.",
              "required": [],
              "title": "name",
              "type": "string"
            }
          },
          "required": [],
          "title": "serviceAccount"
        },
        "startupProbe": {
          "additionalProperties": true,
          "description": "Startup probe. Set `enabled` and exactly one handler (`httpGet`, `tcpSocket`, `exec`\nor `grpc`); unset timing fields fall back to the Kubernetes defaults.",
          "properties": {
            "enabled": {
              "default": false,
              "description": "Enable the startup probe.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            }
          },
          "required": [],
          "title": "startupProbe"
        },
        "strategy": {
          "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.apps.v1.DeploymentStrategy",
          "required": []
        },
        "terminationGracePeriodSeconds": {
          "default": 30,
          "description": "Grace period for pod shutdown.",
          "required": [],
          "title": "terminationGracePeriodSeconds",
          "type": "integer"
        },
        "tolerations": {
          "description": "Tolerations for pod assignment.",
          "items": {
            "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.Toleration",
            "required": []
          },
          "required": [],
          "title": "tolerations",
          "type": "array"
        },
        "topologySpreadConstraints": {
          "description": "Topology spread constraints.",
          "items": {
            "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.TopologySpreadConstraint",
            "required": []
          },
          "required": [],
          "title": "topologySpreadConstraints",
          "type": "array"
        }
      },
      "required": [],
      "title": "common",
      "type": "object"
    },
    "commonAnnotations": {
      "additionalProperties": true,
      "description": "Annotations added to every object this chart creates.",
      "required": [],
      "title": "commonAnnotations",
      "type": "object"
    },
    "commonLabels": {
      "additionalProperties": true,
      "description": "Labels added to every object this chart creates.",
      "required": [],
      "title": "commonLabels",
      "type": "object"
    },
    "config": {
      "additionalProperties": true,
      "description": "Extra configuration, expressed as the TOML tree of\n[the service's README](https://github.com/TimSchoenle/cloudflare-access-webhook-redirect#-configuration)\n(`server.host`, `webhook.target_base`, ...). Merged over everything the chart derives from\nthe values above, so it can both extend and override them. Rendered into the mounted\nConfigMap — never into the environment, which the loader refuses to combine with a file.",
      "required": [],
      "title": "config",
      "type": "object"
    },
    "configExtraToml": {
      "default": "",
      "description": "Verbatim TOML appended after the rendered configuration. The escape hatch for anything the\nchart's TOML renderer cannot express, notably arrays of tables.",
      "required": [],
      "title": "configExtraToml",
      "type": "string"
    },
    "configMount": {
      "additionalProperties": true,
      "description": "Where the rendered configuration and the credential files land in the container, and whether\na change to either rolls the Deployment.",
      "properties": {
        "configDir": {
          "default": "/etc/cloudflare-access-webhook-redirect/config",
          "description": "Directory the rendered `config.toml` is mounted at, passed as `WEBHOOK_REDIRECT_CONFIG`.",
          "required": [],
          "title": "configDir",
          "type": "string"
        },
        "rolloutOnChange": {
          "default": false,
          "description": "Add `checksum/*` pod annotations so a configuration change rolls the Deployment. Off by\ndefault, and deliberately so: the proxy watches the directories its configuration came from\nand rebuilds its client, path patterns, credentials and listener in place when the kubelet\nupdates the mounted ConfigMap or Secret, which is strictly better than a rollout. Turn this\non only if you want configuration changes to behave like an ordinary image bump.\n`telemetry.*` is installed once per process and needs a restart either way.",
          "required": [],
          "title": "rolloutOnChange",
          "type": "boolean"
        },
        "secretsDir": {
          "default": "/etc/cloudflare-access-webhook-redirect/secrets",
          "description": "Directory the credential files are mounted at, passed as\n`WEBHOOK_REDIRECT_SECRETS_DIR`.",
          "required": [],
          "title": "secretsDir",
          "type": "string"
        }
      },
      "required": [],
      "title": "configMount"
    },
    "existingSecret": {
      "default": "",
      "description": "Name of an existing Secret holding this proxy's credentials, which keeps them out of\n`values.yaml` and out of the Helm release object. **Its keys are the configuration paths, not\nfree-form names**: `cloudflare__client_id`, `cloudflare__client_secret` and, once\n`telemetry.sentry.enabled` is set, `telemetry__sentry__dsn` — because the file name is what\nthe loader parses. Set, the chart renders no Secret of its own and `cloudflare.clientId`,\n`cloudflare.clientSecret` and `telemetry.sentry.dsn` are ignored.",
      "required": [],
      "title": "existingSecret",
      "type": "string"
    },
    "extraEnv": {
      "description": "Additional environment variables for the application container.",
      "items": {
        "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.EnvVar",
        "required": []
      },
      "required": [],
      "title": "extraEnv",
      "type": "array"
    },
    "extraVolumeMounts": {
      "description": "Additional volume mounts (e.g., /cache)",
      "items": {
        "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.VolumeMount",
        "required": []
      },
      "required": [],
      "title": "extraVolumeMounts",
      "type": "array"
    },
    "extraVolumes": {
      "description": "Additional volumes (e.g., cache, tmp)",
      "items": {
        "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.Volume",
        "required": []
      },
      "required": [],
      "title": "extraVolumes",
      "type": "array"
    },
    "fullnameOverride": {
      "default": "",
      "description": "Override the full release name",
      "required": [],
      "title": "fullnameOverride",
      "type": "string"
    },
    "gateway": {
      "additionalProperties": true,
      "description": "Gateway API configuration, consumed by `common.gateway.*`. The successor to `ingress`, and\nan independent switch from it: a cluster migrating between an Ingress controller and a Gateway\nimplementation runs both for a while.\n\nThe division of labour is the API's, not this chart's. A `Gateway` — the listeners, the\naddress, the certificates — belongs to the cluster operator; an application owns only the\n`HTTPRoute` that attaches to it. So the default here is route-only, and `create` is for\ninstalls that have no cluster-wide Gateway to attach to.",
      "properties": {
        "addresses": {
          "description": "Addresses requested for the created Gateway, e.g. a fixed `IPAddress`.",
          "items": {
            "required": []
          },
          "required": [],
          "title": "addresses",
          "type": "array"
        },
        "allowedRoutes": {
          "additionalProperties": true,
          "description": "Which routes may attach to the created Gateway's listeners. Defaults to `Same`: a Gateway\nthis chart owns should not be attachable from another namespace unless that is asked for.",
          "required": [],
          "title": "allowedRoutes",
          "type": "object"
        },
        "annotations": {
          "additionalProperties": true,
          "description": "Annotations for the HTTPRoute and the created Gateway. Values may contain Go templates.",
          "required": [],
          "title": "annotations",
          "type": "object"
        },
        "backendRefs": {
          "description": "Backends for rules that name none. Defaults to this chart's own Service. Weights are\nhonoured, so a traffic split needs no custom rule.",
          "items": {
            "required": []
          },
          "required": [],
          "title": "backendRefs",
          "type": "array"
        },
        "create": {
          "default": false,
          "description": "Also create the Gateway itself. Leave off when the cluster already runs one — that is the\nnormal case, and one Gateway per application usually means one load balancer per\napplication. When on, a route that names no parent attaches to it automatically.",
          "required": [],
          "title": "create",
          "type": "boolean"
        },
        "enabled": {
          "default": false,
          "description": "Create the HTTPRoute. Requires the `gateway.networking.k8s.io` CRDs;\n`common.gateway.validate` fails the render loudly rather than silently dropping the route\nwhen they are absent.",
          "required": [],
          "title": "enabled",
          "type": "boolean"
        },
        "filters": {
          "description": "Filters applied to the default rule: `RequestHeaderModifier`, `ResponseHeaderModifier`,\n`RequestRedirect`, `URLRewrite`, `RequestMirror`, `ExtensionRef`. This is where an\nIngress controller's annotations end up, as typed fields. Ignored when `rules` is set.",
          "items": {
            "required": []
          },
          "required": [],
          "title": "filters",
          "type": "array"
        },
        "gatewayClassName": {
          "default": "",
          "description": "GatewayClass that programs the created Gateway, e.g. `cilium`, `istio`, `envoy-gateway`,\n`nginx`. Required by `create`; a Gateway without one is never reconciled. Ignored otherwise.",
          "required": [],
          "title": "gatewayClassName",
          "type": "string"
        },
        "hostnames": {
          "description": "Hostnames the route serves. Values may contain Go templates.\n\nRequired unless `create` is set. A route with no hostnames matches every name its listener\naccepts: on a Gateway this chart owns that is harmless, and sometimes the point — an install\nreached by address has no DNS name to state. On a shared Gateway it means taking over traffic\nmeant for other applications, so the render refuses it.",
          "items": {
            "required": []
          },
          "required": [],
          "title": "hostnames",
          "type": "array"
        },
        "httpPort": {
          "default": 80,
          "description": "Port for the derived HTTP listener.",
          "required": [],
          "title": "httpPort",
          "type": "integer"
        },
        "httpsPort": {
          "default": 443,
          "description": "Port for the derived HTTPS listener.",
          "required": [],
          "title": "httpsPort",
          "type": "integer"
        },
        "httpsRedirect": {
          "additionalProperties": true,
          "description": "A second route that redirects plaintext traffic to HTTPS. Under Ingress this was a\ncontroller-specific annotation; Gateway API expresses it as a typed `RequestRedirect`\nfilter, which means it has to be a real object.",
          "properties": {
            "enabled": {
              "default": false,
              "description": "Create the redirect route.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "port": {
              "default": "null",
              "description": "Port to redirect to. Left unset, the scheme implies it.",
              "required": [],
              "title": "port",
              "type": [
                "integer",
                "null"
              ]
            },
            "sectionName": {
              "default": "",
              "description": "Listener to bind the redirect to. Must be the plaintext one: attached to every listener,\nthe redirect would also apply to the HTTPS listener and loop forever. Defaults to `http`,\nthe name of the listener `create` renders.",
              "required": [],
              "title": "sectionName",
              "type": "string"
            },
            "statusCode": {
              "default": 301,
              "description": "Redirect status code. `301` or `302`.",
              "required": [],
              "title": "statusCode",
              "type": "integer"
            }
          },
          "required": [],
          "title": "httpsRedirect"
        },
        "infrastructure": {
          "additionalProperties": true,
          "description": "`infrastructure.labels` / `infrastructure.annotations` for the created Gateway, passed\nthrough to the load balancer the implementation provisions. Where Cilium's LB-IPAM\nannotations go.",
          "required": [],
          "title": "infrastructure",
          "type": "object"
        },
        "listeners": {
          "description": "Listeners for the created Gateway, replacing the derived ones entirely. Reach for this\nwhen a listener needs its own hostname or certificate.",
          "items": {
            "required": []
          },
          "required": [],
          "title": "listeners",
          "type": "array"
        },
        "parentRefs": {
          "description": "Gateways the route attaches to. Each entry takes `name` and optionally `namespace`,\n`sectionName` (a single listener), `port`, `group` and `kind`; the API's defaults are filled\nin. Values may contain Go templates.\n\nA route that names no parent is accepted by the API server and then does nothing — no\nlistener ever programs it — so this is required unless `create` is set.\nExample:\n```yaml\nparentRefs:\n  - name: shared-gateway\n    namespace: gateway-system\n    sectionName: https\n```",
          "items": {
            "required": []
          },
          "required": [],
          "title": "parentRefs",
          "type": "array"
        },
        "path": {
          "default": "/",
          "description": "Path prefix for the default rule. Ignored when `rules` is set.",
          "required": [],
          "title": "path",
          "type": "string"
        },
        "rules": {
          "description": "Routing rules, in full `HTTPRouteRule` form (`matches`, `filters`, `backendRefs`,\n`timeouts`). An entry that omits `backendRefs` inherits this chart's Service, so a rule that\nonly narrows the path does not have to restate where the traffic goes.\n\nLeft empty, the route gets one rule matching `path` as a prefix — the Gateway API equivalent\nof a single-path Ingress, and a complete configuration together with `hostnames`.",
          "items": {
            "required": []
          },
          "required": [],
          "title": "rules",
          "type": "array"
        },
        "timeouts": {
          "additionalProperties": true,
          "description": "Timeouts for the default rule: `request` and `backendRequest`, as Go durations. Ignored\nwhen `rules` is set.",
          "required": [],
          "title": "timeouts",
          "type": "object"
        },
        "tls": {
          "additionalProperties": true,
          "description": "TLS for the created Gateway's HTTPS listener. Ignored without `create`, and irrelevant\nwhen attaching to somebody else's Gateway — the certificate is theirs.",
          "properties": {
            "certificateRefs": {
              "description": "Secrets holding the certificate. Required by `Terminate`: unlike an Ingress there is no\nconvention by which one is looked up from the hostname. A ref naming another namespace\nadditionally needs a `ReferenceGrant` there, which this chart deliberately does not create\n— a grant is the target namespace owner's to give.",
              "items": {
                "required": []
              },
              "required": [],
              "title": "certificateRefs",
              "type": "array"
            },
            "enabled": {
              "default": false,
              "description": "Add an HTTPS listener.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "mode": {
              "default": "Terminate",
              "description": "TLS mode.",
              "enum": [
                "Terminate",
                "Passthrough"
              ],
              "required": [],
              "title": "mode"
            },
            "options": {
              "additionalProperties": true,
              "description": "Implementation-specific TLS options.",
              "required": [],
              "title": "options",
              "type": "object"
            }
          },
          "required": [],
          "title": "tls"
        }
      },
      "required": [],
      "title": "gateway"
    },
    "global": {
      "description": "Global values are values that can be accessed from any chart or subchart by exactly the same name.",
      "required": [],
      "title": "global",
      "type": "object"
    },
    "image": {
      "additionalProperties": true,
      "description": "Container image the pod runs, composed as `registry/repository:tag`.",
      "properties": {
        "pullPolicy": {
          "default": "",
          "description": "Image pull policy. Empty resolves automatically from the tag/digest.",
          "enum": [
            "",
            "Always",
            "IfNotPresent",
            "Never"
          ],
          "required": [],
          "title": "pullPolicy"
        },
        "registry": {
          "default": "",
          "description": "Registry host. Empty means Docker Hub.",
          "required": [],
          "title": "registry",
          "type": "string"
        },
        "repository": {
          "default": "timmi6790/cloudflare-access-webhook-redirect",
          "description": "Container image repository (e.g. docker.io/user/image)",
          "required": [],
          "title": "repository",
          "type": "string"
        },
        "tag": {
          "default": "v1.2.1@sha256:39a93296a96f65176a8c1f06e9ee8748d82d438ff2a285fcceac5a4382e564a2",
          "description": "The container image tag, pinned by digest (`vX.Y.Z\npull, while the tag stays on as the readable version marker. Defaults to the chart's\n`appVersion` when empty.",
          "required": [],
          "title": "tag",
          "type": "string"
        }
      },
      "required": [],
      "title": "image"
    },
    "imagePullSecrets": {
      "description": "Optional image pull secrets for private registries",
      "items": {
        "required": []
      },
      "required": [],
      "title": "imagePullSecrets",
      "type": "array"
    },
    "ingress": {
      "additionalProperties": true,
      "description": "The Ingress in front of the Service. An independent switch from `gateway`, so a cluster\nmigrating from an Ingress controller to a Gateway implementation can run both.",
      "properties": {
        "annotations": {
          "additionalProperties": true,
          "description": "Additional ingress annotations\nExample:\n  cert-manager.io/cluster-issuer: letsencrypt-prod\n  nginx.ingress.kubernetes.io/rate-limit: \"100\"",
          "required": [],
          "title": "annotations",
          "type": "object"
        },
        "enabled": {
          "default": false,
          "description": "Enable ingress resource",
          "required": [],
          "title": "enabled",
          "type": "boolean"
        },
        "hosts": {
          "description": "Host definitions for ingress\nExample:\n  - host: example.local\n    paths:\n      - path: /\n        pathType: Prefix",
          "items": {
            "required": []
          },
          "required": [],
          "title": "hosts",
          "type": "array"
        },
        "ingressClassName": {
          "default": "nginx",
          "description": "Ingress class name (e.g. nginx)",
          "required": [],
          "title": "ingressClassName",
          "type": "string"
        },
        "tls": {
          "description": "TLS configuration for ingress\nExample:\n  - secretName: example-tls\n    hosts:\n      - example.local",
          "items": {
            "required": []
          },
          "required": [],
          "title": "tls",
          "type": "array"
        }
      },
      "required": [],
      "title": "ingress"
    },
    "kubeVersionOverride": {
      "default": "",
      "description": "Kubernetes version to target when branching on API availability.\nLets `helm template` render for a specific cluster version without a live connection.",
      "required": [],
      "title": "kubeVersionOverride",
      "type": "string"
    },
    "livenessProbe": {
      "additionalProperties": true,
      "description": "Liveness probe, whose failure restarts the container. `successThreshold` is dropped from the\nrendered probe, because the API server accepts nothing but 1 there.",
      "properties": {
        "enabled": {
          "default": true,
          "description": "Enable liveness probe",
          "required": [],
          "title": "enabled",
          "type": "boolean"
        },
        "failureThreshold": {
          "default": 3,
          "description": "Failure threshold",
          "required": [],
          "title": "failureThreshold",
          "type": "integer"
        },
        "httpGet": {
          "additionalProperties": true,
          "description": "The probe handler, in the same four forms `startupProbe.httpGet` accepts.",
          "properties": {
            "path": {
              "default": "/health",
              "description": "Health check path",
              "required": [],
              "title": "path",
              "type": "string"
            },
            "port": {
              "default": "http",
              "description": "Health check port",
              "required": [],
              "title": "port",
              "type": "string"
            }
          },
          "required": [],
          "title": "httpGet"
        },
        "initialDelaySeconds": {
          "default": 10,
          "description": "Initial delay before probe starts",
          "required": [],
          "title": "initialDelaySeconds",
          "type": "integer"
        },
        "periodSeconds": {
          "default": 10,
          "description": "Probe frequency",
          "required": [],
          "title": "periodSeconds",
          "type": "integer"
        },
        "timeoutSeconds": {
          "default": 5,
          "description": "Probe timeout",
          "required": [],
          "title": "timeoutSeconds",
          "type": "integer"
        }
      },
      "required": [],
      "title": "livenessProbe"
    },
    "nameOverride": {
      "default": "",
      "description": "Override the chart name",
      "required": [],
      "title": "nameOverride",
      "type": "string"
    },
    "namespaceOverride": {
      "default": "",
      "description": "Deploy into a namespace other than the release namespace.",
      "required": [],
      "title": "namespaceOverride",
      "type": "string"
    },
    "networkPolicy": {
      "additionalProperties": true,
      "description": "Network policy configuration",
      "properties": {
        "cilium": {
          "additionalProperties": true,
          "description": "Cilium-only additions, used when `engine` is `cilium` or `both`. Everything above is\ntranslated into the CiliumNetworkPolicy automatically; these are the rules the portable API\nhas no way to express.\n\n`extraIngress`, `extraEgress` and the per-section `customRules` above are *not* carried over:\nthose are verbatim `networking.k8s.io/v1` rule objects and are not valid CNP. The fields\nbelow are their counterparts.",
          "properties": {
            "description": {
              "default": "",
              "description": "`spec.description`, which Cilium surfaces in `cilium policy get` and in Hubble flow\nverdicts. The one place to record why a rule exists where an operator debugging a drop\nwill actually see it.",
              "required": [],
              "title": "description",
              "type": "string"
            },
            "egress": {
              "additionalProperties": true,
              "description": "Cilium-only egress rules.",
              "properties": {
                "customRules": {
                  "description": "Additional egress rules in CiliumNetworkPolicy form, appended verbatim.",
                  "items": {
                    "required": []
                  },
                  "required": [],
                  "title": "customRules",
                  "type": "array"
                },
                "dnsMatchPatterns": {
                  "description": "What the DNS proxy may resolve, e.g. `- matchPattern: \"*.example.com\"`. Defaults to\neverything, which only permits the lookup — an answer is still only reachable if some\nrule allows the address.",
                  "items": {
                    "required": []
                  },
                  "required": [],
                  "title": "dnsMatchPatterns",
                  "type": "array"
                },
                "entityPorts": {
                  "description": "Restrict the `toEntities` rule to specific ports. Empty means all ports.",
                  "items": {
                    "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.networking.v1.NetworkPolicyPort",
                    "required": []
                  },
                  "required": [],
                  "title": "entityPorts",
                  "type": "array"
                },
                "fqdnPorts": {
                  "description": "Ports the `toFQDNs` rule allows. Defaults to TCP/443.",
                  "items": {
                    "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.networking.v1.NetworkPolicyPort",
                    "required": []
                  },
                  "required": [],
                  "title": "fqdnPorts",
                  "type": "array"
                },
                "httpRules": {
                  "description": "L7 HTTP rules layered onto the `toFQDNs` rule, e.g. `- method: GET` / `path: \"/v1/.*\"`.\nTurns \"may reach this host\" into \"may make these requests to this host\". Costs a proxy\nhop per connection.",
                  "items": {
                    "required": []
                  },
                  "required": [],
                  "title": "httpRules",
                  "type": "array"
                },
                "toEntities": {
                  "description": "Named destination sets, e.g. `world` for everything outside the cluster, or\n`kube-apiserver`. Not a synonym for the `egress.cidr`/`except` translation: `world` does\nnot carve out the cloud metadata endpoint the way those defaults do.",
                  "items": {
                    "required": [],
                    "type": "string"
                  },
                  "required": [],
                  "title": "toEntities",
                  "type": "array"
                },
                "toFQDNs": {
                  "description": "Destinations by name rather than by address, e.g. `- matchName: api.example.com` or\n`- matchPattern: \"*.example.com\"`. This is the rule the CIDR-based `egress.https` was\nalways a poor approximation of: \"may reach the internet on 443\" permits every public\nhost that exists, where this permits the ones the application actually talks to.\n\nEnforced against the addresses Cilium's DNS proxy saw returned for the name, so\n`egress.dns.enabled` must stay on — the render fails if it is not.",
                  "items": {
                    "required": []
                  },
                  "required": [],
                  "title": "toFQDNs",
                  "type": "array"
                }
              },
              "required": [],
              "title": "egress"
            },
            "enableDefaultDeny": {
              "default": true,
              "description": "State default-deny explicitly rather than relying on it being implied by the presence\nof rules. This is what makes the intentional default-deny case — a policy with an empty\nrule list — actually deny, instead of being treated as no policy at all. Cilium 1.16+.",
              "required": [],
              "title": "enableDefaultDeny",
              "type": "boolean"
            },
            "extraEgress": {
              "description": "Extra egress rules in CiliumNetworkPolicy form, appended regardless of `egress.enabled`.",
              "items": {
                "required": []
              },
              "required": [],
              "title": "extraEgress",
              "type": "array"
            },
            "extraIngress": {
              "description": "Extra ingress rules in CiliumNetworkPolicy form, appended regardless of\n`ingress.enabled`.",
              "items": {
                "required": []
              },
              "required": [],
              "title": "extraIngress",
              "type": "array"
            },
            "ingress": {
              "additionalProperties": true,
              "description": "Cilium-only ingress rules.",
              "properties": {
                "customRules": {
                  "description": "Additional ingress rules in CiliumNetworkPolicy form, appended verbatim.",
                  "items": {
                    "required": []
                  },
                  "required": [],
                  "title": "customRules",
                  "type": "array"
                },
                "fromEntities": {
                  "description": "Named source sets, e.g. `cluster`, `host`, `remote-node`, `world`, `kube-apiserver`.\nA named entity stays correct when the cluster is renumbered; a CIDR list does not.",
                  "items": {
                    "required": [],
                    "type": "string"
                  },
                  "required": [],
                  "title": "fromEntities",
                  "type": "array"
                }
              },
              "required": [],
              "title": "ingress"
            }
          },
          "required": [],
          "title": "cilium"
        },
        "egress": {
          "additionalProperties": true,
          "description": "Egress configuration",
          "properties": {
            "cidr": {
              "default": "0.0.0.0/0",
              "description": "Destination CIDR for the HTTP/HTTPS rules",
              "required": [],
              "title": "cidr",
              "type": "string"
            },
            "customRules": {
              "description": "Example: Allow access to a generic app\n- ports:\n    - port: 8080\n      protocol: TCP\n  to:\n    - podSelector:\n        matchLabels:\n          app: my-generic-app\nCustom egress rules",
              "items": {
                "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.networking.v1.NetworkPolicyEgressRule",
                "required": []
              },
              "required": [],
              "title": "customRules",
              "type": "array"
            },
            "dns": {
              "additionalProperties": true,
              "description": "DNS configuration for egress",
              "properties": {
                "enabled": {
                  "default": true,
                  "description": "Allow egress to the cluster DNS service",
                  "required": [],
                  "title": "enabled",
                  "type": "boolean"
                },
                "namespaceSelector": {
                  "additionalProperties": true,
                  "description": "Namespace selector for the DNS service",
                  "properties": {
                    "kubernetes.io/metadata.name": {
                      "default": "kube-system",
                      "required": [],
                      "title": "kubernetes.io/metadata.name",
                      "type": "string"
                    }
                  },
                  "required": [
                    "kubernetes.io/metadata.name"
                  ],
                  "title": "namespaceSelector",
                  "type": "object"
                },
                "podSelector": {
                  "additionalProperties": true,
                  "description": "Pod selector for the DNS service",
                  "properties": {
                    "k8s-app": {
                      "default": "kube-dns",
                      "required": [],
                      "title": "k8s-app",
                      "type": "string"
                    }
                  },
                  "required": [
                    "k8s-app"
                  ],
                  "title": "podSelector",
                  "type": "object"
                }
              },
              "required": [],
              "title": "dns"
            },
            "enabled": {
              "default": true,
              "description": "Enable egress rules",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "except": {
              "description": "CIDRs carved out of `cidr`. Defaults exclude RFC1918 private space and link-local\n169.254.0.0/16, which covers the cloud instance metadata endpoint.",
              "items": {
                "anyOf": [
                  {
                    "required": [],
                    "type": "string"
                  },
                  {
                    "required": [],
                    "type": "string"
                  },
                  {
                    "required": [],
                    "type": "string"
                  },
                  {
                    "required": [],
                    "type": "string"
                  }
                ],
                "required": []
              },
              "required": [],
              "title": "except",
              "type": "array"
            },
            "http": {
              "additionalProperties": true,
              "description": "HTTP configuration for egress",
              "properties": {
                "enabled": {
                  "default": false,
                  "description": "Allow egress to TCP/80 on the destinations described by `cidr`/`except`",
                  "required": [],
                  "title": "enabled",
                  "type": "boolean"
                }
              },
              "required": [],
              "title": "http"
            },
            "https": {
              "additionalProperties": true,
              "description": "HTTPS configuration for egress.\nAlso covers Sentry, which replaced the former dedicated `sentry` flag: that flag\nemitted a rule byte-identical to this one.",
              "properties": {
                "enabled": {
                  "default": true,
                  "description": "Allow egress to TCP/443 on the destinations described by `cidr`/`except`",
                  "required": [],
                  "title": "enabled",
                  "type": "boolean"
                }
              },
              "required": [],
              "title": "https"
            }
          },
          "required": [],
          "title": "egress"
        },
        "enabled": {
          "default": false,
          "description": "Enable network policies",
          "required": [],
          "title": "enabled",
          "type": "boolean"
        },
        "engine": {
          "default": "kubernetes",
          "description": "Which policy dialect to render. `kubernetes` emits the portable `networking.k8s.io/v1`\npair; `cilium` emits `CiliumNetworkPolicy`, which can express FQDN destinations, named\nentities and L7 rules that the portable API cannot; `both` emits both, for the window in\nwhich a cluster is migrating between CNIs.\n\nThe engine picks the dialect, not the rules: every value below is translated either way.",
          "enum": [
            "kubernetes",
            "cilium",
            "both"
          ],
          "required": [],
          "title": "engine"
        },
        "extraEgress": {
          "description": "Extra egress rules appended regardless of `egress.enabled`.",
          "items": {
            "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.networking.v1.NetworkPolicyEgressRule",
            "required": []
          },
          "required": [],
          "title": "extraEgress",
          "type": "array"
        },
        "extraIngress": {
          "description": "Extra ingress rules appended regardless of `ingress.enabled`.",
          "items": {
            "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.networking.v1.NetworkPolicyIngressRule",
            "required": []
          },
          "required": [],
          "title": "extraIngress",
          "type": "array"
        },
        "ingress": {
          "additionalProperties": true,
          "description": "Ingress configuration",
          "properties": {
            "controller": {
              "additionalProperties": true,
              "description": "Ingress Controller configuration",
              "properties": {
                "enabled": {
                  "default": true,
                  "description": "Allow ingress from Ingress Controller",
                  "required": [],
                  "title": "enabled",
                  "type": "boolean"
                },
                "namespace": {
                  "default": "traefik",
                  "description": "Namespace where Ingress Controller is running (default: traefik)",
                  "required": [],
                  "title": "namespace",
                  "type": "string"
                },
                "ports": {
                  "description": "Restrict the rule to specific ports. Empty means all ports.",
                  "items": {
                    "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.networking.v1.NetworkPolicyPort",
                    "required": []
                  },
                  "required": [],
                  "title": "ports",
                  "type": "array"
                },
                "selector": {
                  "additionalProperties": true,
                  "description": "Pod selector for Ingress Controller (default: Traefik label)",
                  "properties": {
                    "app.kubernetes.io/name": {
                      "default": "traefik",
                      "required": [],
                      "title": "app.kubernetes.io/name",
                      "type": "string"
                    }
                  },
                  "required": [
                    "app.kubernetes.io/name"
                  ],
                  "title": "selector",
                  "type": "object"
                }
              },
              "required": [],
              "title": "controller"
            },
            "customRules": {
              "description": "Custom ingress rules",
              "items": {
                "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.networking.v1.NetworkPolicyIngressRule",
                "required": []
              },
              "required": [],
              "title": "customRules",
              "type": "array"
            },
            "enabled": {
              "default": true,
              "description": "Enable ingress rules",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "gateway": {
              "additionalProperties": true,
              "description": "Allow traffic from the Gateway API data plane. Only rendered when `gateway.enabled` is\nalso set, so it costs nothing on a chart exposed through an Ingress.\n\nNeeds no configuration in the common case: the Gateway that must be admitted is by\ndefinition the one `gateway.parentRefs` names, so both fields below are derived from it.",
              "properties": {
                "enabled": {
                  "default": true,
                  "description": "Allow ingress from the Gateway's data plane.",
                  "required": [],
                  "title": "enabled",
                  "type": "boolean"
                },
                "namespace": {
                  "default": "",
                  "description": "Namespace the data plane runs in. Empty derives it from `gateway.parentRefs`.",
                  "required": [],
                  "title": "namespace",
                  "type": "string"
                },
                "ports": {
                  "description": "Restrict the rule to specific ports. Empty means all ports.",
                  "items": {
                    "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.networking.v1.NetworkPolicyPort",
                    "required": []
                  },
                  "required": [],
                  "title": "ports",
                  "type": "array"
                },
                "selector": {
                  "additionalProperties": true,
                  "description": "Pod selector matching the data plane. Empty derives\n`gateway.networking.k8s.io/gateway-name: \u003cparentRef\u003e`, the label Cilium, Envoy Gateway,\nIstio and NGINX Gateway Fabric all put on the pods they provision for a Gateway.",
                  "required": [],
                  "title": "selector",
                  "type": "object"
                }
              },
              "required": [],
              "title": "gateway"
            },
            "monitoring": {
              "additionalProperties": true,
              "description": "Monitoring configuration for ingress",
              "properties": {
                "enabled": {
                  "default": true,
                  "description": "Allow ingress from monitoring namespace",
                  "required": [],
                  "title": "enabled",
                  "type": "boolean"
                },
                "namespace": {
                  "default": "monitoring",
                  "description": "Namespace where monitoring tools are running",
                  "required": [],
                  "title": "namespace",
                  "type": "string"
                },
                "namespaceSelector": {
                  "additionalProperties": true,
                  "description": "Namespace selector matching the monitoring namespace, replacing `namespace` when set.\nFor a Prometheus labelled rather than named, or one of several namespaces that scrape.",
                  "required": [],
                  "title": "namespaceSelector",
                  "type": "object"
                },
                "ports": {
                  "description": "Restrict the rule to specific ports. Empty means all ports.",
                  "items": {
                    "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.networking.v1.NetworkPolicyPort",
                    "required": []
                  },
                  "required": [],
                  "title": "ports",
                  "type": "array"
                }
              },
              "required": [],
              "title": "monitoring"
            }
          },
          "required": [],
          "title": "ingress"
        }
      },
      "required": [],
      "title": "networkPolicy"
    },
    "nodeSelector": {
      "additionalProperties": {
        "type": "string"
      },
      "description": "Node selector labels for scheduling",
      "required": [],
      "title": "nodeSelector",
      "type": "object"
    },
    "podAnnotations": {
      "additionalProperties": true,
      "description": "Additional annotations for the Pod metadata",
      "required": [],
      "title": "podAnnotations",
      "type": "object"
    },
    "podAntiAffinity": {
      "default": "",
      "description": "Shorthand for spreading replicas across nodes. `soft` prefers, `hard` requires.\nIgnored when `affinity` is set.",
      "enum": [
        "",
        "soft",
        "hard"
      ],
      "required": [],
      "title": "podAntiAffinity"
    },
    "podDisruptionBudget": {
      "additionalProperties": true,
      "description": "PodDisruptionBudget for the pods. `minAvailable` and `maxUnavailable` both default to 1 and\nthe API server refuses a budget carrying both, so set one of them to `null`.",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Enable PodDisruptionBudget",
          "required": [],
          "title": "enabled",
          "type": "boolean"
        },
        "maxUnavailable": {
          "default": 1,
          "description": "Maximum unavailable pods",
          "required": [],
          "title": "maxUnavailable",
          "type": [
            "integer",
            "null"
          ]
        },
        "minAvailable": {
          "default": 1,
          "description": "Minimum available pods",
          "required": [],
          "title": "minAvailable",
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [],
      "title": "podDisruptionBudget"
    },
    "podLabels": {
      "additionalProperties": true,
      "description": "Additional labels for the Pod metadata",
      "required": [],
      "title": "podLabels",
      "type": "object"
    },
    "podSecurityContext": {
      "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.PodSecurityContext",
      "required": []
    },
    "podSecurityContextPreset": {
      "default": "restricted",
      "description": "Pod security context baseline. `restricted` applies the Pod Security Standards\nrestricted profile (`runAsNonRoot`, `seccompProfile: RuntimeDefault`,\n`fsGroupChangePolicy: OnRootMismatch`) on top of the identity fields below.",
      "enum": [
        "restricted",
        "none"
      ],
      "required": [],
      "title": "podSecurityContextPreset"
    },
    "priorityClassName": {
      "default": "",
      "description": "Optional Kubernetes PriorityClass name",
      "required": [],
      "title": "priorityClassName",
      "type": "string"
    },
    "readinessProbe": {
      "additionalProperties": true,
      "description": "Readiness probe. While it fails the pod leaves the Service endpoints and keeps running.",
      "properties": {
        "enabled": {
          "default": true,
          "description": "Enable readiness probe",
          "required": [],
          "title": "enabled",
          "type": "boolean"
        },
        "failureThreshold": {
          "default": 3,
          "description": "Failure threshold",
          "required": [],
          "title": "failureThreshold",
          "type": "integer"
        },
        "httpGet": {
          "additionalProperties": true,
          "description": "The probe handler, in the same four forms `startupProbe.httpGet` accepts.",
          "properties": {
            "path": {
              "default": "/health",
              "description": "Health check path",
              "required": [],
              "title": "path",
              "type": "string"
            },
            "port": {
              "default": "http",
              "description": "Health check port",
              "required": [],
              "title": "port",
              "type": "string"
            }
          },
          "required": [],
          "title": "httpGet"
        },
        "initialDelaySeconds": {
          "default": 5,
          "description": "Initial delay before probe starts",
          "required": [],
          "title": "initialDelaySeconds",
          "type": "integer"
        },
        "periodSeconds": {
          "default": 5,
          "description": "Probe frequency",
          "required": [],
          "title": "periodSeconds",
          "type": "integer"
        },
        "timeoutSeconds": {
          "default": 3,
          "description": "Probe timeout",
          "required": [],
          "title": "timeoutSeconds",
          "type": "integer"
        }
      },
      "required": [],
      "title": "readinessProbe"
    },
    "replicaCount": {
      "default": 1,
      "description": "Number of replicas to deploy",
      "minimum": 1,
      "required": [],
      "title": "replicaCount",
      "type": "integer"
    },
    "resources": {
      "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.ResourceRequirements",
      "required": []
    },
    "revisionHistoryLimit": {
      "default": 3,
      "description": "Number of old ReplicaSets retained for rollback.",
      "required": [],
      "title": "revisionHistoryLimit",
      "type": "integer"
    },
    "securityContext": {
      "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.SecurityContext",
      "required": []
    },
    "securityContextPreset": {
      "default": "restricted",
      "description": "Container security context baseline. `restricted` drops all Linux capabilities and\nforbids privilege escalation, running as root and a writable root filesystem.",
      "enum": [
        "restricted",
        "none"
      ],
      "required": [],
      "title": "securityContextPreset"
    },
    "server": {
      "additionalProperties": true,
      "description": "The listener the proxy binds. Rendered into the mounted `config.toml` under `server`, never\ninto the environment.",
      "properties": {
        "host": {
          "default": "0.0.0.0",
          "description": "Bind address (`server.host`). The application's own default is `127.0.0.1`, which in a\ncontainer answers nothing; `0.0.0.0` is what makes the Service reach it.",
          "required": [],
          "title": "host",
          "type": "string"
        },
        "port": {
          "default": 8080,
          "description": "Bind port (`server.port`). Also the container port, the Service target and what every\nprobe and NetworkPolicy rule is written against.",
          "maximum": 65535,
          "minimum": 1,
          "required": [],
          "title": "port",
          "type": "integer"
        }
      },
      "required": [],
      "title": "server"
    },
    "service": {
      "additionalProperties": true,
      "description": "The Service in front of the pods. `port` is what the Ingress and the HTTPRoute route to; the\ncontainer port comes from `server.port`.",
      "properties": {
        "annotations": {
          "additionalProperties": true,
          "description": "Additional service annotations",
          "required": [],
          "title": "annotations",
          "type": "object"
        },
        "port": {
          "default": 80,
          "description": "Service port",
          "required": [],
          "title": "port",
          "type": "integer"
        },
        "type": {
          "default": "ClusterIP",
          "description": "Kubernetes service type",
          "enum": [
            "ClusterIP",
            "NodePort",
            "LoadBalancer"
          ],
          "required": [],
          "title": "type"
        }
      },
      "required": [],
      "title": "service"
    },
    "serviceAccount": {
      "additionalProperties": true,
      "description": "ServiceAccount the pods run under. `create: false` with no `name` means the `default` one.",
      "properties": {
        "annotations": {
          "additionalProperties": true,
          "description": "Additional annotations for the service account",
          "required": [],
          "title": "annotations",
          "type": "object"
        },
        "automountToken": {
          "default": false,
          "description": "Whether to automount the service account token",
          "required": [],
          "title": "automountToken",
          "type": "boolean"
        },
        "create": {
          "default": true,
          "description": "Whether to create a dedicated service account",
          "required": [],
          "title": "create",
          "type": "boolean"
        },
        "name": {
          "default": "",
          "description": "Custom service account name (auto-generated if empty)",
          "required": [],
          "title": "name",
          "type": "string"
        }
      },
      "required": [],
      "title": "serviceAccount"
    },
    "startupProbe": {
      "additionalProperties": true,
      "description": "Startup probe, which holds the other two off until it passes. `failureThreshold` times\n`periodSeconds` is the budget before the kubelet restarts the container: 150 seconds here.",
      "properties": {
        "enabled": {
          "default": true,
          "description": "Enable startup probe",
          "required": [],
          "title": "enabled",
          "type": "boolean"
        },
        "failureThreshold": {
          "default": 30,
          "description": "Failure threshold",
          "required": [],
          "title": "failureThreshold",
          "type": "integer"
        },
        "httpGet": {
          "additionalProperties": true,
          "description": "The probe handler. `tcpSocket`, `exec` and `grpc` are accepted in its place; an enabled\nprobe with no handler at all fails the render.",
          "properties": {
            "path": {
              "default": "/health",
              "description": "Health check path",
              "required": [],
              "title": "path",
              "type": "string"
            },
            "port": {
              "default": "http",
              "description": "Health check port",
              "required": [],
              "title": "port",
              "type": "string"
            }
          },
          "required": [],
          "title": "httpGet"
        },
        "initialDelaySeconds": {
          "default": 2,
          "description": "Initial delay before probe starts",
          "required": [],
          "title": "initialDelaySeconds",
          "type": "integer"
        },
        "periodSeconds": {
          "default": 5,
          "description": "Probe frequency",
          "required": [],
          "title": "periodSeconds",
          "type": "integer"
        },
        "successThreshold": {
          "default": 1,
          "description": "Success threshold",
          "required": [],
          "title": "successThreshold",
          "type": "integer"
        },
        "timeoutSeconds": {
          "default": 3,
          "description": "Probe timeout",
          "required": [],
          "title": "timeoutSeconds",
          "type": "integer"
        }
      },
      "required": [],
      "title": "startupProbe"
    },
    "strategy": {
      "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.apps.v1.DeploymentStrategy",
      "required": []
    },
    "telemetry": {
      "additionalProperties": true,
      "description": "Logging and error reporting. Installed once when the process starts, so a change here needs a\nrestart even though the rest of the configuration reloads in place.",
      "properties": {
        "logLevel": {
          "default": "info",
          "description": "Log level (`telemetry.log_level`).",
          "enum": [
            "trace",
            "debug",
            "info",
            "warn",
            "error"
          ],
          "required": [],
          "title": "logLevel"
        },
        "sentry": {
          "additionalProperties": true,
          "description": "Sentry error reporting and request tracing. **Off**, and wholly inert while it is: no\nclient, no panic hook, no subscriber layer and no HTTP middleware is installed, and nothing\nleaves the process.\n\nTwo things the chart deliberately does not do for you. It never reads the DSN, so it cannot\nname the ingest host in the NetworkPolicies; a deployment that narrowed the egress rules has\nto name that endpoint itself, and getting it wrong is silent, because an SDK that cannot\nreach its endpoint queues events and then discards them — which reads as \"no errors\". And it\nconfigures nothing inside Sentry: the project, its quota and its server-side data-scrubbing\nrules are yours.\n\nUnlike the rest of this chart's configuration, the block is read once as the process boots\nrather than re-read when the kubelet refreshes the mount, so a change to it takes effect on\nthe next restart. `configMount.rolloutOnChange` is what turns one into a rollout.",
          "properties": {
            "attachStacktraces": {
              "default": true,
              "description": "Attach a stack trace to events that carry none of their own\n(`telemetry.sentry.attach_stacktraces`, plural — `s3-bucket-perma-link` spells the same\nsetting singular, and the wrong spelling is a key this image does not read).",
              "required": [],
              "title": "attachStacktraces",
              "type": "boolean"
            },
            "breadcrumbLevel": {
              "default": "info",
              "description": "Least severe `tracing` level kept as a breadcrumb, the trail attached to the next issue\n(`telemetry.sentry.breadcrumb_level`). Records at or above `captureLevel` become issues\ninstead. Quote `\"off\"`.",
              "enum": [
                "off",
                "error",
                "warn",
                "info",
                "debug",
                "trace"
              ],
              "required": [],
              "title": "breadcrumbLevel"
            },
            "captureLevel": {
              "default": "error",
              "description": "Least severe `tracing` level reported as a Sentry issue\n(`telemetry.sentry.capture_level`). Quote `\"off\"` — unquoted, YAML reads it as the boolean\n`false`. Bounded from above by `logLevel`: the Sentry layer sits under the same filter the\nconsole log does, so a record that level drops is never reported either.",
              "enum": [
                "off",
                "error",
                "warn",
                "info",
                "debug",
                "trace"
              ],
              "required": [],
              "title": "captureLevel"
            },
            "debug": {
              "default": false,
              "description": "Print the SDK's own diagnostics to stderr (`telemetry.sentry.debug`). For proving a DSN\nworks, not for running.",
              "required": [],
              "title": "debug",
              "type": "boolean"
            },
            "dsn": {
              "default": "",
              "description": "Ingest URL, `https://\u003ckey\u003e@\u003chost\u003e/\u003cproject\u003e`. A credential — the embedded key is a\nbearer token for the project's ingest endpoint — so it is delivered as a file in\n`WEBHOOK_REDIRECT_SECRETS_DIR` under `telemetry__sentry__dsn`, alongside the Cloudflare\nAccess service token, and never written into the ConfigMap. Leave it empty and put that\nkey in the Secret named by `existingSecret` to keep it out of `helm get values` entirely.",
              "required": [],
              "title": "dsn",
              "type": "string"
            },
            "enabled": {
              "default": false,
              "description": "Initialise the Sentry client (`telemetry.sentry.enabled`). Off, every other key here is\ninert. On, the proxy refuses to boot without a DSN rather than starting with a reporter\nthat reports nowhere, so this and `dsn` are set together.",
              "required": [],
              "title": "enabled",
              "type": "boolean"
            },
            "environment": {
              "default": "",
              "description": "Environment tag on every event (`telemetry.sentry.environment`). Empty falls to the\nimage's own default of `production`. The proxy always sends one, so `SENTRY_ENVIRONMENT` —\na channel that would bypass the layered loader and its shadow-key rejection entirely — is\nnever consulted.",
              "required": [],
              "title": "environment",
              "type": "string"
            },
            "httpTransactions": {
              "default": true,
              "description": "Record one transaction per request, named by the method and the matched path\n(`telemetry.sentry.http_transactions`). Whether a started transaction is kept is\n`tracesSampleRate`'s decision; this is the switch for a deployment that wants error\nreporting and no performance data at all.",
              "required": [],
              "title": "httpTransactions",
              "type": "boolean"
            },
            "maxBreadcrumbs": {
              "default": 100,
              "description": "How many breadcrumbs one event carries (`telemetry.sentry.max_breadcrumbs`).",
              "minimum": 0,
              "required": [],
              "title": "maxBreadcrumbs",
              "type": "integer"
            },
            "release": {
              "default": "",
              "description": "Release tag on every event (`telemetry.sentry.release`). Empty uses the crate name and\nversion the binary was built from, which is what makes a regression attributable to a\ndeploy — set this only to match a release you create in Sentry under a name of your own.",
              "required": [],
              "title": "release",
              "type": "string"
            },
            "sampleRate": {
              "default": 1,
              "description": "Fraction of captured events actually sent (`telemetry.sentry.sample_rate`). A blunt\nvolume cap — it drops whole issues, not repetitions of one, so a rare error is exactly what\nit loses — so leave it at `1` unless a quota forces otherwise.",
              "maximum": 1,
              "minimum": 0,
              "required": [],
              "title": "sampleRate",
              "type": "number"
            },
            "sendDefaultPii": {
              "default": false,
              "description": "Send personally identifying data with every event: the client IP, the whole request\nheader set, and request bodies of a known content type\n(`telemetry.sentry.send_default_pii`). **Off, and worth leaving off** — every header this\nproxy receives is forwarded to the protected service, so the header set of a webhook\ndelivery routinely carries the caller's own signing secret, which is exactly what a crash\nreport does not need in order to be actionable. It is two controls rather than one,\nbesides: off is also what keeps the HTTP middleware redacting sensitive headers.",
              "required": [],
              "title": "sendDefaultPii",
              "type": "boolean"
            },
            "serverName": {
              "default": "",
              "description": "Host tag on every event (`telemetry.sentry.server_name`). Empty reports none, which is\nthe right answer here: the value would be one replica's pod name, gone by the time anyone\nreads the issue.",
              "required": [],
              "title": "serverName",
              "type": "string"
            },
            "shutdownTimeoutSecs": {
              "default": 2,
              "description": "How long process exit waits for queued events to drain\n(`telemetry.sentry.shutdown_timeout_secs`). Paid on every pod shutdown, so it is time added\nto every rollout; keep it well inside `terminationGracePeriodSeconds`. `0` discards\nwhatever is still queued.",
              "minimum": 0,
              "required": [],
              "title": "shutdownTimeoutSecs",
              "type": "integer"
            },
            "spanAttributes": {
              "default": false,
              "description": "Copy `tracing` span fields onto the Sentry span as attributes\n(`telemetry.sentry.span_attributes`). Off: the request span this proxy opens carries the\nfull request path, and a transaction is retained for longer than a log line.",
              "required": [],
              "title": "spanAttributes",
              "type": "boolean"
            },
            "tracesSampleRate": {
              "default": 0,
              "description": "Fraction of traces this proxy **starts** that are recorded\n(`telemetry.sentry.traces_sample_rate`). `0` starts none, which is what makes the feature\nfree to switch on for error reporting alone. It does not take the proxy out of a trace that\nreaches it already sampled: an inbound `sentry-trace` header is continued whatever this\nsays, and rewritten onto the forwarded request, which is what keeps one webhook delivery\nreadable across the caller, this hop and the protected service behind it.",
              "maximum": 1,
              "minimum": 0,
              "required": [],
              "title": "tracesSampleRate",
              "type": "number"
            }
          },
          "required": [],
          "title": "sentry"
        }
      },
      "required": [],
      "title": "telemetry"
    },
    "terminationGracePeriodSeconds": {
      "default": 30,
      "description": "Grace period for pod shutdown.",
      "required": [],
      "title": "terminationGracePeriodSeconds",
      "type": "integer"
    },
    "tolerations": {
      "description": "Tolerations for taints",
      "items": {
        "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.Toleration",
        "required": []
      },
      "required": [],
      "title": "tolerations",
      "type": "array"
    },
    "topologySpreadConstraints": {
      "description": "Pod topology spread constraints for availability",
      "items": {
        "$ref": "https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.34.0/_definitions.json#/definitions/io.k8s.api.core.v1.TopologySpreadConstraint",
        "required": []
      },
      "required": [],
      "title": "topologySpreadConstraints",
      "type": "array"
    },
    "webhook": {
      "additionalProperties": true,
      "description": "What the proxy forwards and where. Both keys are required and the render fails without them,\nunless `configExtraToml` is set: the chart never parses that, so it stops checking.",
      "properties": {
        "paths": {
          "additionalProperties": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "description": "Path regex to the methods allowed on it (`webhook.paths`). Patterns are anchored, so\n`/webhook/.*` matches `/webhook/github` but not `/api/webhook/github`. Methods are `ALL`,\n`GET`, `POST`, `PUT`, `PATCH` or `DELETE`. Required — a proxy with no allowed path\nforwards nothing.\nExample:\n  \"/webhook/.*\":\n    - ALL\n  \"/api/public/.*\":\n    - GET\n    - POST",
          "required": [],
          "title": "paths",
          "type": "object"
        },
        "targetBase": {
          "default": "",
          "description": "The Cloudflare Access protected service every allowed path is joined onto\n(`webhook.target_base`). Required.",
          "required": [],
          "title": "targetBase",
          "type": "string"
        }
      },
      "required": [],
      "title": "webhook"
    }
  },
  "required": [],
  "type": "object"
}