{
  "uid": "common-fixture-overview",
  "title": "Common Fixture Overview",
  "panels": [
    {
      "type": "timeseries",
      "title": "Requests",
      "targets": [
        {
          "expr": "sum by (job) (rate(http_requests_total[5m]))",
          "legendFormat": "{{job}}"
        }
      ]
    }
  ]
}
